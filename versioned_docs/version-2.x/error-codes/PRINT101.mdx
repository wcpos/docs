---
title: "PRINT101: Autoprint did not start"
sidebar_label: PRINT101
description: "Automatic printing did not start for this receipt."
---

import ErrorMeta from '@site/src/components/ErrorMeta';

## What this means {#what-this-means}

Automatic printing did not start for this receipt.

Print the receipt by hand with **Print Receipt**, then check the automatic-printing settings and the printer.

## Your data {#your-data}

No order or product data is affected.

## Troubleshoot {#troubleshoot}

1. On the receipt shown after the sale, press **Print Receipt** to print it by hand — the sale is unaffected. If no receipt is on screen, on WCPOS Pro open **Orders**, use the order's **Receipt** action and press **Print Receipt**.
2. If no receipt appears after sales at all, or the till never tries to print, the settings are off: open the cart's settings (the sliders icon above the cart) and turn on both **Automatically show receipt after checkout** and **Automatically print receipt after checkout**. Automatic printing happens on that receipt, so it needs both.
3. Check the printer in **Settings → Printing**: the printer you expect is listed under **Printers**, and **Test Print** from its **Edit** screen prints.
4. If **Print Receipt** fails too, it is the printer or its connection — see [PRINT301](/error-codes/PRINT301). If manual printing works but automatic printing keeps failing with both settings on, use **Copy debug info** in **Store health → Logs** and contact support.

## Details {#details}

<ErrorMeta code="PRINT101" />
