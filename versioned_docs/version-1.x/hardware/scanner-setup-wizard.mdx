---
title: Scanner Setup Wizard
sidebar_label: Scanner Setup Wizard
sidebar_position: 3
description: "Answer a few questions and we'll walk you through setting up your barcode scanner — and help you fix it if a scan doesn't come through."
---

import SetupWizard from '@site/src/components/SetupWizard';
import { WizardQuestion, WizardChoice, WizardStep, WizardGate, WizardFix, WizardTerminal } from '@site/src/components/SetupWizard/nodes';

<SetupWizard>

  <WizardQuestion id="start" title="What are you setting up?">
    <WizardChoice value="setup" label="I have a barcode scanner and want to set it up" goTo="platform-setup" />
    <WizardChoice value="trouble" label="My scanner already types scans, but something's wrong" goTo="platform-trouble" />
    <WizardChoice value="camera" label="I don't have a scanner — can I scan with a camera?" goTo="camera" />
  </WizardQuestion>

  <WizardTerminal id="camera" kind="success" title="Use camera scanning — no hardware needed">

WCPOS can scan barcodes with your device's camera: tap the **camera icon** in the POS search bar and point it at the barcode.

It works on phones, tablets, and laptops with a webcam, and it's a fine way to start or to handle the occasional scan. For a busy till, a dedicated scanner is faster — when you get one, come back and run this wizard.

  </WizardTerminal>

  <WizardQuestion id="platform-setup" title="Where do you run WCPOS?">
    <WizardChoice value="desktop" label="Desktop app (Windows, Mac, or Linux)" goTo="kb-first-d" />
    <WizardChoice value="web" label="Web browser" goTo="browser-setup" />
    <WizardChoice value="android" label="Android phone or tablet" goTo="android-register" />
    <WizardChoice value="ios" label="iPhone or iPad" goTo="ios-kb" />
  </WizardQuestion>

  <WizardQuestion id="platform-trouble" title="Where do you run WCPOS?">
    <WizardChoice value="desktop" label="Desktop app (Windows, Mac, or Linux)" goTo="symptom-d" />
    <WizardChoice value="web" label="Web browser" goTo="browser-trouble" />
    <WizardChoice value="android" label="Android phone or tablet" goTo="symptom-a" />
    <WizardChoice value="ios" label="iPhone or iPad" goTo="symptom-i" />
  </WizardQuestion>

  <WizardQuestion id="browser-setup" title="Which browser?">
    <WizardChoice value="chromium" label="Chrome, Edge, or another Chromium-based browser" goTo="kb-first-d" />
    <WizardChoice value="other" label="Safari or Firefox" goTo="kb-only-web" />
  </WizardQuestion>

  <WizardQuestion id="browser-trouble" title="Which browser?">
    <WizardChoice value="chromium" label="Chrome, Edge, or another Chromium-based browser" goTo="symptom-d" />
    <WizardChoice value="other" label="Safari or Firefox" goTo="symptom-w" />
  </WizardQuestion>

  <WizardStep id="kb-first-d" title="Start in keyboard mode — no configuration needed">

Out of the box, nearly every scanner acts as a **keyboard**: it "types" each barcode. WCPOS recognises the fast keystrokes as a scan. There is nothing to configure.

1. **USB scanner:** plug it in — that's it.
2. **Bluetooth scanner:** put it in pairing mode (check the manual — usually holding the trigger or a beep sequence) and pair it in your **system Bluetooth settings**, like any keyboard.
3. Open WCPOS and scan any product barcode. The product should appear and be added to the sale.

    <WizardGate id="g-kb-first-d" question="Scan a product barcode — did the product come up?">
      <WizardChoice value="yes" label="Yes — it's working" goTo="success-kb" />
      <WizardChoice value="no" label="No, or something's not right" goTo="symptom-d" />
    </WizardGate>
  </WizardStep>

  <WizardStep id="kb-first-w" title="Start in keyboard mode — no configuration needed">

Out of the box, nearly every scanner acts as a **keyboard**: it "types" each barcode. WCPOS recognises the fast keystrokes as a scan. There is nothing to configure.

1. **USB scanner:** plug it in — that's it.
2. **Bluetooth scanner:** put it in pairing mode (check the manual — usually holding the trigger or a beep sequence) and pair it in your **system Bluetooth settings**, like any keyboard.
3. Open WCPOS and scan any product barcode. The product should appear and be added to the sale.

    <WizardGate id="g-kb-first-w" question="Scan a product barcode — did the product come up?">
      <WizardChoice value="yes" label="Yes — it's working" goTo="success-kb" />
      <WizardChoice value="no" label="No, or something's not right" goTo="symptom-w" />
    </WizardGate>
  </WizardStep>

  <WizardStep id="kb-only-web" title="Safari and Firefox: keyboard mode (or camera)">

In Safari and Firefox, WCPOS supports scanners in **keyboard mode** and **camera scanning**. A direct scanner connection needs the **Desktop app** or a **Chromium browser** (Chrome/Edge) — the direct-connection APIs aren't available in this browser.

Keyboard mode works well for most stores, so let's set that up.

    <WizardGate id="g-kb-only-web" question="How do you want to continue?">
      <WizardChoice value="continue" label="Continue with keyboard mode" goTo="kb-first-w" />
      <WizardChoice value="switch" label="Switch to the Desktop app or Chrome instead" goTo="kb-first-d" />
    </WizardGate>
  </WizardStep>

  <WizardTerminal id="success-kb" kind="success" title="Keyboard mode is set up — you're done">

Keyboard mode is the right choice for most stores: zero setup, and it works everywhere.

**Fine-tune it:** open <kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd> and use the **test panel** — scan into it and it charts your scanner's keystroke timing, then suggests settings (threshold, prefix/suffix) with one-tap **Apply**.

:::info When to come back
Consider upgrading to a direct connection only if you see one of these: scans come out **garbled**, scans **land in the wrong field**, or scans are **missed when no field is focused**. Restart this wizard and pick "something's wrong" — we'll take you there.
:::

  </WizardTerminal>

  <WizardQuestion id="symptom-d" title="What's going wrong?">
    <WizardChoice value="nothing" label="Nothing happens when I scan" goTo="fix-nothing" />
    <WizardChoice value="garbled" label="Scans come out garbled — wrong characters or mixed-up symbols" goTo="fix-garbled-d" />
    <WizardChoice value="focus" label="Scans land in the wrong place, or get lost when no box is focused" goTo="fix-focus-d" />
  </WizardQuestion>

  <WizardQuestion id="symptom-w" title="What's going wrong?">
    <WizardChoice value="nothing" label="Nothing happens when I scan" goTo="fix-nothing" />
    <WizardChoice value="garbled" label="Scans come out garbled — wrong characters or mixed-up symbols" goTo="fix-garbled-w" />
    <WizardChoice value="focus" label="Scans land in the wrong place, or get lost when no box is focused" goTo="fix-focus-w" />
  </WizardQuestion>

  <WizardQuestion id="symptom-a" title="What's going wrong?">
    <WizardChoice value="nothing" label="Nothing happens when I scan" goTo="fix-nothing" />
    <WizardChoice value="garbled" label="Scans come out garbled — wrong characters or mixed-up symbols" goTo="fix-garbled-a" />
    <WizardChoice value="focus" label="Scans land in the wrong place, or get lost when no box is focused" goTo="fix-focus-a" />
    <WizardChoice value="osk" label="The on-screen keyboard disappeared since I paired the scanner" goTo="fix-osk-a" />
  </WizardQuestion>

  <WizardQuestion id="symptom-i" title="What's going wrong?">
    <WizardChoice value="nothing" label="Nothing happens when I scan" goTo="fix-nothing" />
    <WizardChoice value="garbled" label="Scans come out garbled — wrong characters or mixed-up symbols" goTo="fix-garbled-i" />
    <WizardChoice value="focus" label="Scans land in the wrong place, or get lost when no box is focused" goTo="fix-focus-i" />
    <WizardChoice value="osk" label="The on-screen keyboard disappeared since I paired the scanner" goTo="fix-osk-i" />
  </WizardQuestion>

  <WizardFix id="fix-nothing" title="When scanning does nothing">

1. **Does the scanner react?** Pull the trigger — no beep or light means power/battery, not WCPOS.
2. **Is it connected?** Bluetooth settings must say **Connected**, not just "paired". Re-pair if unsure.
3. **Prove it types:** open any notes app or an empty text field and scan. If characters appear there, the scanner is fine — the POS just isn't recognising the input as a scan.
4. If it types but WCPOS ignores it: open <kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd> and scan into the **test panel**. It shows exactly why each attempt was treated as typing and suggests the fix (e.g. raise the timing threshold) with an **Apply** button.

    <WizardGate id="g-fix-nothing" question="Is scanning working now?">
      <WizardChoice value="yes" label="Yes — fixed" goTo="success-kb" />
      <WizardChoice value="no" label="No, still nothing" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-garbled-d" title="When scans come out garbled">

A keyboard-mode scanner "types" using a **keyboard layout**. If the scanner's layout doesn't match your device's (QWERTY vs AZERTY, symbol positions), characters swap.

1. Scan into the **test panel** (<kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd>). It detects the common cases — like a scanner sending Shift as separate keys — and tells you what it sees.
2. Check your scanner manual for **"keyboard language"** or **"keyboard layout"** setup barcodes and set it to match your device (US layout is the usual safe choice).
3. The most robust fix: a **direct connection**. A directly connected scanner sends the barcode as data — no keyboard layout involved, so nothing can garble.

    <WizardGate id="g-fix-garbled-d" question="How do you want to proceed?">
      <WizardChoice value="fixed" label="The test panel / layout barcode fixed it" goTo="success-kb" />
      <WizardChoice value="direct" label="Set up a direct connection" goTo="conn" />
      <WizardChoice value="help" label="Still garbled — I need help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-garbled-w" title="When scans come out garbled">

A keyboard-mode scanner "types" using a **keyboard layout**. If the scanner's layout doesn't match your device's (QWERTY vs AZERTY, symbol positions), characters swap.

1. Scan into the **test panel** (<kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd>). It detects the common cases — like a scanner sending Shift as separate keys — and tells you what it sees.
2. Check your scanner manual for **"keyboard language"** or **"keyboard layout"** setup barcodes and set it to match your device (US layout is the usual safe choice).

A direct connection needs the **Desktop app** or a **Chromium browser** (Chrome/Edge) — the direct-connection APIs aren't available in this browser.

    <WizardGate id="g-fix-garbled-w" question="How do you want to proceed?">
      <WizardChoice value="fixed" label="The test panel / layout barcode fixed it" goTo="success-kb" />
      <WizardChoice value="help" label="Still garbled — I need help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-garbled-a" title="When scans come out garbled">

A keyboard-mode scanner "types" using a **keyboard layout**. If the scanner's layout doesn't match your device's (QWERTY vs AZERTY, symbol positions), characters swap.

1. Scan into the **test panel** (<kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd>). It detects the common cases — like a scanner sending Shift as separate keys — and tells you what it sees.
2. Check your scanner manual for **"keyboard language"** or **"keyboard layout"** setup barcodes and set it to match your device (US layout is the usual safe choice).
3. On Android, also **register the scanner** in WCPOS — registered scanners are captured by device identity, which removes the timing guesswork (layout barcodes still apply).

    <WizardGate id="g-fix-garbled-a" question="How do you want to proceed?">
      <WizardChoice value="fixed" label="The test panel / layout barcode fixed it" goTo="success-kb" />
      <WizardChoice value="register" label="Register the scanner in WCPOS" goTo="android-register" />
      <WizardChoice value="help" label="Still garbled — I need help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-garbled-i" title="When scans come out garbled">

A keyboard-mode scanner "types" using a **keyboard layout**. If the scanner's layout doesn't match your device's (QWERTY vs AZERTY, symbol positions), characters swap.

1. Scan into the **test panel** (<kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd>). It detects the common cases — like a scanner sending Shift as separate keys — and tells you what it sees.
2. Check your scanner manual for **"keyboard language"** or **"keyboard layout"** setup barcodes and set it to match your device (US layout is the usual safe choice).

    <WizardGate id="g-fix-garbled-i" question="How do you want to proceed?">
      <WizardChoice value="fixed" label="The test panel / layout barcode fixed it" goTo="success-kb" />
      <WizardChoice value="help" label="Still garbled — I need help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-focus-d" title="Scans in the wrong place, or lost without focus">

This is the built-in limit of keyboard mode: the scanner types into **whatever has focus**. If the cursor is in a quantity field, the barcode lands there; if another window is focused, the scan goes to it.

The real fix on this platform is a **direct connection**: every scan is delivered straight to WCPOS — regardless of focus, even with the POS in a background tab.

    <WizardGate id="g-fix-focus-d" question="How do you want to proceed?">
      <WizardChoice value="direct" label="Set up a direct connection" goTo="conn" />
      <WizardChoice value="keep" label="Keep keyboard mode as-is" goTo="success-kb" />
      <WizardChoice value="help" label="I need more help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-focus-w" title="Scans in the wrong place, or lost without focus">

This is the built-in limit of keyboard mode: the scanner types into **whatever has focus**. If the cursor is in a quantity field, the barcode lands there; if another window is focused, the scan goes to it.

On this platform scans always follow the keyboard focus. Practical habits: keep WCPOS in the foreground, and tap the search box before scanning bursts. For focus-independent scanning you'd need the Desktop app or Chrome on a computer, or an Android device.

    <WizardGate id="g-fix-focus-w" question="How do you want to proceed?">
      <WizardChoice value="keep" label="Keep keyboard mode as-is" goTo="success-kb" />
      <WizardChoice value="help" label="I need more help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-focus-a" title="Scans in the wrong place, or lost without focus">

This is the built-in limit of keyboard mode: the scanner types into **whatever has focus**. If the cursor is in a quantity field, the barcode lands there; if another window is focused, the scan goes to it.

The real fix on Android is **registering the scanner** in WCPOS: registered scanners are recognised by device identity and captured directly, so scans stop depending on where the cursor is.

    <WizardGate id="g-fix-focus-a" question="How do you want to proceed?">
      <WizardChoice value="register" label="Register the scanner in WCPOS" goTo="android-register" />
      <WizardChoice value="keep" label="Keep keyboard mode as-is" goTo="success-kb" />
      <WizardChoice value="help" label="I need more help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-focus-i" title="Scans in the wrong place, or lost without focus">

This is the built-in limit of keyboard mode: the scanner types into **whatever has focus**. If the cursor is in a quantity field, the barcode lands there; if another window is focused, the scan goes to it.

On this platform scans always follow the keyboard focus. Practical habits: keep WCPOS in the foreground, and tap the search box before scanning bursts. For focus-independent scanning you'd need the Desktop app or Chrome on a computer, or an Android device.

    <WizardGate id="g-fix-focus-i" question="How do you want to proceed?">
      <WizardChoice value="keep" label="Keep keyboard mode as-is" goTo="success-kb" />
      <WizardChoice value="help" label="I need more help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-osk-a" title="Getting the on-screen keyboard back">

Phones and tablets hide the on-screen keyboard while a hardware keyboard is connected — and a keyboard-mode scanner *is* a hardware keyboard. This is the operating system's behaviour, not a WCPOS setting.

1. **Most scanners have a shortcut to toggle it back** — commonly holding the trigger or a small function button for a few seconds (Inateck: hold the scan button ~3&nbsp;s). Look for *"iOS keyboard"* or *"virtual keyboard"* in your scanner's manual.
2. Or disconnect the scanner in Bluetooth settings when you need to type a lot.
3. On Android, **registering the scanner** in WCPOS avoids this entirely — registered scanners stop counting as a typing keyboard for the POS input fields.

    <WizardGate id="g-fix-osk-a" question="Sorted?">
      <WizardChoice value="yes" label="Yes — keyboard is back" goTo="success-kb" />
      <WizardChoice value="register" label="Register the scanner instead" goTo="android-register" />
      <WizardChoice value="help" label="No — I need help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardFix id="fix-osk-i" title="Getting the on-screen keyboard back">

Phones and tablets hide the on-screen keyboard while a hardware keyboard is connected — and a keyboard-mode scanner *is* a hardware keyboard. This is the operating system's behaviour, not a WCPOS setting.

1. **Most scanners have a shortcut to toggle it back** — commonly holding the trigger or a small function button for a few seconds (Inateck: hold the scan button ~3&nbsp;s). Look for *"iOS keyboard"* or *"virtual keyboard"* in your scanner's manual.
2. Or disconnect the scanner in Bluetooth settings when you need to type a lot.

    <WizardGate id="g-fix-osk-i" question="Sorted?">
      <WizardChoice value="yes" label="Yes — keyboard is back" goTo="success-kb" />
      <WizardChoice value="help" label="No — I need help" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardStep id="android-register" title="Register your scanner — no mode switch needed">

On Android, WCPOS identifies your scanner by its **device identity**, so you can leave it in its factory keyboard mode. No setup barcodes, no re-pairing.

1. Pair the scanner in Android's Bluetooth settings (or plug it in over USB) — exactly as it came out of the box.
2. In WCPOS open <kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd> → **Registered scanners** → tap <kbd>Register scanner</kbd>.
3. Scan **any barcode**. WCPOS detects which device it came from — "Detected *NETUM Bluetooth* — name it and save".
4. Give it a name (e.g. "Front counter") and tap <kbd>Save scanner</kbd>.

From now on every scan from that scanner goes straight to WCPOS — no typing-speed guesswork, and scans aren't lost when no field is focused.

    <WizardGate id="g-android-register" question="Scan a product barcode — did the product come up?">
      <WizardChoice value="yes" label="Yes — working" goTo="success-android" />
      <WizardChoice value="no" label="No" goTo="fix-android" />
    </WizardGate>
  </WizardStep>

  <WizardTerminal id="success-android" kind="success" title="Scanner registered — you're done">

Your scanner now appears under **Registered scanners** and its scans are captured by device identity.

**Verify and fine-tune:** the **test panel** on the same settings page charts each scan and flags anything odd (prefixes, trailing characters) with one-tap fixes.

:::info Good to know
Registration is per-device. If you replace the scanner, just register the new one and remove the old row.
:::

  </WizardTerminal>

  <WizardFix id="fix-android" title="When registration doesn't detect the scanner">

1. Registration listens for keyboard input, so the scanner must be in its **factory keyboard mode**. If it was ever switched to SPP/BLE mode for another app, scan the **"HID mode"** / **"keyboard mode"** barcode in its manual to restore the default, then forget &amp; re-pair it in Bluetooth settings.
2. Bluetooth settings must show **Connected** — not just paired.
3. Prove it types: open any notes app and scan. No characters there means the scanner isn't delivering input at all (battery, pairing).

    <WizardGate id="g-fix-android" question="Try Register scanner again — detected now?">
      <WizardChoice value="yes" label="Yes — registered" goTo="success-android" />
      <WizardChoice value="no" label="Still not detected" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardStep id="ios-kb" title="iPhone &amp; iPad: keyboard mode (or camera)">

On iPhone and iPad, scanners work in **keyboard mode** — pair in <kbd>Settings</kbd> → <kbd>Bluetooth</kbd> and the scanner types into WCPOS like a keyboard. Camera scanning is built in as an alternative. A direct scanner connection isn't available on iPhone/iPad today.

:::warning Known quirk: the on-screen keyboard hides
While a scanner is paired, iOS hides the on-screen keyboard (it thinks a keyboard is attached). Most scanners have a button-hold to bring it back — often holding the trigger or a function button for a few seconds; look for *"iOS keyboard"* in the manual.
:::

Open WCPOS and scan a product barcode to test.

    <WizardGate id="g-ios-kb" question="Did the product come up?">
      <WizardChoice value="yes" label="Yes — it's working" goTo="success-kb" />
      <WizardChoice value="no" label="No, or something's wrong" goTo="symptom-i" />
    </WizardGate>
  </WizardStep>

  <WizardQuestion id="conn" title="How does the scanner connect to this till?">
    <WizardChoice value="bt" label="Bluetooth" goTo="brand" />
    <WizardChoice value="usb" label="USB cable" goTo="brand" />
    <WizardChoice value="dongle" label="A 2.4 GHz USB dongle (came in the box)" goTo="dongle" />
  </WizardQuestion>

  <WizardStep id="dongle" title="The 2.4 GHz dongle is always a keyboard">

The little USB dongle presents itself to the computer as a **keyboard** — there is no direct-connection mode over the dongle, on any brand.

Two options:

- **Keep using the dongle** in keyboard mode — fine for most stores.
- If your scanner also supports Bluetooth (most do), unplug the dongle and connect over **Bluetooth** for a direct connection.

<WizardGate id="g-dongle" question="How do you want to continue?">
  <WizardChoice value="keep" label="Keep the dongle — keyboard mode is fine" goTo="success-kb" />
  <WizardChoice value="bluetooth" label="Connect over Bluetooth instead" goTo="brand" />
</WizardGate>
  </WizardStep>

  <WizardQuestion id="brand" title="What brand is your scanner?">

The next step tells you which setup barcode to find in your manual — each brand names it differently.

    <WizardChoice value="netum" label="Netum" goTo="mode-netum" />
    <WizardChoice value="inateck" label="Inateck" goTo="mode-inateck" />
    <WizardChoice value="eyoyo" label="Eyoyo" goTo="mode-eyoyo" />
    <WizardChoice value="tera" label="Tera" goTo="mode-tera" />
    <WizardChoice value="other" label="Another brand, or not sure" goTo="mode-generic" />
  </WizardQuestion>

  <WizardStep id="mode-netum" title="Switch your Netum into serial (SPP) mode">

Your Netum ships in keyboard (HID) mode. To connect it directly, switch its mode with a barcode from **your model's own manual**:

:::info In the manual, find the section and scan the barcode named:
**`Bluetooth SPP Mode`**

Section: **"Bluetooth Connection Modes"** — Netum's manuals for every model are at [netum.net → Barcode Scanner User Manuals](https://www.netum.net/pages/barcode-scanner-user-manuals). Netum's own description of SPP mode: *"If you have an application that supports our scanner, this is the recommended mode."*
:::

:::warning Forget, then pair again
After switching modes, your device still remembers the old keyboard pairing. Open your **system Bluetooth settings**, forget/remove the scanner, then pair it again. Skipping this is the most common reason the next step fails.
:::

**Connecting over USB instead?** In **"USB Connection Modes"**, scan **`USB COM Port Emulation`** or **`USB Virtual COM`**.

**To undo at any time:** scan the **Bluetooth HID Mode** barcode in the same section — that restores the factory keyboard mode. (Netum's sequence: forget the scanner in Bluetooth settings → Bluetooth off → scan the barcode → Bluetooth on → pair again.)

    <WizardGate id="g-mode-netum" question="Ready to connect it?">
      <WizardChoice value="done" label="Done — mode switched. Connect it in WCPOS" goTo="connect-app" />
    </WizardGate>
  </WizardStep>

  <WizardStep id="mode-inateck" title="Switch your Inateck into serial (SPP) mode">

Inateck uses a **three-barcode sequence** — scan all three, in order, from **your model's own manual**:

:::info In the manual, scan these three barcodes in order:
**`1. Enter Setup`** · **`2. Bluetooth SPP Mode`** · **`3. Save and Exit`**

Every Inateck model's full manual (BCST-42/43/54/55…) is a free PDF at [inateck.com](https://www.inateck.com) → Support → Manuals. The mode barcodes are in the **"Bluetooth Modes"** chapter.
:::

:::warning Forget, then pair again
After switching modes, your device still remembers the old keyboard pairing. Open your **system Bluetooth settings**, forget/remove the scanner, then pair it again. Skipping this is the most common reason the next step fails.
:::

**Connecting over USB instead?** Inateck's manuals don't document a USB serial mode for the BCST scanners — over a USB cable they act as a keyboard. To connect an Inateck directly, use **Bluetooth SPP** instead.

**To undo at any time:** the same sequence with **Bluetooth HID Mode** as step 2 restores the factory keyboard mode. (Note: Inateck's out-of-box default is the 2.4 GHz dongle — "Wireless Adapter Mode".)

    <WizardGate id="g-mode-inateck" question="Ready to connect it?">
      <WizardChoice value="done" label="Done — mode switched. Connect it in WCPOS" goTo="connect-app" />
    </WizardGate>
  </WizardStep>

  <WizardStep id="mode-eyoyo" title="Switch your Eyoyo into serial (SPP) mode">

Switch the mode with a barcode from **your model's own manual** (models with a screen switch modes in the on-device menu instead — look under *Bluetooth mode*):

:::info In the manual's Bluetooth section, scan the barcode named:
**`BT SPP mode`**

Eyoyo hosts manuals per model at [eyoyoscan.com → Download](https://eyoyoscan.com/pages/download) (sign-in required) — keep the paper manual from the box handy.
:::

:::info Handy check — the Bluetooth name tells you the mode
Eyoyo scanners advertise their mode in the Bluetooth device name: `ScannerHID`, `ScannerSPP`, or `ScannerBLE`. After switching, your Bluetooth settings should show `ScannerSPP` — if it still says `ScannerHID`, the barcode didn't take.
:::

:::warning Forget, then pair again
After switching modes, your device still remembers the old keyboard pairing. Open your **system Bluetooth settings**, forget/remove the scanner, then pair it again. Skipping this is the most common reason the next step fails.
:::

**Connecting over USB instead?** Use Bluetooth SPP for a direct connection; the Eyoyo instructions provide no USB serial mode.

**To undo at any time:** scan the **BT HID mode** barcode in the same section (the name shows `ScannerHID` again), then forget &amp; re-pair.

    <WizardGate id="g-mode-eyoyo" question="Ready to connect it?">
      <WizardChoice value="done" label="Done — mode switched. Connect it in WCPOS" goTo="connect-app" />
    </WizardGate>
  </WizardStep>

  <WizardStep id="mode-tera" title="Switch your Tera into serial (SPP) mode">

Tera manuals have a **separate pairing section for each mode**. Find the section for the mode you need and scan its setting barcodes in the order shown:

:::info In the manual, find the pairing section named:
**`Bluetooth SPP`**

Tera's manuals per model are at [tera-digital.com → User Manual](https://tera-digital.com/pages/user-manual).
:::

:::warning Forget, then pair again
After switching modes, your device still remembers the old keyboard pairing. Open your **system Bluetooth settings**, forget/remove the scanner, then pair it again. Skipping this is the most common reason the next step fails.
:::

**Connecting over USB instead?** Find **`USB COM`** or **`USB Virtual COM`**. On Windows, USB-COM may need the vendor's serial driver from the same page.

**To undo at any time:** the **Bluetooth HID** pairing section's barcode restores the factory keyboard mode; forget &amp; re-pair afterwards.

    <WizardGate id="g-mode-tera" question="Ready to connect it?">
      <WizardChoice value="done" label="Done — mode switched. Connect it in WCPOS" goTo="connect-app" />
    </WizardGate>
  </WizardStep>

  <WizardStep id="mode-generic" title="Find the mode barcode in your scanner's manual">

Every scanner brand switches modes the same way — a **setup barcode printed in its manual** — but each brand names it differently. In the manual's Bluetooth or connection chapter, look for a barcode named like:

:::info Words to look for (Bluetooth):
**`SPP mode`** · **`Serial mode`** · **`Data mode`**

No manual in the box? Search the web for *"&lt;brand&gt; &lt;model&gt; user manual PDF"* — scanner vendors publish them freely.
:::

:::warning Forget, then pair again
After switching modes, your device still remembers the old keyboard pairing. Open your **system Bluetooth settings**, forget/remove the scanner, then pair it again. Skipping this is the most common reason the next step fails.
:::

**Connecting over USB instead?** Look for **`USB COM`**, **`Virtual COM / VCP`**, or **`HID-POS`**.

**To undo at any time:** the same section always has a **"HID mode"** / **"keyboard mode"** barcode — that restores the factory default at any time (forget &amp; re-pair afterwards).

:::info If the manual only lists HID / keyboard mode
Then this scanner doesn't offer a direct connection — and that's fine: keyboard mode works with WCPOS out of the box.
:::

    <WizardGate id="g-mode-generic" question="What did you find?">
      <WizardChoice value="found" label="Found it and scanned it — connect it in WCPOS" goTo="connect-app" />
      <WizardChoice value="hid-only" label="My manual only lists HID / keyboard mode" goTo="success-kb" />
    </WizardGate>
  </WizardStep>

  <WizardStep id="connect-app" title="Connect it in WCPOS">

1. Open <kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd> → **Registered scanners**.
2. Press <kbd>Connect serial scanner</kbd> — or <kbd>Connect HID scanner</kbd> for HID-POS.
3. A device chooser opens: the desktop app shows a device list inside the app, while Chrome shows a browser popup. Pick your scanner and confirm.

Once connected, the scanner appears as a row under **Registered scanners** — and reconnects automatically the next time you open WCPOS.

    <WizardGate id="g-connect-app" question="Did your scanner appear in the chooser and connect?">
      <WizardChoice value="yes" label="Yes — connected" goTo="success-direct" />
      <WizardChoice value="no" label="It's not in the list / won't connect" goTo="fix-chooser" />
    </WizardGate>
  </WizardStep>

  <WizardFix id="fix-chooser" title="When the scanner isn't in the chooser">

1. **It's probably still in keyboard mode** — by far the most common cause. The mode barcode may not have taken: scan it again, then power the scanner off and on. Quick test: click into any text field and pull the trigger — if characters appear, it's still a keyboard. On Eyoyo, check the Bluetooth name: it must say `ScannerSPP`.
2. **The old pairing is in the way** (Bluetooth): forget/remove the scanner in your system Bluetooth settings and pair it again *after* the mode switch. The chooser only shows properly paired serial devices.
3. **It shows but won't connect:** close any vendor configuration tool that might be holding the port, move the scanner close, and try again.
4. **Still nothing?** A few scanners identify their serial connection with a vendor-specific **service UUID** the chooser doesn't admit by default. Check the manual's Bluetooth chapter for a long `8-4-4-4-12` code (often near the SPP section), add it under <kbd>Settings</kbd> → <kbd>Barcode Scanning</kbd> with the **Add** button next to the UUID field, then try Connect again.
5. **No UUID in the manual either?** Contact support with your scanner's brand and model.

    <WizardGate id="g-fix-chooser" question="Try Connect again — is it appearing and connected now?">
      <WizardChoice value="yes" label="Yes — connected" goTo="success-direct" />
      <WizardChoice value="no" label="Still not appearing" goTo="support" />
    </WizardGate>
  </WizardFix>

  <WizardTerminal id="success-direct" kind="success" title="Direct connection working — you're done">

Scan a product barcode: it should be found instantly — no matter which field is focused, even with WCPOS in a background tab.

**Verify and fine-tune:** the **test panel** on the same settings page charts every scan and flags prefixes/suffixes with one-tap fixes.

:::info Going back
To return to keyboard mode any time: scan your manual's **HID / keyboard mode** barcode and re-pair. Remove the row under Registered scanners if you no longer need it.
:::

  </WizardTerminal>

  <WizardTerminal id="support" kind="support" title="Let's get you help">

You've hit something this wizard can't fix remotely. Copy the summary below and include it in a message to [support@wcpos.com](mailto:support@wcpos.com) — along with your scanner's **brand and model** — and we'll take it from there.

Tip: this page's link now contains your answers — paste the URL into the support thread and we see exactly where you got stuck.

  </WizardTerminal>

</SetupWizard>
