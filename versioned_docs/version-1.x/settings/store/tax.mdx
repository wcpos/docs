---
title: Tax Settings
sidebar_label: Tax
sidebar_position: 3
description: "Configure tax calculation and display settings in WCPOS, including tax-inclusive pricing, itemised tax display, and WooCommerce Tax (TaxJar) automated taxes."
---

import Image from "@theme/IdealImage";

The Tax Settings control how taxes are calculated and displayed within the POS application.

<div class="image-container">
  <Image
    alt="Tax Settings in the POS Store Settings"
    img="/img/tax-settings.png"
    style={{maxHeight: 500}}
  />
  <p class="image-caption">Tax Settings</p>
</div>

:::warning Important
On initial load, the POS will get the default tax settings from WooCommerce. In general, users should keep the same settings as WooCommerce unless they have a specific need. Having different tax settings than the server will almost certainly cause the POS and WooCommerce to calculate different tax values, which can lead to discrepancies and problems with order synchronization.
:::

## View All Tax Rates {#view-all-tax-rates}

Click the **View all tax rates** button to see all tax rates that have been downloaded to the POS. This shows:

- Tax rate names
- Tax rate percentages
- Applicable regions/locations

This is useful for verifying that the correct tax rates are loaded and for debugging tax calculation issues.

## Tax Calculation Settings {#tax-calculation-settings}

### Prices Entered With Tax {#prices-entered-with-tax}

Determines whether product prices include tax:

| Option | Description |
|--------|-------------|
| **Yes** | Product prices include tax. The POS calculates tax by working backwards from the total price |
| **No** | Product prices do not include tax. The POS adds tax on top of the entered price |

Stores using [WooCommerce Tax](#woocommerce-tax) with automated taxes on are locked to **No** by WooCommerce, and the POS should mirror that.

### Calculate Tax Based On {#calculate-tax-based-on}

Determines the address used for tax calculation:

| Option | Description |
|--------|-------------|
| **Customer shipping address** | Tax rates based on where the order will be shipped |
| **Customer billing address** | Tax rates based on the customer's billing address |
| **Shop base address** | Tax rates based on your store's physical location |

### Shipping Tax Class {#shipping-tax-class}

Determines how shipping costs are taxed:

| Option | Description |
|--------|-------------|
| **Shipping tax class based on cart items** | Shipping tax is calculated based on the tax class of items in the cart |
| **Standard** | Shipping is taxed at the standard tax rate |
| **Reduced rate** | Shipping is taxed at a reduced tax rate |
| **Zero rate** | No tax is applied to shipping costs |

### Rounding {#rounding}

| Option | Description |
|--------|-------------|
| **Round tax at subtotal level** | Tax is calculated for the entire subtotal and then rounded, rather than rounding tax for each individual line item |

WCPOS follows WooCommerce's own tax maths so an order totals the same whether it's rung up at the till or placed on your website:

- **Full precision** — line and order totals are held at full precision and only rounded for display, so the amount saved to the order is the exact figure WooCommerce expects rather than a value pre-rounded to the display decimals.
- **Store rounding** — rounding uses your currency's cents precision and honours the **Round tax at subtotal level** setting above.
- **Compound taxes** — when a rate is marked *compound*, rates are applied in WooCommerce's priority order (`tax_rate_priority`), so a tax that stacks on top of another is calculated after it.

## Display Settings {#display-settings}

### Display Prices in the Shop {#display-prices-in-the-shop}

| Option | Description |
|--------|-------------|
| **Including tax** | Prices shown include tax amounts |
| **Excluding tax** | Prices shown do not include tax |

### Display Prices During Cart and Checkout {#display-prices-during-cart-and-checkout}

| Option | Description |
|--------|-------------|
| **Including tax** | Cart and checkout prices include tax |
| **Excluding tax** | Cart and checkout prices exclude tax |

### Price Display Suffix {#price-display-suffix}

Text that appears after prices to indicate tax status (e.g., "inc. VAT" or "ex. VAT").

### Display Tax Totals {#display-tax-totals}

| Option | Description |
|--------|-------------|
| **As a single total** | All taxes are combined into one total line |
| **Itemized** | Each tax rate is shown as a separate line item |

## Restore Server Settings {#restore-server-settings}

Click to fetch and apply the current WooCommerce tax settings from your server.

:::tip How and when to use it
WooCommerce is the source of truth for tax settings — the POS should mirror it. **Restore Server Settings** is the safe way to pull the latest configuration from your store, and is the right action whenever you've changed tax settings in `WP Admin > WooCommerce > Settings > Tax` and want the POS to pick them up.

The only time to think twice: if you've **deliberately** overridden a POS-side tax setting to differ from the server (an advanced setup), Restore Server Settings will overwrite that override. If you're not sure whether you've done that, you almost certainly haven't.
:::

## WooCommerce Tax (Automated Taxes) {#woocommerce-tax}

WCPOS works with [WooCommerce Tax](https://woocommerce.com/document/woocommerce-tax/), the WooCommerce plugin that looks up sales tax automatically through TaxJar instead of you maintaining a rate table by hand. POS sales get the same jurisdiction rates as your online orders.

### How it works {#how-woocommerce-tax-works}

WooCommerce Tax only asks TaxJar for rates when an order goes through the online checkout or is edited on the WooCommerce order screen. POS orders take neither path, so WCPOS asks on their behalf:

1. When the POS saves an open order, WCPOS asks WooCommerce Tax for the rates at the order's tax location. The location follows your **Calculate Tax Based On** setting: the shop base address, or the customer's billing or shipping address.
2. WooCommerce Tax writes those rates into your store's normal tax rate table as one row per jurisdiction, exactly as it does for an online order. You will see them under `WP Admin > WooCommerce > Settings > Tax > Standard rates`, keyed by postcode and city.
3. WooCommerce applies the rows to the order. The order carries one tax line per jurisdiction, with WooCommerce Tax's own names (for example *CA STATE TAX*, *CA COUNTY TAX*).
4. The POS downloads the same rows the next time it syncs tax rates, so its local totals agree with your store from then on. **Restore Server Settings** forces that sync.

Adding or removing lines on an open POS order recalculates its tax from the current lines. Once an order is paid, WCPOS leaves it alone and WooCommerce Tax behaves as it does for any other order.

### Setup {#woocommerce-tax-setup}

1. Install and activate WooCommerce Tax and connect it to WordPress.com. `WP Admin > WooCommerce > Status > WooCommerce Tax` should say **Connected to WordPress.com**.
2. Under `WP Admin > WooCommerce > Settings > Tax`, set **Automated taxes** to **Enable automated taxes**. Your store must be in a country WooCommerce Tax supports. Turning this on backs up any rates you entered by hand to a CSV file and removes them from the table, so download the backup from `WP Admin > WooCommerce > Status > WooCommerce Tax` if you might need them again.
3. In the POS, open **Tax Settings** and click **Restore Server Settings**. WooCommerce Tax locks prices to entered and displayed *excluding* tax while automated taxes are on, and the POS has to mirror that.
4. Ring up a test sale for a taxable product. Check the order in WP Admin: it should carry jurisdiction-named tax lines, and the matching rows should have appeared under **Standard rates**.

Nothing needs to be switched on in WCPOS. With automated taxes off, the POS uses your store's tax rate table as usual.

### What to expect {#woocommerce-tax-what-to-expect}

- **The first sale at a new location can disagree once.** The POS totals a sale from the rate rows it has already downloaded. If your store has never taxed an address before, there is no row for it yet, so the POS may show the [totals notice](/support/troubleshooting/totals-disagree) on that one sale. Your store's figures are what is recorded. After the POS syncs rates, later sales at that location agree. With **Calculate Tax Based On** set to the shop base address this normally happens only on the very first sale, and again if a jurisdiction changes its rate. With a customer address, it can happen once per new postcode.
- **Fees are not sent to TaxJar.** A taxable fee is taxed with whatever rate your table already holds for the fee's tax class, which is how WooCommerce Tax treats fees on its own order screen too.
- **A failed lookup never blocks a sale.** If TaxJar cannot be reached, or WooCommerce Tax declines to look the order up (no postcode on the address, a tax-exempt customer, a location where your store has no nexus), the order still saves with the rates already in your table. The POS logs on your server record which it was: *WooCommerce Tax rate priming failed* for an outage, *WooCommerce Tax returned no rates for the POS order* for a decline. See [Logs](/support/logs).
- **Stripe Tax is different.** Stripe Tax does not work with POS orders at all. See [Plugin Conflicts](/support/troubleshooting/plugin-conflicts).

## Common Issues {#common-issues}

### POS and WooCommerce Showing Different Totals {#pos-and-woocommerce-showing-different-totals}

The most frequent tax problem is a mismatch between POS tax settings and WooCommerce tax settings. When these diverge, the same order will calculate different totals depending on where it's placed. Always keep POS settings in sync — use **Restore Server Settings** after changing anything in `WP Admin > WooCommerce > Settings > Tax`.

Stores using WooCommerce Tax have one more cause to rule out: the first sale at a location your store has never taxed. See [What to expect](#woocommerce-tax-what-to-expect).

### Tax-Inclusive Pricing Confusion {#tax-inclusive-pricing-confusion}

If "Prices Entered With Tax" is set to **Yes** but "Display Prices in the Shop" is set to **Excluding tax**, the displayed price will be lower than the entered price. This is correct behaviour — the POS is stripping the tax component for display — but it can surprise cashiers if they're expecting to see the sticker price.

### Customer Address and Tax Calculation {#customer-address-and-tax-calculation}

When "Calculate Tax Based On" is set to **Customer shipping address** or **Customer billing address**, the POS needs a valid address to look up the correct rate. If a walk-in customer has no address on file, WCPOS falls back to the **shop base address** for calculation. This is usually the right behaviour for in-store sales, but if you see unexpected rates for specific customers, check their address details.

## Troubleshooting {#troubleshooting}

### Tax Amounts Are Incorrect {#tax-amounts-are-incorrect}

1. Go to **Tax Settings** in the POS and click **Restore Server Settings** to pull the latest configuration from WooCommerce.
2. Click **View all tax rates** and confirm the expected rates appear. If rates are missing, check that they're properly configured in `WP Admin > WooCommerce > Settings > Tax > Standard rates`. With [WooCommerce Tax](#woocommerce-tax) automated taxes on, do not add rows by hand: a location's rows appear on the first sale there, and the POS picks them up after **Restore Server Settings**.
3. Verify the product's tax class — products assigned to a "Zero rate" tax class won't have tax applied regardless of settings.

### Tax Is Showing as Zero {#tax-is-showing-as-zero}

- Check that tax is enabled in WooCommerce (`WP Admin > WooCommerce > Settings > General > Enable tax rates and calculations`).
- Use **View all tax rates** in the POS to confirm rates have been downloaded. If the list is empty, try a full sync.
- Confirm the product's tax status is set to "Taxable" (not "None" or "Shipping only").

### Rounding Discrepancies {#rounding-discrepancies}

Small rounding differences (a penny or two) between POS and WooCommerce can occur when "Round tax at subtotal level" is enabled in one system but not the other. Ensure this setting matches in both places.
