---
title: "CHECKOUT211: Checkout empty response"
sidebar_label: CHECKOUT211
description: "The store returned no checkout result, so the order status is unknown."
---

import ErrorMeta from '@site/src/components/ErrorMeta';

## What this means {#what-this-means}

The store returned no checkout result, so the order status is unknown.

Check whether the order was created before trying checkout again.

## Your data {#your-data}

The outcome could not be confirmed — verify before retrying. If this persists, export diagnostics and contact WCPOS support.

## Troubleshoot {#troubleshoot}

**At the till:** sync the POS and check whether the order appears in the till's order list before doing anything else — an empty response often means the order was created but the confirmation never arrived. Only retry checkout once you have confirmed no order was created.

**On the store** — for the site administrator:

1. Check WooCommerce → Orders in WP Admin to confirm whether the order was created.
2. Check WooCommerce → Status → Logs for a fatal error during checkout — a PHP crash after the order is created produces exactly this.

**For a developer:** in the browser's developer tools (Network tab), the checkout request with a success status but an empty or truncated body points at a plugin or server buffer cutting the response short.

## Details {#details}

<ErrorMeta code="CHECKOUT211" />
