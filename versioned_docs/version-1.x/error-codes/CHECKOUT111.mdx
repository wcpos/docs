---
title: "CHECKOUT111: Cart update failed"
sidebar_label: CHECKOUT111
description: "This change could not be applied to the cart, which is unchanged."
---

import ErrorMeta from '@site/src/components/ErrorMeta';

## What this means {#what-this-means}

This change could not be applied to the cart, which is unchanged.

The item, fee, shipping line, or coupon was not added and nothing was removed. Try the action again.

## Your data {#your-data}

The order itself is safe and unchanged.

## Troubleshoot {#troubleshoot}

1. Try adding the item, fee, shipping line or coupon again — the cart itself is unchanged.
2. If the same item keeps failing, try a different product once: one failing item points at that record, everything failing points at the device.
3. If it keeps failing, restart WCPOS; export debug info and contact support if it continues.

## Details {#details}

<ErrorMeta code="CHECKOUT111" />
