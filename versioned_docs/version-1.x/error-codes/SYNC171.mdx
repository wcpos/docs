---
title: "SYNC171: Local DB setup failed"
sidebar_label: SYNC171
description: "A local database on this device could not be created or removed."
---

import ErrorMeta from '@site/src/components/ErrorMeta';

## What this means {#what-this-means}

A local database on this device could not be created or removed.

Restart WCPOS and try again. If it keeps failing, the device may be low on storage or the browser profile may be restricting storage; free up space, then contact support if it persists.

## Your data {#your-data}

This action could not be saved on the device — the local database itself could not be set up. Free up storage and retry; if it persists, export diagnostics and contact WCPOS support.

## Troubleshoot {#troubleshoot}

1. Restart WCPOS and try the same action again — most setup failures are transient.
2. Check free storage on the device; creating a local database fails outright when the disk or the browser's storage quota is full.
3. On web, check that the browser allows persistent storage for the site — private windows and block-all-data settings do not.
4. If it keeps failing, export debug info and contact support.

## Details {#details}

<ErrorMeta code="SYNC171" />
