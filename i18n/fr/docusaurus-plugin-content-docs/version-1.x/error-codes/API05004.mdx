---
title: "API05004 : API WordPress Désactivée"
sidebar_label: API05004
---

# API05004 : API WordPress Désactivée

## Ce Que Cela Signifie

L'API REST de WordPress est désactivée sur votre site. Toutes les fonctionnalités modernes de WordPress, y compris WooCommerce et WCPOS, dépendent de cette API.

## Causes Courantes

- **Plugin de sécurité** — Un plugin bloque l'accès à l'API REST
- **Restriction d'hébergement** — Votre hébergeur a désactivé l'API REST
- **Code personnalisé** — Un thème ou un plugin a désactivé l'API
- **Règles de pare-feu** — Un WAF bloque les points de terminaison de l'API REST

## Comment Résoudre

### 1. Tester l'API REST

Visitez `https://yoursite.com/wp-json/` dans votre navigateur :
- Doit renvoyer du JSON avec les routes disponibles
- Si vous obtenez une erreur ou rien, elle est bloquée

### 2. Vérifier les Plugins de Sécurité

Plugins courants bloquant l'API REST :

**Wordfence :**
- Pare-feu → Toutes les options de pare-feu
- Désactiver l'option "Désactiver l'API REST"

**iThemes Security :**
- Sécurité → Paramètres → Ajustements WordPress
- Activer l'API REST

**Plugin Disable REST API :**
- Désactiver complètement ce plugin

### 3. Vérifier le Code Personnalisé

Recherchez dans le `functions.php` de votre thème ou dans les plugins personnalisés :

```php
// Ce code désactive l'API REST - supprimez-le
add_filter('rest_authentication_errors', function($result) {
    return new WP_Error('rest_disabled', 'REST API disabled');
});
```

### 4. Vérifier .htaccess

Supprimez toute règle bloquant `/wp-json/` :

```apache
# Mauvais - bloque l'API REST
RewriteRule ^wp-json - [F,L]
```

### 5. Contacter le Fournisseur d'Hébergement

Certains hébergeurs bloquent l'API REST par défaut :
- Demander de l'activer
- Demander des informations sur les restrictions de sécurité
- Consulter la documentation d'hébergement

## Pourquoi l'API REST Est Importante

L'API REST de WordPress est essentielle pour :
- Applications mobiles
- Intégrations tierces
- Fonctions WooCommerce
- Fonctionnement de WCPOS

La désactiver casse de nombreuses fonctionnalités.

## Erreurs Associées

- [API05001](./API05001) — API WooCommerce Désactivée
- [API03006](./API03006) — Méthode Non Supportée