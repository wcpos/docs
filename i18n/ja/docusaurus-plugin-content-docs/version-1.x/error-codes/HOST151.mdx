---
title: "HOST151: キャッシュが共有されて再生されています"
sidebar_label: HOST151
description: "店舗の前段にあるキャッシュが、ある人のAPI応答をすべての人に再送信しています。"
---

import ErrorMeta from '@site/src/components/ErrorMeta';

## この意味 {#what-this-means}

店舗の前段にあるキャッシュが、ある人のAPI応答をすべての人に再送信しています。

キャッシュ層が、リクエスト元を確認せずに保存済みREST API応答を返しています。アプリは認証情報の異なる2つのプローブを送信しましたが、どちらにも最初のプローブへの応答が返されました。稼働中の店舗で起きると、あるレジ係が別のレジ係のデータを見られるため、キャッシュが店舗のAPIを除外するまで接続はブロックされます。これはホスティング層の問題です。WordPress自体は常にユーザーごとに応答します。

## お客様のデータ {#your-data}

このデバイスのデータが危険にさらされている可能性があります — ローカルデータを消去しないでください。この状態が続く場合は、診断情報をエクスポートして WCPOS サポートに連絡してください。

## トラブルシューティング {#troubleshoot}

1. ストアの REST API パス（/wp-json/ と /?rest_route=）をページキャッシュから除外してください — LiteSpeed では REST のキャッシュを無効にするか no-cache ルールを追加し、WP Engine では WooCommerce の除外設定を /wp-json/wcpos まで拡張し、Sucuri ではキャッシュレベルを Cache-Control を尊重する設定にします。
2. ストアの API はすでに Cache-Control: no-store と Vary: Authorization を送信しています — キャッシュ層がそれらを無視しています。ホストにそれらのヘッダーを確認するよう伝えてください。
3. キャッシュ設定を変更したら、再接続する前にキャッシュをパージしてください。

## 詳細 {#details}

<ErrorMeta code="HOST151" />
