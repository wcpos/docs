---
title: "AUTH311: REST 경로 없음"
sidebar_label: AUTH311
description: "WCPOS 스토어 경로를 사용할 수 없습니다."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## 이것이 의미하는 바 {#what-this-means}

WCPOS 스토어 경로를 사용할 수 없습니다.

보안 또는 프록시 플러그인이 WCPOS 요청 헤더를 제거하고 있는지 사이트 관리자에게 확인을 요청하세요.

## 조치 방법 {#what-to-do}

이 기능이 작동하려면 먼저 설정을 변경해야 합니다. 위의 변경을 적용한 후에 재시도하세요. 변경하지 않고 재시도하면 같은 방식으로 실패합니다.

## 내 데이터 {#your-data}

주문이나 상품 데이터에는 영향이 없습니다. 이 문제가 계속되면 WordPress 사이트를 관리하는 담당자에게 문의하세요.

## 문제 해결 {#troubleshoot}

1. 사이트에서 WCPOS가 활성화되어 있는지 확인하세요: WP Admin → 플러그인.
2. 지원되는 두 가지 REST 형식을 브라우저에서 모두 테스트하세요. 스토어의 /wp-json/ 주소와 /index.php?rest_route=/ 주소입니다. 두 주소 모두 JSON을 반환하지 않는 경우에만 REST 차단으로 진단하세요.
3. 두 REST 형식이 모두 작동하지 않는다면 보안 플러그인, 프록시 또는 CDN이 WCPOS 요청 헤더를 제거하거나 REST 요청을 차단하고 있는지 사이트 관리자에게 문의하세요. 방화벽 규칙과 REST API 비활성화 강화 옵션이 흔한 원인입니다.
4. 일부 호스트에서는 고유주소를 다시 저장하면(설정 → 고유주소 → 저장) 누락된 REST 경로가 복구됩니다.

## 확인할 위치 {#where-to-look}

WCPOS가 이 오류를 저장할 수 있는 경우, 오류는 이를 발생시킨 기기에 기록됩니다. **스토어 상태 → 로그**(내비게이션 서랍 하단의 심박 아이콘)를 열고 이 코드가 표시된 항목을 찾아 펼치세요. 펼쳐진 행에는 알기 쉽게 설명된 원인과 오류가 발생한 시점에 수집된 컨텍스트가 표시됩니다. 스토어 요청의 경우 이 컨텍스트에 서버 자체의 오류 코드(`serverCode`), HTTP `status` 또는 `endpoint`가 포함될 수 있으며, 표시되는 필드는 오류가 발생한 위치에 따라 다릅니다. 문제를 신고할 때는 스크린샷 대신 로그 화면 상단의 **디버그 정보 복사**(휴대폰과 태블릿에서는 **디버그 정보 공유**)를 사용하세요. 여기에는 앱 버전, 연결 상태, 가장 최근의 오류가 함께 담깁니다. 로그는 최대 30일간만 보관되므로 문제가 발생한 직후에 수집하세요. 또한 POS가 자체 로그 항목을 기록하기 전에 나타난 브라우저 콘솔 오류가 있다면 함께 복사하세요.

앱 내 로그 외에도 이 오류는 다음 위치에 흔적을 남길 수 있습니다:

- **네트워크 검사기**(웹 및 데스크톱): 개발자 도구를 여세요. 브라우저에서는 F12를 누르고, 데스크톱 앱에서는 메뉴의 **고급 → 개발자 도구 전환**을 선택한 다음 **네트워크** 탭을 선택하세요. 먼저 이 페이지의 재시도 안내를 따르고, 안전하다고 안내된 경우에만 해당 작업을 재현하세요. 실패한 요청에는 HTTP 상태와 원시 응답 본문이 표시되며, 여기에는 POS 로그에 전혀 도달하지 않는 오류 페이지도 포함됩니다.
- **WooCommerce → 상태 → 로그**: WordPress 사이트에서 가장 최근의 `fatal-errors-*.log`에 PHP 충돌이 있는지 확인하고, 이 오류와 관련된 플러그인 이름이 붙은 로그 소스도 함께 확인하세요. 스토어에서 발생한 500번대 오류는 거의 항상 그 원인을 여기에 남깁니다.

## 세부 정보 {#details}

- **코드:** `AUTH311` (`REST_ROUTE_MISSING`)
- **심각도:** error
- **도입 버전:** WCPOS 1.10.0
