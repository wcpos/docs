---
title: "SYNC131: Error del servidor de la tienda"
sidebar_label: SYNC131
description: "Su tienda devolvió un error, por lo que esta acción no se completó."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Qué significa esto {#what-this-means}

Su tienda devolvió un error, por lo que esta acción no se completó.

Se llegó a la tienda, pero su servidor no pudo gestionar la solicitud. Este es un problema del sitio web y no del POS, por lo que reintentar por sí solo puede no ayudar: revise el registro de errores del sitio o pida al proveedor de alojamiento que lo haga, y reinténtelo después. No se ha perdido nada en este dispositivo.

## Qué hacer {#what-to-do}

Vuelva a intentar la acción. WCPOS no lo reintenta por sí solo: reinténtelo cuando esté listo.

## Sus datos {#your-data}

El cambio se ha guardado en este dispositivo, pero no ha llegado a su tienda. Si esto persiste, exporte los diagnósticos y contacte con el soporte de WCPOS.

## Resolución de problemas {#troubleshoot}

1. Expanda la entrada del registro y anote el status, el endpoint y el serverCode — indican qué estaba haciendo el servidor cuando falló.
2. Abra WooCommerce → Estado → Registros en el sitio y revise el registro de errores fatales más reciente en torno a la hora del fallo; una respuesta de clase 500 casi siempre deja allí un error de PHP.
3. Si el fallo comenzó tras la actualización de un plugin, un tema o PHP en el sitio, sospeche primero de ese cambio y reviértalo o desactívelo.
4. Si los registros de WooCommerce no muestran nada, solicite al proveedor de alojamiento el registro de errores de PHP del servidor para esa misma franja horaria.
5. Una vez corregida la causa en el servidor, reinténtelo desde el POS — el cambio sigue guardado en este dispositivo.

## Dónde buscar {#where-to-look}

Cuando WCPOS puede guardar este error, queda registrado en el dispositivo que lo generó. Abra **Estado de la tienda → Registros** (el icono de latido situado al final del cajón de navegación), busque la entrada marcada con este código y expándala: la fila expandida muestra el motivo en lenguaje sencillo y el contexto capturado en el momento del fallo. En el caso de una solicitud a la tienda, ese contexto puede incluir el código de error propio del servidor (`serverCode`), el `status` HTTP o el `endpoint`; los campos mostrados dependen de dónde se haya producido el fallo. Al informar de un problema, utilice **Copiar información de depuración** en la parte superior de la pantalla de Registros (**Compartir información de depuración** en teléfonos y tabletas) en lugar de capturas de pantalla: agrupa la versión de la aplicación, el estado de la conexión y los errores más recientes. Los registros se conservan un máximo de 30 días, así que recójalos mientras el problema esté reciente. Copie también cualquier error de la consola del navegador que haya aparecido antes de que el POS pudiera escribir su propia entrada de registro.

Más allá del registro dentro de la aplicación, este fallo puede dejar rastro en:

- **Inspector de red** (web y escritorio): abra las herramientas de desarrollo —pulse F12 en el navegador, o **Avanzado → Alternar herramientas para desarrolladores** en el menú de la aplicación de escritorio— y seleccione la pestaña **Red**. Siga primero las indicaciones de reintento de esta página; reproduzca la acción solo cuando esos pasos indiquen que es seguro. Una solicitud fallida muestra el estado HTTP y el cuerpo de la respuesta sin procesar, incluidas las páginas de error que nunca llegan al registro del POS.
- **Registros del POS en el servidor**: en WP Admin, abra **POS → Ajustes → Herramientas → Registros**. Esta página registra las advertencias y los errores relacionados con el POS que se producen en el propio servidor y que pueden no aparecer nunca dentro de la aplicación. Una insignia roja en el menú indica que hay errores del servidor sin leer.
- **WooCommerce → Estado → Registros**: en el sitio de WordPress, revise el archivo `fatal-errors-*.log` más reciente en busca de errores fatales de PHP, además de cualquier fuente de registro que lleve el nombre de un plugin implicado en el fallo. Un error de clase 500 procedente de la tienda casi siempre deja aquí su causa.
- **El registro de errores de PHP del sitio**: si la página de registros de WooCommerce no muestra nada para la hora del fallo, solicite al proveedor de alojamiento el registro de errores de PHP — algunos errores fatales solo se capturan a nivel de servidor.

## Detalles {#details}

- **Código:** `SYNC131` (`STORE_SERVER_ERROR`)
- **Gravedad:** error
- **Introducido en:** WCPOS 1.10.0
