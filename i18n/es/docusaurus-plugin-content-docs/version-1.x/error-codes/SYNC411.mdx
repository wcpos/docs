---
title: "SYNC411: Avalancha de solicitudes bajo demanda"
sidebar_label: SYNC411
description: "Este dispositivo solicitó datos a la tienda con mucha más frecuencia de lo normal."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Qué significa esto {#what-this-means}

Este dispositivo solicitó datos a la tienda con mucha más frecuencia de lo normal.

No se ha ralentizado ni bloqueado nada, y no se ve afectado ningún dato de pedidos — se trata de una alarma únicamente de detección. Las avalanchas sostenidas de solicitudes suelen indicar un problema de la aplicación (una pantalla que vuelve a pedir los mismos datos en bucle) más que algo que usted haya hecho. Si este código sigue apareciendo, exporte los diagnósticos y contacte con el soporte para que se pueda localizar y corregir el bucle.

## Qué hacer {#what-to-do}

Puede seguir trabajando: WCPOS se encarga de esto automáticamente. WCPOS lo reintenta automáticamente.

## Sus datos {#your-data}

No se ve afectado ningún dato de pedidos ni de productos. Si esto persiste, exporte los diagnósticos y contacte con el soporte de WCPOS.

## Resolución de problemas {#troubleshoot}

1. Siga trabajando — se trata de una alarma únicamente de detección. WCPOS no ralentiza, encola ni descarta ninguna solicitud, y no se ve afectado ningún dato de pedidos ni de productos.
2. Si el código aparece una vez y no vuelve, no es necesario hacer nada. Si se repite, anote qué pantalla estaba abierta y qué acción estaba realizando cuando apareció.
3. Utilice Copiar información de depuración mientras el evento siga dentro de la ventana de registro de 30 días y contacte con el soporte de WCPOS; la exportación permite al soporte seguir el bucle de solicitudes.

## Dónde buscar {#where-to-look}

Cuando WCPOS puede guardar este error, queda registrado en el dispositivo que lo generó. Abra **Estado de la tienda → Registros** (el icono de latido situado al final del cajón de navegación), busque la entrada marcada con este código y expándala: la fila expandida muestra el motivo en lenguaje sencillo y el contexto capturado en el momento del fallo. En el caso de una solicitud a la tienda, ese contexto puede incluir el código de error propio del servidor (`serverCode`), el `status` HTTP o el `endpoint`; los campos mostrados dependen de dónde se haya producido el fallo. Al informar de un problema, utilice **Copiar información de depuración** en la parte superior de la pantalla de Registros (**Compartir información de depuración** en teléfonos y tabletas) en lugar de capturas de pantalla: agrupa la versión de la aplicación, el estado de la conexión y los errores más recientes. Los registros se conservan un máximo de 30 días, así que recójalos mientras el problema esté reciente. Copie también cualquier error de la consola del navegador que haya aparecido antes de que el POS pudiera escribir su propia entrada de registro.

## Detalles {#details}

- **Código:** `SYNC411` (`DEMAND_REQUEST_FLOOD`)
- **Gravedad:** warn
- **Introducido en:** WCPOS 1.10.0
