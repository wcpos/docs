---
title: "LICENSE999: Problema inesperado con la licencia"
sidebar_label: LICENSE999
description: "La comprobación de la licencia ha encontrado un problema inesperado."
---

{/* PÁGINA GENERADA — no editar. Fuente de referencia: packages/utils/src/logger/error-registry.json en wcpos/monorepo. Regenerar con `pnpm generate:error-codes`. */}

## Qué significa esto {#what-this-means}

La comprobación de la licencia ha encontrado un problema inesperado.

El POS sigue funcionando. Si las funciones Pro permanecen bloqueadas, vuelva a introducir la clave de licencia y contacte con el soporte técnico.

## Qué hacer {#what-to-do}

Puede seguir trabajando — WCPOS gestiona esto automáticamente. WCPOS vuelve a intentarlo automáticamente.

## Sus datos {#your-data}

No se ven afectados los datos de pedidos ni productos. Si el problema persiste, exporte los diagnósticos y contacte con el soporte de WCPOS.

## Solución de problemas {#troubleshoot}

1. Siga trabajando: el POS no se bloquea por las comprobaciones de licencia.
2. Si las funciones Pro siguen bloqueadas, vuelva a introducir la clave de licencia en los ajustes del POS.
3. Si esto no lo soluciona, exporte la información de depuración y póngase en contacto con el soporte.

## Dónde buscar {#where-to-look}

Cada aparición de este código se registra en el dispositivo que lo generó. Abra **Estado de la tienda → Registros** (el icono de pulso cardíaco situado en la parte inferior del panel de navegación), busque la entrada marcada con este código y expándala: la fila expandida muestra el motivo en lenguaje sencillo y el contexto capturado en el momento del fallo; para cualquier elemento que implicara una solicitud a su tienda, esto incluye el propio código de error del servidor (`serverCode`), el `status` HTTP y el `endpoint`. Al informar de un problema, use **Copiar información de depuración** en la parte superior de la pantalla Registros (**Compartir información de depuración** en teléfonos y tabletas) en lugar de capturas de pantalla: incluye la versión de la aplicación, el estado de la conexión y los errores más recientes. Los registros se conservan durante un máximo de 30 días, por lo que conviene recopilarlos mientras el problema esté reciente.

## Detalles {#details}

- **Código:** `LICENSE999` (`LICENSE_UNEXPECTED`)
- **Gravedad:** error
- **Incorporado en:** WCPOS 1.10.0
