---
title: "SYNC161: Base de datos local no disponible"
sidebar_label: SYNC161
description: "La base de datos local de este dispositivo ha dejado de responder, por lo que las acciones que la necesitan están en pausa."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Qué significa esto {#what-this-means}

La base de datos local de este dispositivo ha dejado de responder, por lo que las acciones que la necesitan están en pausa.

Reinicie WCPOS para reconectar la base de datos local. Es posible que los cambios sin guardar de este dispositivo no se hayan escrito; revise los pedidos recientes después de reiniciar. Si sigue ocurriendo, exporte los diagnósticos y contacte con el soporte.

## Qué hacer {#what-to-do}

Reinicie WCPOS; si esto sigue ocurriendo, los datos locales de este dispositivo necesitan reparación. WCPOS no lo reintenta por sí solo: reinténtelo cuando esté listo.

## Sus datos {#your-data}

Los datos de este dispositivo pueden estar en riesgo: no borre los datos locales. Si esto persiste, exporte los diagnósticos y contacte con el soporte de WCPOS.

## Resolución de problemas {#troubleshoot}

1. Reinicie WCPOS — cerrar y volver a abrir la aplicación (o la pestaña del navegador) reconecta la base de datos local en casi todos los casos.
2. Después de reiniciar, revise los pedidos más recientes: es posible que un cambio realizado en los segundos previos al fallo no se haya escrito.
3. En la web, evite las ventanas privadas o de incógnito y las extensiones agresivas de limpieza de almacenamiento — ambas pueden destruir el almacenamiento local en mitad de una sesión.
4. Si esto se repite con regularidad en el mismo dispositivo, exporte la información de depuración y contacte con el soporte, indicando qué estaba haciendo el dispositivo (suspensión, cambio de pestaña, poca memoria) cuando ocurrió.

## Dónde buscar {#where-to-look}

Cuando WCPOS puede guardar este error, queda registrado en el dispositivo que lo generó. Abra **Estado de la tienda → Registros** (el icono de latido situado al final del cajón de navegación), busque la entrada marcada con este código y expándala: la fila expandida muestra el motivo en lenguaje sencillo y el contexto capturado en el momento del fallo. En el caso de una solicitud a la tienda, ese contexto puede incluir el código de error propio del servidor (`serverCode`), el `status` HTTP o el `endpoint`; los campos mostrados dependen de dónde se haya producido el fallo. Al informar de un problema, utilice **Copiar información de depuración** en la parte superior de la pantalla de Registros (**Compartir información de depuración** en teléfonos y tabletas) en lugar de capturas de pantalla: agrupa la versión de la aplicación, el estado de la conexión y los errores más recientes. Los registros se conservan un máximo de 30 días, así que recójalos mientras el problema esté reciente. Copie también cualquier error de la consola del navegador que haya aparecido antes de que el POS pudiera escribir su propia entrada de registro.

Más allá del registro dentro de la aplicación, este fallo puede dejar rastro en:

- **Consola** (web y escritorio): la pestaña **Consola** de esas mismas herramientas de desarrollo registra los errores del lado del cliente, incluidos los que se producen antes de que el POS pueda escribir su propia entrada de registro.

## Detalles {#details}

- **Código:** `SYNC161` (`LOCAL_DB_UNAVAILABLE`)
- **Gravedad:** error
- **Introducido en:** WCPOS 1.10.0
