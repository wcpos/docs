---
title: Depuración
description: Si la depuración está habilitada, WCPOS registrará varios mensajes en la consola que pueden ayudar a diagnosticar problemas.
---

:::caution
Este artículo está dirigido a desarrolladores web; la depuración no es algo que el usuario promedio necesite utilizar.
:::

WCPOS es en gran parte una aplicación web JavaScript y, como tal, la mayor parte de la depuración se realiza a través de la [consola del navegador](http://codex.wordpress.org/Using_Your_Browser_to_Diagnose_JavaScript_Errors).
Si la depuración está habilitada, WCPOS registrará varios mensajes en la consola que pueden ayudar a diagnosticar problemas.

![Una consola JavaScript abierta en Chrome para Mac](https://wcpos.com/wp-content/uploads/2016/06/javascript-console.png "An open JavaScript Console in Chrome for the Mac")

### Activar la depuración mediante wp-config.php {#turn-on-debugging-using-wp-configphp}

Al establecer el indicador [SCRIPT_DEBUG](https://codex.wordpress.org/Debugging_in_WordPress#SCRIPT_DEBUG) en el archivo wp-config.php, es posible habilitar la depuración de WCPOS.
Esto cargará los archivos de script sin minificar y también habilitará los mensajes de depuración en la consola del navegador.

### Activar la depuración mediante la consola del navegador {#turn-on-debugging-using-the-browser-console}

Si no se tiene acceso al archivo wp-config.php, es posible habilitar los mensajes de depuración agregando una marca `debug` al Local Storage.
Esto se puede hacer ingresando `localStorage.setItem('debug', '*')` en la consola.
A continuación, se debe actualizar la página y la depuración estará activada. El registro en consola puede consumir recursos del navegador, por lo que es recomendable desactivar la depuración una vez finalizada la tarea.
Para desactivar la depuración, basta con ingresar `localStorage.removeItem('debug')` en la consola y actualizar la página.
