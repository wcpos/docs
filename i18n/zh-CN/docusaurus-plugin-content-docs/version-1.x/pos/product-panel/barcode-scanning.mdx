---
title: 条形码扫描
sidebar_label: 条形码扫描
sidebar_position: 3
description: "使用 USB 扫描仪、摄像头扫描或自定义条形码字段在 WCPOS 中设置条形码扫描。"
---

import Image from "@theme/IdealImage";
import Accordion from '@site/src/components/Accordion';
import AccordionItem from '@site/src/components/AccordionItem';

大多数条形码扫描仪的行为类似于连接到您设备的键盘。
当您扫描条形码时，WCPOS 检测到字符输入速度比正常打字快。
它使用这些“快速按键”识别输入为条形码扫描。
几乎所有扫描仪都能开箱即用——但扫描仪还支持其他[连接模式](#connection-modes)，WCPOS 可以直接连接这些模式。

## 配置条形码扫描 {#configuring-barcode-scanning}

由于条形码扫描发生得非常快，POS 可以区分条形码和手动输入的内容。
在 POS 设置中，您会找到微调条形码检测运行方式的选项。

<div class="image-container">
  <Image
    alt="POS 设置中的条形码扫描设置"
    img="/img/barcode-scanning-settings.png"
    style={{ maxHeight: 500 }}
  />
  <p class="image-caption">POS 设置中的条形码扫描设置</p>
</div>

| 设置 | 作用 | 典型取值 |
|---|---|---|
| **平均输入时间** | 输入速度必须达到多快才能被计为条形码 | 一个较短的间隔——快到手动打字不会触发它 |
| **最小长度** | 连续字符的字符串必须多长才能被视为条形码 | 与您使用的最短条形码保持一致（例如 EAN-8 为 8） |
| **前缀/后缀移除** | 剥离扫描仪添加的额外字符（前缀或后缀），只保留主要条形码 | 除非您的扫描仪被配置为添加这些字符，否则请留空 |

## 连接模式 {#connection-modes}

如需引导式的设置或故障排除流程，请使用[扫描仪设置向导](/hardware/scanner-setup-wizard)。

每台条形码扫描仪出厂时都处于以下几种模式之一，而该模式决定了 WCPOS 如何接收它的扫描结果：

| 模式 | 数据如何送达 | 设置 |
|---|---|---|
| **键盘（HID）**——几乎所有扫描仪的出厂默认模式 | 扫描仪“键入”条形码；WCPOS 识别这些快速击键 | 无需设置——配对或插入后即可扫描 |
| **串行（SPP / USB-COM）** | 扫描仪与 WCPOS 之间的直接连接 | 切换扫描仪的模式，然后在条形码设置中连接它 |
| **HID-POS** | 直接连接（USB 扫描仪） | 切换扫描仪的模式，然后在条形码设置中连接它 |

键盘模式无需任何设置，适合大多数商店。如果扫描结果有时会出现乱码（因键盘布局差异导致大小写/符号混乱），或在搜索框未获得焦点时落到错误的位置，那么直接连接就很值得——直接连接的扫描仪会把每次扫描直接送达 WCPOS，与打字速度、键盘布局或焦点无关。

:::info 处于键盘模式的扫描仪无法直接连接
您的设备会把键盘模式的扫描仪完全当作键盘对待，而任何应用都不允许接管键盘。
如果您在条形码设置中按下 **连接串行扫描仪** 或 **连接 HID 扫描仪**，而列表中没有出现您的扫描仪，那么它几乎肯定仍处于键盘模式。
:::

**要切换模式**，请在扫描仪手册中找到设置条形码（查找 *“SPP mode”*、*“serial mode”* 或 *“HID-POS mode”*）并扫描它——扫描仪会自行重新配置。对于蓝牙扫描仪，请在设备的蓝牙设置中忘记旧的配对，切换后重新配对。随时扫描手册中的 *“HID”* 或 *“keyboard”* 条形码即可切换回来。

目前，桌面应用和基于 Chrome 的浏览器支持直接连接。在 iOS 和 Android 上，受支持的方式是键盘模式和摄像头扫描。

## 检测到条形码时会发生什么？ {#what-happens-when-a-barcode-is-detected}

当 POS 检测到条形码时，它会在其本地数据库中查找匹配的产品或产品变体。
可能有三种结果：

1. **完全匹配（找到一个产品）：**
   如果条形码与 POS 本地数据库中的单一产品或产品变体完全匹配，那个产品会自动添加到您的购物车中。

2. **多个匹配（找到多个产品）：**
   如果有多个产品具有相同的条形码，POS 无法知道要添加哪个。
   在这种情况下，它将扫描的条形码置于搜索栏中，以便您查看哪些产品共享该代码。
   当您看到多个匹配时，这通常意味着产品数据需要更新——每个产品应有唯一的条形码。

3. **没有本地匹配（未找到任何产品）：**
   如果条形码与您设备上存储的任何产品都不匹配，扫描的代码将显示在搜索栏中。
   此时，POS 将尝试通过您的 WooCommerce 商店的数据库在线查找匹配项。

## 理解产品同步 {#understanding-product-synchronisation}

POS 会在后台分批下载您的产品目录，因此并非从第一分钟起所有产品都已存入设备——其工作方式请参见 [产品同步](/products/sync)。

### 为什么它对条形码扫描很重要 {#why-it-matters-for-barcode-scanning}

当您扫描尚未在本地存储的条形码时，POS 会在线访问您的 WooCommerce 商店，找到该产品并将其下载下来——因此下次扫描同一条码时会立即响应。您无需做任何事情来加快这个过程：后台的目录预填充会自行补齐您的其余库存，您可以在 **商店健康 → 数据库** 中查看进度。

## 常见问题解答 {#faq}

<Accordion>
  <AccordionItem question="为什么扫描条形码时会出现'未找到本地产品'？">

并非所有产品一开始就已存入设备——POS 会在最初使用的几分钟内于后台预填充您的产品目录。
如果您刚扫描的产品尚未存储，该次扫描会触发 POS 在线查找并下载它，因此下次会立即响应。

  </AccordionItem>

  <AccordionItem question="POS 是否会生成和打印条形码？">

不，目前不支持。我们的 POS 旨在扫描和读取现有条形码，但不包括条形码创建或打印的功能。
如果您需要为产品生成条形码，可以使用专门用于条形码创建和打印的第三方 WooCommerce 插件。一些示例包括：

- [EAN for WooCommerce](https://wordpress.org/plugins/ean-for-woocommerce/)
- [A4 Barcode Generator](https://wordpress.org/plugins/a4-barcode-generator/)

一旦您为产品生成了条形码，您可以轻松地在收银台扫描它们，从而加快 POS 的结账过程。

  </AccordionItem>
</Accordion>