---
title: "SYNC221: 记录冲突"
sidebar_label: SYNC221
description: "本设备上的更改与您商店中所做的编辑发生了冲突。"
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## 这意味着什么 {#what-this-means}

本设备上的更改与您商店中所做的编辑发生了冲突。

请打开该记录，确认哪个版本是正确的。在冲突解决之前，POS 会同时保全双方的数据——不会有任何丢失。

## 该怎么做 {#what-to-do}

请先查看下面的详细信息再重试。请在完成上述更改后再重试；不做更改就重试同样会失败。

## 您的数据 {#your-data}

订单本身是安全的，未发生变化。

## 故障排查 {#troubleshoot}

1. 打开日志条目中指明的记录，并与 WP Admin 中的同一条记录进行比较。
2. 判断哪个版本是正确的——收银机的还是商店的——然后一次性重新应用正确的值；POS 会同时保全双方的数据，在冲突解决之前，对该记录的后续编辑都会被暂缓。
3. 同一批记录频繁发生冲突，通常意味着两个人在并行编辑同一份数据；请商定各类编辑分别在哪里进行。

## 在哪里查看 {#where-to-look}

当 WCPOS 能够保存此错误时，它会被记录在触发该错误的设备上。打开 **商店健康 → 日志**（导航抽屉底部的心跳图标），找到标有此代码的条目并展开：展开后的行会显示通俗易懂的原因，以及故障发生时捕获的上下文。对于商店请求，该上下文可能包含服务器自身的错误代码（`serverCode`）、HTTP `status` 或 `endpoint`；具体显示哪些字段取决于故障发生的位置。上报问题时，请使用日志页面顶部的 **复制调试信息**（在手机和平板上为 **分享调试信息**），而不要使用截图：它会打包应用版本、连接状态和最近的错误。日志最多保留 30 天，因此请趁问题还新鲜时收集。此外，也请复制在 POS 能够写入自身日志条目之前出现的任何浏览器控制台错误。

## 详细信息 {#details}

- **代码：** `SYNC221`（`RECORD_CONFLICT`）
- **严重程度：** 警告
- **引入版本：** WCPOS 1.10.0
