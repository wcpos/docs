---
title: "SYNC161: 本地数据库不可用"
sidebar_label: SYNC161
description: "本设备上的本地数据库停止响应，因此需要它的操作已暂停。"
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## 这意味着什么 {#what-this-means}

本设备上的本地数据库停止响应，因此需要它的操作已暂停。

请重启 WCPOS 以重新连接本地数据库。本设备上未保存的更改可能没有写入；重启后请检查最近的订单。如果反复发生，请导出诊断信息并联系支持团队。

## 该怎么做 {#what-to-do}

请重启 WCPOS；如果反复发生，说明本设备上的本地数据需要修复。WCPOS 不会自行重试——请在您准备好时重试。

## 您的数据 {#your-data}

本设备上的数据可能面临风险——请不要清除本地数据。如果问题持续存在，请导出诊断信息并联系 WCPOS 支持团队。

## 故障排查 {#troubleshoot}

1. 重启 WCPOS——关闭并重新打开应用（或浏览器标签页）几乎总能重新连接本地数据库。
2. 重启后请检查最近的订单：故障发生前几秒内所做的更改可能没有写入。
3. 在网页版中，请避免使用隐私/无痕窗口和激进的存储清理扩展——两者都可能在会话进行中拆毁本地存储。
4. 如果同一设备上定期出现此问题，请导出调试信息并联系支持团队，同时说明发生时设备正在做什么（休眠、切换标签页、内存不足）。

## 在哪里查看 {#where-to-look}

当 WCPOS 能够保存此错误时，它会被记录在触发该错误的设备上。打开 **商店健康 → 日志**（导航抽屉底部的心跳图标），找到标有此代码的条目并展开：展开后的行会显示通俗易懂的原因，以及故障发生时捕获的上下文。对于商店请求，该上下文可能包含服务器自身的错误代码（`serverCode`）、HTTP `status` 或 `endpoint`；具体显示哪些字段取决于故障发生的位置。上报问题时，请使用日志页面顶部的 **复制调试信息**（在手机和平板上为 **分享调试信息**），而不要使用截图：它会打包应用版本、连接状态和最近的错误。日志最多保留 30 天，因此请趁问题还新鲜时收集。此外，也请复制在 POS 能够写入自身日志条目之前出现的任何浏览器控制台错误。

除应用内日志外，此故障还可能在以下位置留下线索：

- **控制台**（网页版和桌面版）：同一开发者工具中的 **控制台** 选项卡会记录客户端错误，包括那些在 POS 尚未能写入自身日志条目之前发生的错误。

## 详细信息 {#details}

- **代码：** `SYNC161`（`LOCAL_DB_UNAVAILABLE`）
- **严重程度：** 错误
- **引入版本：** WCPOS 1.10.0
