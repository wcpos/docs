---
title: "CHECKOUT211: 结账响应为空"
sidebar_label: CHECKOUT211
description: "商店没有返回结账结果，因此订单状态未知。"
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## 这意味着什么 {#what-this-means}

商店没有返回结账结果，因此订单状态未知。

在再次尝试结账之前，请先确认订单是否已创建。

## 该怎么做 {#what-to-do}

请先查看下面的详细信息再重试。在确认实际发生了什么之前不要重试——盲目重试可能造成重复扣款或重复订单。

## 您的数据 {#your-data}

无法确认结果——请在重试之前先核实。如果问题持续存在，请导出诊断信息并联系 WCPOS 支持团队。

## 故障排查 {#troubleshoot}

1. 在做其他任何事情之前，先在 WP Admin 中查看 WooCommerce → 订单——空响应通常意味着订单已创建，但确认信息始终没有送达。
2. 网络检查器会显示结账请求：状态为成功但正文为空或被截断，说明有插件或服务器缓冲区把响应截短了。
3. 在 WooCommerce → 状态 → 日志中查看结账过程中的致命错误——订单创建之后发生的 PHP 崩溃正会导致这种情况。
4. 只有在确认没有创建任何订单之后，才重试结账。

## 在哪里查看 {#where-to-look}

当 WCPOS 能够保存此错误时，它会被记录在触发该错误的设备上。打开 **商店健康 → 日志**（导航抽屉底部的心跳图标），找到标有此代码的条目并展开：展开后的行会显示通俗易懂的原因，以及故障发生时捕获的上下文。对于商店请求，该上下文可能包含服务器自身的错误代码（`serverCode`）、HTTP `status` 或 `endpoint`；具体显示哪些字段取决于故障发生的位置。上报问题时，请使用日志页面顶部的 **复制调试信息**（在手机和平板上为 **分享调试信息**），而不要使用截图：它会打包应用版本、连接状态和最近的错误。日志最多保留 30 天，因此请趁问题还新鲜时收集。此外，也请复制在 POS 能够写入自身日志条目之前出现的任何浏览器控制台错误。

除应用内日志外，此故障还可能在以下位置留下线索：

- **网络检查器**（网页版和桌面版）：打开开发者工具——在浏览器中按 F12，或在桌面应用的菜单中选择 **高级 → 切换开发者工具**——然后选择 **网络** 选项卡。请先遵循本页的重试指引；只有当这些步骤说明可以安全操作时，才重现该操作。失败的请求会显示 HTTP 状态码和原始响应正文，其中包括那些从未传到 POS 日志的错误页面。
- **WooCommerce → 状态 → 日志**：在 WordPress 网站上，查看最新的 `fatal-errors-*.log` 是否有 PHP 崩溃记录，以及以故障相关插件命名的任何日志来源。商店返回的 500 类错误几乎总会在这里留下原因。
- **网站的 PHP 错误日志**：如果 WooCommerce 的日志页面在故障时段没有任何记录，请向主机服务商索取 PHP 错误日志——有些致命错误只在服务器层面被捕获。

## 详细信息 {#details}

- **代码：** `CHECKOUT211`（`CHECKOUT_EMPTY_RESPONSE`）
- **严重程度：** 错误
- **引入版本：** WCPOS 1.10.0
