---
title: "PRODUCT111: 变体添加失败"
sidebar_label: PRODUCT111
description: "无法将此变体添加到该产品。"
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## 这意味着什么 {#what-this-means}

无法将此变体添加到该产品。

请检查并修正变体字段，然后重试；如果仍被拒绝，请导出诊断信息。

## 该怎么做 {#what-to-do}

请修正高亮显示的细节，然后再试一次。请在完成上述更改后再重试；不做更改就重试同样会失败。

## 您的数据 {#your-data}

该更改已保存在本设备上，但尚未传到您的商店。如果问题持续存在，请导出诊断信息并联系 WCPOS 支持团队。

## 故障排查 {#troubleshoot}

1. 展开日志条目查看商店给出的原因（serverCode）——通常是缺少某个属性，或选项组合无效。
2. 在 WP Admin 中检查父产品的属性：变体使用的每个属性都必须存在于父产品上，并被标记为用于变体。
3. 修正该变体的字段后重试。
4. 仍然被拒绝？请导出调试信息并联系支持团队。

## 在哪里查看 {#where-to-look}

当 WCPOS 能够保存此错误时，它会被记录在触发该错误的设备上。打开 **商店健康 → 日志**（导航抽屉底部的心跳图标），找到标有此代码的条目并展开：展开后的行会显示通俗易懂的原因，以及故障发生时捕获的上下文。对于商店请求，该上下文可能包含服务器自身的错误代码（`serverCode`）、HTTP `status` 或 `endpoint`；具体显示哪些字段取决于故障发生的位置。上报问题时，请使用日志页面顶部的 **复制调试信息**（在手机和平板上为 **分享调试信息**），而不要使用截图：它会打包应用版本、连接状态和最近的错误。日志最多保留 30 天，因此请趁问题还新鲜时收集。此外，也请复制在 POS 能够写入自身日志条目之前出现的任何浏览器控制台错误。

除应用内日志外，此故障还可能在以下位置留下线索：

- **网络检查器**（网页版和桌面版）：打开开发者工具——在浏览器中按 F12，或在桌面应用的菜单中选择 **高级 → 切换开发者工具**——然后选择 **网络** 选项卡。请先遵循本页的重试指引；只有当这些步骤说明可以安全操作时，才重现该操作。失败的请求会显示 HTTP 状态码和原始响应正文，其中包括那些从未传到 POS 日志的错误页面。

## 详细信息 {#details}

- **代码：** `PRODUCT111`（`VARIATION_ADD_FAILED`）
- **严重程度：** 错误
- **引入版本：** WCPOS 1.10.0
