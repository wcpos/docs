---
title: "AUTH121: 登录了错误的用户"
sidebar_label: AUTH121
description: "已登录的商店账户与本收银机上的收银员不一致。"
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## 这意味着什么 {#what-this-means}

已登录的商店账户与本收银机上的收银员不一致。

每位收银员都保有各自的收银数据，因此在他人账户下操作可能导致订单归属错误。请退出登录，然后用收银员自己的账户登录。

## 该怎么做 {#what-to-do}

需要先更改设置，此功能才能正常工作。WCPOS 不会自行重试——请在您准备好时重试。

## 您的数据 {#your-data}

订单和产品数据不受影响。

## 故障排查 {#troubleshoot}

1. POS 会自行退出该会话，以保护每位收银员的数据——不同账户之间不会混淆订单数据。
2. 用收银员自己的账户重新登录；在他人账户下操作可能导致订单归属错误。
3. 如果多位收银员共用收银机，请把切换账户作为交接流程的一部分。

## 在哪里查看 {#where-to-look}

当 WCPOS 能够保存此错误时，它会被记录在触发该错误的设备上。打开 **商店健康 → 日志**（导航抽屉底部的心跳图标），找到标有此代码的条目并展开：展开后的行会显示通俗易懂的原因，以及故障发生时捕获的上下文。对于商店请求，该上下文可能包含服务器自身的错误代码（`serverCode`）、HTTP `status` 或 `endpoint`；具体显示哪些字段取决于故障发生的位置。上报问题时，请使用日志页面顶部的 **复制调试信息**（在手机和平板上为 **分享调试信息**），而不要使用截图：它会打包应用版本、连接状态和最近的错误。日志最多保留 30 天，因此请趁问题还新鲜时收集。此外，也请复制在 POS 能够写入自身日志条目之前出现的任何浏览器控制台错误。

## 详细信息 {#details}

- **代码：** `AUTH121`（`SIGNED_IN_AS_WRONG_USER`）
- **严重程度：** 错误
- **引入版本：** WCPOS 1.10.0
