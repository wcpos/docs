---
title: 日志
sidebar_label: 日志
sidebar_position: 2
description: "访问和理解 WCPOS 日志以进行故障排除，包括商店健康活动日志、服务器端日志和错误代码。"
---

import Image from "@theme/IdealImage";
import LinkCards from '@site/src/components/LinkCards';
import LinkCard from '@site/src/components/LinkCard';

POS 有两个日志界面：**应用内活动日志**（POS 应用程序内部）和 **WP Admin 日志页面**（WordPress 内部）。它们一起为您提供 POS 活动的完整图景，用于调试和审计。

## POS 应用程序日志 {#pos-application-logs}

:::info v1.10.0 中的变更
从 v1.10.0 起，应用内日志位于 **商店健康 → 日志**（导航抽屉中的心跳图标——它所取代的独立日志屏幕已被移除）。本节介绍 v1.10.0 的日志；该区域的完整导览请参见[商店健康](/support/store-health#logs)。
:::

### 访问日志 {#accessing-logs}

从左侧导航抽屉打开 **商店健康**（心跳图标），然后选择 **日志** 选项卡。

### 界面概述 {#interface-overview}

该日志是这台设备上活动的流水账，保留 30 天。每一行显示：

| 列 | 描述 |
|--------|-------------|
| **时间** | 事件发生的时间 |
| **级别** | 严重性——error、warning、action、sync 或 info |
| **事件** | 人类可读的描述（在显示时翻译） |
| **耗时** | 该操作花费了多长时间 |
| **状态** | 操作结果 |

在流水账上方，有一行状态显示同步心跳——上次变更检查的运行时间以及下次检查的预定时间——还有一个统计标题栏，统计当天的事件数和错误数、卡住的记录数以及待发送的更改数。其中大多数都可以点击，作为筛选条件使用。

#### 筛选 {#filtering}

预设标签用于筛选流水账：**全部**、**操作**（人为执行的操作——销售、编辑、设置更改）、**错误** 和 **同步**。另有一个 **详细诊断** 开关，可在 24 小时内捕获额外细节，之后会自动关闭。

### 行详情与错误代码 {#row-details}

点击某个错误或警告行以展开它。详情视图会提供：

- **发生了什么**，以通俗语言说明
- **您的数据** — 明确的数据安全说明（例如，该更改在服务器上是否安全，或者只存在于这台设备上）
- **该怎么做** — 建议的下一步操作
- **一个帮助链接** — 错误会带有一个代码（例如 `SYNC121`、`AUTH311`），链接到[错误代码](/error-codes)文档
- **一个可复制的事件代码** — 行标题是在显示时翻译的，因此事件代码才是稳定的标识符。联系支持时请引用它。

## WP Admin 日志页面 {#wp-admin-logs-page}

POS 插件还在 WordPress 管理区域提供了一个服务器端日志页面。此页面显示在服务器上记录的 POS 特定警告和错误，这有助于诊断可能不会出现在 POS 应用程序本身中的问题。

### 访问 WP Admin 日志页面 {#accessing-the-wp-admin-logs-page}

导航到 **WP Admin > POS > Settings**，然后选择 **Tools** 选项卡并点击 **Logs**。

### 未读徽章 {#unread-badge}

Logs 菜单项显示一个指示未读日志条目的徽章：

- **红色徽章** - 有未读的 error 级别条目
- **琥珀色徽章** - 有未读的 warning 级别条目（但没有错误）

访问 Logs 页面会重置未读计数。计数按用户跟踪，因此每位管理员都看到自己的未读状态。

### 过滤条目 {#filtering-entries}

使用级别过滤器缩小日志条目范围：

- **All** - 显示所有日志条目
- **Errors** - 仅显示 error 级别条目
- **Warnings** - 仅显示 warning 级别条目

### 可展开条目 {#expandable-entries}

较长的日志消息会在大约 100 个字符处被截断。点击某个条目以将其展开，查看完整消息以及任何附加上下文数据。

### 分页 {#pagination}

日志条目在服务器端分页。使用分页控件浏览较早的条目。

### 致命错误横幅 {#fatal-errors-banner}

如果服务器上有 `fatal-errors-*.log` 文件，Logs 页面会在顶部显示一个警告横幅。此横幅链接到 **WooCommerce → 状态 → 日志**，您可以在那里查看完整的致命错误日志。致命错误通常是阻止 POS API 响应的 PHP 级崩溃。

## 使用日志进行支持 {#using-logs-for-support}

请求支持时：

1. **重现问题** - 执行导致问题的操作
2. **检查日志** - 在 **商店健康 → 日志** 中查找错误或警告
3. **展开该行** - 阅读数据安全说明和建议的下一步操作
4. **复制事件代码** - 在支持请求中包含它；它是对所发生情况的稳定标识符
5. 如果支持人员需要更多细节，请开启 **详细诊断** — 它会在 24 小时内捕获额外信息

## 性能监控 {#performance-monitoring}

汇总的性能数据位于 **商店健康 → 性能**：过去 24 小时内的请求数、传输的数据量（当响应未声明大小时，该值是一个下限）和典型响应时间，以及按小时显示的正常运行状态条。如果响应时间偏高，请参阅[服务器性能](/support/performance/server)。

## 相关文档 {#related-documentation}

<LinkCards>
  <LinkCard to="/error-codes" title="错误代码" description="详细的错误解释" />
  <LinkCard to="/category/troubleshooting" title="故障排除" description="常见问题和解决方案" />
  <LinkCard to="/support" title="支持" description="获得帮助" />
</LinkCards>
