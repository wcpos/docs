---
title: "PRODUCT421: Metadados de preço variável inválidos"
sidebar_label: PRODUCT421
description: "Não foi possível ler os dados de preço das variações deste produto, portanto os preços exibidos podem estar errados ou ausentes."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## O que isso significa {#what-this-means}

Não foi possível ler os dados de preço das variações deste produto, portanto os preços exibidos podem estar errados ou ausentes.

Salve o produto novamente no WooCommerce para reconstruir os dados de preço dele ou entre em contato com o suporte se isso aparecer em muitos produtos.

## O que fazer {#what-to-do}

Verifique os detalhes abaixo antes de tentar novamente. Tente de novo depois de fazer a alteração acima; repetir sem ela falhará da mesma forma.

## Seus dados {#your-data}

Nenhum dado de pedido ou produto é afetado. Se isso persistir, procure o administrador da sua loja.

## Solução de problemas {#troubleshoot}

1. Abra o produto no WP Admin e salve-o novamente — isso reconstrói os dados de preço que o POS não conseguiu ler.
2. Verifique se cada variação do produto tem um preço definido; uma variação sem preço gera faixas de preço ilegíveis.
3. Se muitos produtos apresentarem este código de uma vez, provavelmente um plugin de preços está gravando os dados de preço em um formato que o POS não consegue ler — anote qual plugin gerencia os preços e entre em contato com o suporte.

## Onde verificar {#where-to-look}

Quando o WCPOS consegue salvar este erro, ele é registrado no dispositivo que o gerou. Abra **Saúde da loja → Logs** (o ícone de batimento cardíaco na parte inferior do menu de navegação), localize a entrada marcada com este código e expanda-a: a linha expandida mostra o motivo em linguagem simples e o contexto capturado no momento da falha. Em uma requisição à loja, esse contexto pode incluir o código de erro do próprio servidor (`serverCode`), o `status` HTTP ou o `endpoint`; os campos exibidos dependem de onde a falha ocorreu. Ao relatar um problema, use **Copiar informações de depuração** no topo da tela de Logs (**Compartilhar informações de depuração** em celulares e tablets) em vez de capturas de tela: isso reúne a versão do aplicativo, o estado da conexão e os erros mais recentes. Os logs são mantidos por no máximo 30 dias, portanto colete-os enquanto o problema estiver recente. Copie também qualquer erro do console do navegador que tenha aparecido antes de o POS conseguir gravar sua própria entrada de log.

## Detalhes {#details}

- **Código:** `PRODUCT421` (`VARIABLE_PRICE_META_INVALID`)
- **Severidade:** warn
- **Introduzido em:** WCPOS 1.10.0
