---
title: "PAYMENT301: Gateway indisponível"
sidebar_label: PAYMENT301
description: "O gateway de pagamento selecionado está indisponível para esta loja."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## O que isso significa {#what-this-means}

O gateway de pagamento selecionado está indisponível para esta loja.

Peça ao administrador do site que verifique se o gateway é compatível, está ativado e está configurado corretamente.

## O que fazer {#what-to-do}

É necessário alterar uma configuração antes que isso funcione. Tente de novo depois de fazer a alteração acima; repetir sem ela falhará da mesma forma.

## Seus dados {#your-data}

Nenhum dado de pedido ou produto é afetado. Se isso persistir, procure a pessoa responsável pelo seu site WordPress.

## Solução de problemas {#troubleshoot}

1. Peça ao administrador do site que verifique o gateway em WooCommerce → Configurações → Pagamentos: ele precisa estar instalado, ativado e habilitado para uso no POS nas configurações do POS.
2. Enquanto isso, conclua a venda com outro método, como dinheiro.
3. Se o gateway funcionava ontem, verifique se ele ou o WooCommerce foi atualizado — WooCommerce → Status → Logs registra erros de inicialização de gateway.

## Onde verificar {#where-to-look}

Quando o WCPOS consegue salvar este erro, ele é registrado no dispositivo que o gerou. Abra **Saúde da loja → Logs** (o ícone de batimento cardíaco na parte inferior do menu de navegação), localize a entrada marcada com este código e expanda-a: a linha expandida mostra o motivo em linguagem simples e o contexto capturado no momento da falha. Em uma requisição à loja, esse contexto pode incluir o código de erro do próprio servidor (`serverCode`), o `status` HTTP ou o `endpoint`; os campos exibidos dependem de onde a falha ocorreu. Ao relatar um problema, use **Copiar informações de depuração** no topo da tela de Logs (**Compartilhar informações de depuração** em celulares e tablets) em vez de capturas de tela: isso reúne a versão do aplicativo, o estado da conexão e os erros mais recentes. Os logs são mantidos por no máximo 30 dias, portanto colete-os enquanto o problema estiver recente. Copie também qualquer erro do console do navegador que tenha aparecido antes de o POS conseguir gravar sua própria entrada de log.

Além do log no aplicativo, esta falha pode deixar evidências em:

- **Logs do POS no servidor**: no WP Admin, abra **POS → Configurações → Ferramentas → Logs**. Essa página registra avisos e erros relacionados ao POS gerados no próprio servidor, que podem nunca aparecer dentro do aplicativo. Um selo vermelho no menu indica erros do servidor não lidos.
- **WooCommerce → Status → Logs**: no site WordPress, verifique o `fatal-errors-*.log` mais recente em busca de falhas do PHP, além de qualquer fonte de log com o nome de um plugin envolvido na falha. Um erro da classe 500 vindo da loja quase sempre deixa sua causa aqui.

## Detalhes {#details}

- **Código:** `PAYMENT301` (`GATEWAY_UNAVAILABLE`)
- **Severidade:** error
- **Introduzido em:** WCPOS 1.10.0
