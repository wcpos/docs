---
title: "SYNC411: Excesso de requisições sob demanda"
sidebar_label: SYNC411
description: "Este dispositivo pediu dados à loja com uma frequência muito maior que o normal."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## O que isso significa {#what-this-means}

Este dispositivo pediu dados à loja com uma frequência muito maior que o normal.

Nada foi retardado nem bloqueado, e nenhum dado de pedido é afetado — este é um alarme apenas de detecção. Excessos contínuos de requisições costumam indicar um problema do aplicativo (uma tela solicitando os mesmos dados em looping), e não algo que você tenha feito. Se este código continuar aparecendo, exporte os diagnósticos e entre em contato com o suporte para que o looping possa ser encontrado e corrigido.

## O que fazer {#what-to-do}

Você pode continuar trabalhando — o WCPOS resolve isso automaticamente. O WCPOS repete isso automaticamente.

## Seus dados {#your-data}

Nenhum dado de pedido ou produto é afetado. Se isso persistir, exporte os diagnósticos e entre em contato com o suporte do WCPOS.

## Solução de problemas {#troubleshoot}

1. Continue trabalhando — este é um alarme apenas de detecção. O WCPOS não retarda, não enfileira nem descarta nenhuma requisição, e nenhum dado de pedido ou produto é afetado.
2. Se o código aparecer uma vez e parar, nenhuma ação é necessária. Se ele se repetir, anote qual tela estava aberta e qual ação você estava realizando quando ele apareceu.
3. Use Copiar informações de depuração enquanto o evento ainda estiver na janela de log de 30 dias e entre em contato com o suporte do WCPOS; a exportação permite que o suporte rastreie o looping de requisições.

## Onde verificar {#where-to-look}

Quando o WCPOS consegue salvar este erro, ele é registrado no dispositivo que o gerou. Abra **Saúde da loja → Logs** (o ícone de batimento cardíaco na parte inferior do menu de navegação), localize a entrada marcada com este código e expanda-a: a linha expandida mostra o motivo em linguagem simples e o contexto capturado no momento da falha. Em uma requisição à loja, esse contexto pode incluir o código de erro do próprio servidor (`serverCode`), o `status` HTTP ou o `endpoint`; os campos exibidos dependem de onde a falha ocorreu. Ao relatar um problema, use **Copiar informações de depuração** no topo da tela de Logs (**Compartilhar informações de depuração** em celulares e tablets) em vez de capturas de tela: isso reúne a versão do aplicativo, o estado da conexão e os erros mais recentes. Os logs são mantidos por no máximo 30 dias, portanto colete-os enquanto o problema estiver recente. Copie também qualquer erro do console do navegador que tenha aparecido antes de o POS conseguir gravar sua própria entrada de log.

## Detalhes {#details}

- **Código:** `SYNC411` (`DEMAND_REQUEST_FLOOD`)
- **Severidade:** warn
- **Introduzido em:** WCPOS 1.10.0
