---
title: "CHECKOUT401: Totais divergentes"
sidebar_label: CHECKOUT401
description: "Sua loja calculou para este pedido totais diferentes dos exibidos no caixa."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## O que isso significa {#what-this-means}

Sua loja calculou para este pedido totais diferentes dos exibidos no caixa.

Verifique o pedido no painel da sua loja antes de receber qualquer outro pagamento — os totais da loja são a fonte da verdade. Se a diferença for inesperada, exporte os diagnósticos e entre em contato com o suporte.

## O que fazer {#what-to-do}

Verifique os detalhes abaixo antes de tentar novamente. O WCPOS não repete isso sozinho — tente de novo quando estiver pronto.

## Seus dados {#your-data}

Pode ter havido movimentação de dinheiro — verifique o pagamento antes de agir. Se isso persistir, exporte os diagnósticos e entre em contato com o suporte do WCPOS.

## Solução de problemas {#troubleshoot}

1. Pare antes de receber qualquer outro pagamento — os totais da loja são a fonte da verdade.
2. Abra o pedido no WP Admin e compare os totais dele com o que o caixa exibiu; a entrada de log registra quais valores divergiram.
3. As causas mais comuns são configurações de impostos diferentes das que o caixa esperava ou um plugin de desconto ou de preços recalculando no servidor. Se a diferença for um valor de imposto, revise primeiro as configurações de impostos da loja.
4. Se o valor cobrado foi o do caixa e a loja indica outro, acerte a diferença (reembolso ou cobrança adicional) pelo seu processo habitual antes de o cliente ir embora.
5. Se a divergência não tiver explicação, exporte as informações de depuração e entre em contato com o suporte informando o número do pedido.

## Onde verificar {#where-to-look}

Quando o WCPOS consegue salvar este erro, ele é registrado no dispositivo que o gerou. Abra **Saúde da loja → Logs** (o ícone de batimento cardíaco na parte inferior do menu de navegação), localize a entrada marcada com este código e expanda-a: a linha expandida mostra o motivo em linguagem simples e o contexto capturado no momento da falha. Em uma requisição à loja, esse contexto pode incluir o código de erro do próprio servidor (`serverCode`), o `status` HTTP ou o `endpoint`; os campos exibidos dependem de onde a falha ocorreu. Ao relatar um problema, use **Copiar informações de depuração** no topo da tela de Logs (**Compartilhar informações de depuração** em celulares e tablets) em vez de capturas de tela: isso reúne a versão do aplicativo, o estado da conexão e os erros mais recentes. Os logs são mantidos por no máximo 30 dias, portanto colete-os enquanto o problema estiver recente. Copie também qualquer erro do console do navegador que tenha aparecido antes de o POS conseguir gravar sua própria entrada de log.

Além do log no aplicativo, esta falha pode deixar evidências em:

- **Inspetor de rede** (web e desktop): abra as ferramentas de desenvolvedor — pressione F12 no navegador ou use **Avançado → Alternar ferramentas de desenvolvedor** no menu do aplicativo desktop — e selecione a aba **Rede**. Siga primeiro as orientações de nova tentativa desta página; só reproduza a ação quando esses passos indicarem que é seguro. Uma requisição com falha mostra o status HTTP e o corpo bruto da resposta, incluindo páginas de erro que nunca chegam ao log do POS.
- **Logs do POS no servidor**: no WP Admin, abra **POS → Configurações → Ferramentas → Logs**. Essa página registra avisos e erros relacionados ao POS gerados no próprio servidor, que podem nunca aparecer dentro do aplicativo. Um selo vermelho no menu indica erros do servidor não lidos.

## Detalhes {#details}

- **Código:** `CHECKOUT401` (`TOTALS_DIVERGED`)
- **Severidade:** error
- **Introduzido em:** WCPOS 1.10.0
