---
title: "SYNC101: Falha na gravação no banco de dados local"
sidebar_label: SYNC101
description: "Não foi possível salvar esta alteração no banco de dados local, e ela permanece apenas neste dispositivo."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## O que isso significa {#what-this-means}

Não foi possível salvar esta alteração no banco de dados local, e ela permanece apenas neste dispositivo.

Não limpe nem recarregue os dados locais. Exporte os diagnósticos e entre em contato com o suporte antes de tentar novamente ou de reparar qualquer coisa.

## O que fazer {#what-to-do}

Exporte os diagnósticos e entre em contato com o suporte. O WCPOS não repete isso sozinho — tente de novo quando estiver pronto.

## Seus dados {#your-data}

Os dados neste dispositivo podem estar em risco — não limpe os dados locais. Se isso persistir, exporte os diagnósticos e entre em contato com o suporte do WCPOS.

## Solução de problemas {#troubleshoot}

1. Se a entrada de log indicar um registro, anote-o — essa é a alteração que não foi salva. Gravações de site e de credenciais podem indicar apenas a operação que falhou e o erro; registre esses detalhes antes de continuar.
2. Não limpe os dados do site, não redefina o aplicativo e não o desinstale: a alteração não salva existe apenas neste dispositivo, e limpar o armazenamento local a apaga definitivamente.
3. Verifique o espaço livre do dispositivo — um disco cheio ou uma cota de armazenamento do navegador esgotada é o motivo mais comum de falha em uma gravação local.
4. Reinicie o WCPOS uma vez e faça uma edição pequena e inofensiva para confirmar se novas alterações são salvas.
5. Se as gravações continuarem falhando, use Copiar informações de depuração na tela de Logs e entre em contato com o suporte antes de tentar qualquer reparo.

## Onde verificar {#where-to-look}

Quando o WCPOS consegue salvar este erro, ele é registrado no dispositivo que o gerou. Abra **Saúde da loja → Logs** (o ícone de batimento cardíaco na parte inferior do menu de navegação), localize a entrada marcada com este código e expanda-a: a linha expandida mostra o motivo em linguagem simples e o contexto capturado no momento da falha. Em uma requisição à loja, esse contexto pode incluir o código de erro do próprio servidor (`serverCode`), o `status` HTTP ou o `endpoint`; os campos exibidos dependem de onde a falha ocorreu. Ao relatar um problema, use **Copiar informações de depuração** no topo da tela de Logs (**Compartilhar informações de depuração** em celulares e tablets) em vez de capturas de tela: isso reúne a versão do aplicativo, o estado da conexão e os erros mais recentes. Os logs são mantidos por no máximo 30 dias, portanto colete-os enquanto o problema estiver recente. Copie também qualquer erro do console do navegador que tenha aparecido antes de o POS conseguir gravar sua própria entrada de log.

Além do log no aplicativo, esta falha pode deixar evidências em:

- **Console** (web e desktop): a aba **Console** das mesmas ferramentas de desenvolvedor registra erros do lado do cliente, inclusive os que ocorrem antes de o POS conseguir gravar sua própria entrada de log.

## Detalhes {#details}

- **Código:** `SYNC101` (`LOCAL_DB_WRITE_FAILED`)
- **Severidade:** error
- **Introduzido em:** WCPOS 1.10.0
