---
title: "PRODUCT421: Preis-Metadaten der Variation ungültig"
sidebar_label: PRODUCT421
description: "Die Variationspreisdaten dieses Produkts konnten nicht gelesen werden, daher sind angezeigte Preise möglicherweise falsch oder fehlen."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Was dies bedeutet {#what-this-means}

Die Variationspreisdaten dieses Produkts konnten nicht gelesen werden, daher sind angezeigte Preise möglicherweise falsch oder fehlen.

Speichern Sie das Produkt in WooCommerce erneut, um seine Preisdaten neu aufzubauen, oder wenden Sie sich an den Support, wenn dies bei vielen Produkten auftritt.

## Was zu tun ist {#what-to-do}

Prüfen Sie die Details unten, bevor Sie es erneut versuchen. Versuchen Sie es erst nach der oben genannten Änderung erneut; ohne sie schlägt der Versuch genauso fehl.

## Ihre Daten {#your-data}

Es sind keine Bestell- oder Produktdaten betroffen. Wenn dies weiterhin auftritt, wenden Sie sich an Ihren Shop-Administrator.

## Fehlerbehebung {#troubleshoot}

1. Öffnen Sie das Produkt in WP Admin und speichern Sie es erneut — dadurch werden die Preisdaten neu aufgebaut, die das POS nicht lesen konnte.
2. Prüfen Sie, ob für jede Variation des Produkts ein Preis gesetzt ist; eine Variation ohne Preis erzeugt unlesbare Preisspannen.
3. Wenn viele Produkte diesen Code gleichzeitig zeigen, schreibt wahrscheinlich ein Preis-Plugin die Preisdaten in einem Format, das das POS nicht lesen kann — halten Sie fest, welches Plugin die Preise verwaltet, und wenden Sie sich an den Support.

## Wo Sie nachsehen können {#where-to-look}

Wenn WCPOS diesen Fehler speichern kann, wird er auf dem Gerät aufgezeichnet, das ihn ausgelöst hat. Öffnen Sie **Shop-Zustand → Protokolle** (das Herzschlagsymbol unten im Navigationsmenü), suchen Sie den mit diesem Code gekennzeichneten Eintrag und klappen Sie ihn auf: Die aufgeklappte Zeile zeigt die Ursache in verständlicher Sprache sowie den Kontext, der im Moment des Fehlers erfasst wurde. Bei einer Shop-Anfrage kann dieser Kontext den eigenen Fehlercode des Servers (`serverCode`), den HTTP-`status` oder den `endpoint` enthalten; welche Felder angezeigt werden, hängt davon ab, wo der Fehler aufgetreten ist. Verwenden Sie beim Melden eines Problems **Debug-Infos kopieren** oben im Protokollbildschirm (**Debug-Infos teilen** auf Smartphones und Tablets) statt Screenshots: Damit werden App-Version, Verbindungsstatus und die neuesten Fehler zusammengefasst. Protokolle werden höchstens 30 Tage aufbewahrt — sammeln Sie sie daher, solange das Problem frisch ist. Kopieren Sie außerdem jede Fehlermeldung aus der Browser-Konsole, die erschienen ist, bevor das POS seinen eigenen Protokolleintrag schreiben konnte.

## Details {#details}

- **Code:** `PRODUCT421` (`VARIABLE_PRICE_META_INVALID`)
- **Schweregrad:** warn
- **Eingeführt in:** WCPOS 1.10.0
