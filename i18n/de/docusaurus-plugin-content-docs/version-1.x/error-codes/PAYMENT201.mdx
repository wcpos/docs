---
title: "PAYMENT201: Zahlungsergebnis unbekannt"
sidebar_label: PAYMENT201
description: "WCPOS konnte nicht bestätigen, ob das Terminal die Zahlung belastet hat."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Was dies bedeutet {#what-this-means}

WCPOS konnte nicht bestätigen, ob das Terminal die Zahlung belastet hat.

Prüfen Sie das Zahlungsterminal, bevor Sie eine weitere Belastung versuchen, und wenden Sie sich an den Zahlungsanbieter, wenn das Ergebnis unklar bleibt.

## Was zu tun ist {#what-to-do}

Prüfen Sie die Details unten, bevor Sie es erneut versuchen. Versuchen Sie es erst erneut, wenn Sie geklärt haben, was tatsächlich passiert ist — ein blinder Wiederholungsversuch kann zu einer doppelten Abbuchung oder Bestellung führen.

## Ihre Daten {#your-data}

Das Ergebnis konnte nicht bestätigt werden — prüfen Sie es, bevor Sie es erneut versuchen. Wenn dies weiterhin auftritt, wenden Sie sich an Ihren Zahlungsanbieter.

## Fehlerbehebung {#troubleshoot}

1. Ziehen Sie die Karte noch nicht erneut ein.
2. Prüfen Sie den Bildschirm des Terminals und danach das Dashboard Ihres Zahlungsanbieters — die Aufzeichnung des Anbieters ist maßgeblich dafür, ob die Belastung durchgeführt wurde.
3. Prüfen Sie die Bestellung in WP Admin: Eine erfolgreiche Zahlung ist dort möglicherweise bereits erfasst.
4. Nehmen Sie die Zahlung erst dann erneut entgegen, wenn das Dashboard des Anbieters keine Belastung zeigt.
5. Wenn die Belastung als ausstehend oder unsicher angezeigt wird, wenden Sie sich an den Zahlungsanbieter, bevor Sie handeln.

## Wo Sie nachsehen können {#where-to-look}

Wenn WCPOS diesen Fehler speichern kann, wird er auf dem Gerät aufgezeichnet, das ihn ausgelöst hat. Öffnen Sie **Shop-Zustand → Protokolle** (das Herzschlagsymbol unten im Navigationsmenü), suchen Sie den mit diesem Code gekennzeichneten Eintrag und klappen Sie ihn auf: Die aufgeklappte Zeile zeigt die Ursache in verständlicher Sprache sowie den Kontext, der im Moment des Fehlers erfasst wurde. Bei einer Shop-Anfrage kann dieser Kontext den eigenen Fehlercode des Servers (`serverCode`), den HTTP-`status` oder den `endpoint` enthalten; welche Felder angezeigt werden, hängt davon ab, wo der Fehler aufgetreten ist. Verwenden Sie beim Melden eines Problems **Debug-Infos kopieren** oben im Protokollbildschirm (**Debug-Infos teilen** auf Smartphones und Tablets) statt Screenshots: Damit werden App-Version, Verbindungsstatus und die neuesten Fehler zusammengefasst. Protokolle werden höchstens 30 Tage aufbewahrt — sammeln Sie sie daher, solange das Problem frisch ist. Kopieren Sie außerdem jede Fehlermeldung aus der Browser-Konsole, die erschienen ist, bevor das POS seinen eigenen Protokolleintrag schreiben konnte.

Über das Protokoll in der App hinaus kann dieser Fehler an folgenden Stellen Spuren hinterlassen:

- **Dashboard des Zahlungsanbieters**: Die eigene Aufzeichnung des Terminal-Anbieters (zum Beispiel Stripe Dashboard → Payments oder das Square Dashboard) ist maßgeblich dafür, ob eine Abbuchung erfolgt ist — prüfen Sie sie, bevor Sie eine Zahlung erneut versuchen.
- **Netzwerk-Inspektor** (Web und Desktop): Öffnen Sie die Entwicklertools — drücken Sie F12 im Browser oder wählen Sie **Erweitert → Entwicklertools umschalten** im Menü der Desktop-App — und wechseln Sie zum Tab **Netzwerk**. Befolgen Sie zuerst die Hinweise zum erneuten Versuch auf dieser Seite; wiederholen Sie die Aktion nur, wenn diese Schritte sie als unbedenklich bezeichnen. Eine fehlgeschlagene Anfrage zeigt den HTTP-Status und den unbearbeiteten Antworttext, einschließlich Fehlerseiten, die nie im POS-Protokoll ankommen.

## Details {#details}

- **Code:** `PAYMENT201` (`PAYMENT_OUTCOME_UNKNOWN`)
- **Schweregrad:** error
- **Eingeführt in:** WCPOS 1.10.0
