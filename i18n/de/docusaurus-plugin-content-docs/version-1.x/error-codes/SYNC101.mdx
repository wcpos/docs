---
title: "SYNC101: Schreiben in lokale Datenbank fehlgeschlagen"
sidebar_label: SYNC101
description: "Diese Änderung konnte nicht in der lokalen Datenbank gespeichert werden und besteht nur auf diesem Gerät."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Was dies bedeutet {#what-this-means}

Diese Änderung konnte nicht in der lokalen Datenbank gespeichert werden und besteht nur auf diesem Gerät.

Löschen und laden Sie keine lokalen Daten neu. Exportieren Sie Diagnosedaten und wenden Sie sich an den Support, bevor Sie etwas erneut versuchen oder reparieren.

## Was zu tun ist {#what-to-do}

Exportieren Sie Diagnosedaten und wenden Sie sich an den Support. WCPOS wiederholt dies nicht von selbst — versuchen Sie es erneut, wenn Sie so weit sind.

## Ihre Daten {#your-data}

Daten auf diesem Gerät könnten gefährdet sein — löschen Sie keine lokalen Daten. Wenn dies weiterhin auftritt, exportieren Sie Diagnosedaten und wenden Sie sich an den WCPOS-Support.

## Fehlerbehebung {#troubleshoot}

1. Wenn der Protokolleintrag einen Datensatz nennt, halten Sie ihn fest — das ist die Änderung, die nicht gespeichert wurde. Bei Schreibvorgängen zu Website und Zugangsdaten werden stattdessen möglicherweise nur der fehlgeschlagene Vorgang und der Fehler genannt; halten Sie diese Angaben fest, bevor Sie fortfahren.
2. Löschen Sie keine Website-Daten, setzen Sie die App nicht zurück und deinstallieren Sie sie nicht: Die nicht gespeicherte Änderung existiert nur auf diesem Gerät, und das Löschen des lokalen Speichers entfernt sie dauerhaft.
3. Prüfen Sie den freien Speicher des Geräts — eine volle Festplatte oder ein ausgeschöpftes Browser-Speicherkontingent ist der häufigste Grund dafür, dass ein lokaler Schreibvorgang fehlschlägt.
4. Starten Sie WCPOS einmal neu und nehmen Sie eine kleine, harmlose Änderung vor, um zu prüfen, ob neue Änderungen gespeichert werden.
5. Wenn Schreibvorgänge weiterhin fehlschlagen, verwenden Sie Debug-Infos kopieren im Protokollbildschirm und wenden Sie sich an den Support, bevor Sie eine Reparatur versuchen.

## Wo Sie nachsehen können {#where-to-look}

Wenn WCPOS diesen Fehler speichern kann, wird er auf dem Gerät aufgezeichnet, das ihn ausgelöst hat. Öffnen Sie **Shop-Zustand → Protokolle** (das Herzschlagsymbol unten im Navigationsmenü), suchen Sie den mit diesem Code gekennzeichneten Eintrag und klappen Sie ihn auf: Die aufgeklappte Zeile zeigt die Ursache in verständlicher Sprache sowie den Kontext, der im Moment des Fehlers erfasst wurde. Bei einer Shop-Anfrage kann dieser Kontext den eigenen Fehlercode des Servers (`serverCode`), den HTTP-`status` oder den `endpoint` enthalten; welche Felder angezeigt werden, hängt davon ab, wo der Fehler aufgetreten ist. Verwenden Sie beim Melden eines Problems **Debug-Infos kopieren** oben im Protokollbildschirm (**Debug-Infos teilen** auf Smartphones und Tablets) statt Screenshots: Damit werden App-Version, Verbindungsstatus und die neuesten Fehler zusammengefasst. Protokolle werden höchstens 30 Tage aufbewahrt — sammeln Sie sie daher, solange das Problem frisch ist. Kopieren Sie außerdem jede Fehlermeldung aus der Browser-Konsole, die erschienen ist, bevor das POS seinen eigenen Protokolleintrag schreiben konnte.

Über das Protokoll in der App hinaus kann dieser Fehler an folgenden Stellen Spuren hinterlassen:

- **Konsole** (Web und Desktop): Der Tab **Konsole** derselben Entwicklertools zeichnet clientseitige Fehler auf, auch solche, die auftreten, bevor das POS seinen eigenen Protokolleintrag schreiben kann.

## Details {#details}

- **Code:** `SYNC101` (`LOCAL_DB_WRITE_FAILED`)
- **Schweregrad:** error
- **Eingeführt in:** WCPOS 1.10.0
