---
title: Referenz zu Belegdaten
sidebar_label: Belegdaten
sidebar_position: 5
description: "Vollständige Referenz zum kanonischen Datenvertrag, der in WCPOS-Belegvorlagen verfügbar ist."
---

import Accordion from '@site/src/components/Accordion';
import AccordionItem from '@site/src/components/AccordionItem';
import { ReceiptAnatomy, PriceFlow, FieldIndex, Recipes, Recipe } from '@site/src/components/ReceiptData';

Logikfreie HTML-Vorlagen von WCPOS und thermische XML-Vorlagen werden aus derselben kanonischen Belegdaten-Payload gerendert. Verwenden Sie Mustache-Punktpfade wie `{{order.number}}`, `{{store.name}}` und `{{totals.total_display}}`. Arrays werden mit Abschnitten gerendert:

```mustache
{{#lines}}
  {{name}} x {{qty}} — {{line_total_display}}
{{/lines}}
```

Der kanonische Datenvertrag wird auf dem Server vom WCPOS-Belegdaten-Builder erstellt und vom Offline-Belegrenderer in der App gespiegelt. Belege werden sofort anhand lokaler Daten geöffnet und bei Verfügbarkeit mit der Serverantwort aktualisiert. Benutzerdefinierte Vorlagen sollten daher die unten aufgeführten Felder statt PHP-Bestellmethoden verwenden.

Belegdaten v1.1 ergänzt für jede Produktzeile erfasste Felder für regulären Preis, Verkaufspreis vor Couponabzug und Einsparungen. Außerdem stellt es Einsparungssummen auf Bestellungsebene bereit, einschließlich eines Werts **Gesamt eingespart**, der Einsparungen gegenüber dem regulären Preis mit WooCommerce-Rabatten kombiniert, ohne Bestellsummen oder Berichte zu ändern.

## Der Beleg im Überblick {#receipt-map}

Jeder Teil eines gedruckten Belegs stammt aus einem Abschnitt des Datenvertrags. Bewegen Sie den Mauszeiger über eine Seite, um die Zuordnung zu sehen, und klicken Sie, um zu den Feldern dieses Abschnitts zu springen. Alle Beispiele auf dieser Seite verwenden dieselbe Beispielbestellung.

<ReceiptAnatomy
  facsimile={[
    {
      id: 'store',
      rows: [
        { left: 'COFFEE MONSTER', center: true, bold: true, store: true },
        { left: "Röststraße 12, Portland OR", center: true, sub: true },
        { left: "USt-IdNr.: US-998877", center: true, sub: true },
      ],
    },
    {
      id: 'order',
      rows: [
        { left: "Beleg Nr. 1042", right: "13. Juli 2026" },
        { left: "Kasse: Ada", right: "Kunde: Nia", sub: true },
      ],
    },
    {
      id: 'lines',
      rows: [
        { left: "2 × Espressobohnen", right: "29.00" },
        { left: "~34.00~ · Ersparnis −5.00", sub: true },
        { left: "1 × Keramiktasse", right: "18.00" },
        { left: '1 × Croissant', right: '5.75' },
      ],
    },
    {
      id: 'totals',
      rows: [
        { left: "Zwischensumme", right: "52.75" },
        { left: 'Coupon SUMMER10', right: '−5.28' },
        { left: "Geschenkverpackung", right: "2.50" },
        { left: "Versand", right: "10.00" },
        { left: "GESAMT", right: "59.97", bold: true },
        { left: "Gesamt eingespart", right: "10.28", sub: true },
      ],
    },
    {
      id: 'payments',
      rows: [{ left: "Karte", right: "59.97" }],
    },
    {
      id: 'footer',
      rows: [{ left: "Vielen Dank für Ihren Einkauf!", center: true, sub: true }],
    },
  ]}
  zones={[
    {
      id: 'store',
      anchor: "#store",
      label: "Filialkopf",
      fields: ["store.name", "store.address_lines", "store.tax_ids", "store.logo"],
    },
    {
      id: 'order',
      anchor: "#order",
      label: "Bestellung und Personen",
      fields: ["order.number", "order.created", "cashier.name", "customer.name"],
    },
    {
      id: 'lines',
      anchor: "#lines",
      label: "Positionen",
      fields: ["lines[]", "line_savings", "savings_in_discounts"],
    },
    {
      id: 'totals',
      anchor: "#totals",
      label: "Summenblock",
      fields: ["totals.*", "discounts[]", "fees[]", "shipping[]", "totals.total_saved"],
    },
    {
      id: 'payments',
      anchor: "#payments",
      label: "Zahlungen",
      fields: ["payments[]"],
    },
    {
      id: 'footer',
      anchor: "#store",
      label: "Fußzeile",
      fields: ["store.personal_notes", "store.policies_and_conditions", "i18n.thank_you_purchase"],
    },
  ]}
/>

## Häufige Vorlagenaufgaben {#common-recipes}

Die meisten Vorlagenänderungen gehören zu einer dieser drei Aufgaben. Jede Karte zeigt zuerst das Druckergebnis und danach das genaue Markup, das Sie in jede logikfreie HTML- oder thermische XML-Vorlage kopieren können. Die dritte Karte zeigt statt der obigen Beispielbestellung eine ältere Bestellung vor v1.9.

<Recipes>
  <Recipe
    title="Regulären Preis durchstreichen"
    why="Die Abschnittsprüfung blendet alles bei Bestellungen ohne erfassten Preisverlauf aus."
    preview={[
      { left: '2 × Espressobohnen', right: '29.00' },
      { left: '~34.00~ · Ersparnis −5.00', sub: true },
    ]}
    snippet={`{{#lines}}
  {{#line_savings}}
    <s>{{line_regular_total_display}}</s>
    {{i18n.savings}}: -{{line_savings_display}}
  {{/line_savings}}
{{/lines}}`}
  />
  <Recipe
    title="Zeile „Gesamt eingespart“ hinzufügen"
    why="Zwei Prüfungen: Die Zeile verschwindet, wenn der Preisverlauf unvollständig ist oder nichts eingespart wurde."
    preview={[
      { left: 'GESAMT', right: '59.97', bold: true },
      { left: 'Gesamt eingespart', right: '10.28', sub: true },
    ]}
    snippet={`{{#totals.total_saved_complete}}
  {{#totals.total_saved}}
    {{i18n.total_saved}}:
    {{totals.total_saved_display}}
  {{/totals.total_saved}}
{{/totals.total_saved_complete}}`}
  />
  <Recipe
    title="Bestellung vor v1.9 sicher erneut drucken"
    why="Bei alten Bestellungen steckt die Ersparnis bereits im Rabatt. So wird sie nicht doppelt gedruckt."
    preview={[
      { left: '1 × Espressobohnen', right: '14.50' },
      { left: '~17.00~  Rabatt −2.50', sub: true },
    ]}
    snippet={`{{#lines}}
  {{#line_savings}}
    {{^savings_in_discounts}}
      {{i18n.savings}}: -{{line_savings_display}}
    {{/savings_in_discounts}}
  {{/line_savings}}
{{/lines}}`}
  />
</Recipes>

## Darstellungsregeln {#rendering-rules}

### Währungsfelder {#currency-fields}

Numerische Geldfelder werden als Zahlen beibehalten, und der Renderer ergänzt für die Vorlagenausgabe sprachabhängige `_display`-Felder:

| Numerisches Feld | Anzeigefeld |
|---------------|---------------|
| `totals.total` | `totals.total_display` |
| `lines[].line_total` | `lines[].line_total_display` |
| `payments[].amount` | `payments[].amount_display` |
| `tax_summary[].tax_amount` | `tax_summary[].tax_amount_display` |

Beim Drucken von Belegen sollten `_display`-Felder bevorzugt werden. Numerische Felder nur für bedingte Abschnitte oder maschinenlesbare Ausgaben verwenden.

### Felder mit steueranzeigegerechten Werten {#tax-display-aware-fields}

Mehrere Felder verfügen über Varianten inklusive und exklusive Steuern sowie über einen praktischen Anzeigewert. Dieser richtet sich nach der Einstellung der Filiale für die Steueranzeige im Warenkorb.

| Komfortfeld | Feld inklusive Steuern | Feld exklusive Steuern |
|-------------------|-----------------|-----------------|
| `lines[].regular_price` | `lines[].regular_price_incl` | `lines[].regular_price_excl` |
| `lines[].selling_price` | `lines[].selling_price_incl` | `lines[].selling_price_excl` |
| `lines[].unit_savings` | `lines[].unit_savings_incl` | `lines[].unit_savings_excl` |
| `lines[].line_regular_total` | `lines[].line_regular_total_incl` | `lines[].line_regular_total_excl` |
| `lines[].line_selling_total` | `lines[].line_selling_total_incl` | `lines[].line_selling_total_excl` |
| `lines[].line_savings` | `lines[].line_savings_incl` | `lines[].line_savings_excl` |
| `lines[].unit_price` | `lines[].unit_price_incl` | `lines[].unit_price_excl` |
| `lines[].unit_subtotal` | `lines[].unit_subtotal_incl` | `lines[].unit_subtotal_excl` |
| `lines[].line_subtotal` | `lines[].line_subtotal_incl` | `lines[].line_subtotal_excl` |
| `lines[].discounts` | `lines[].discounts_incl` | `lines[].discounts_excl` |
| `lines[].line_total` | `lines[].line_total_incl` | `lines[].line_total_excl` |
| `fees[].total` | `fees[].total_incl` | `fees[].total_excl` |
| `shipping[].total` | `shipping[].total_incl` | `shipping[].total_excl` |
| `discounts[].total` | `discounts[].total_incl` | `discounts[].total_excl` |
| `totals.subtotal` | `totals.subtotal_incl` | `totals.subtotal_excl` |
| `totals.discount_total` | `totals.discount_total_incl` | `totals.discount_total_excl` |
| `totals.sale_savings_total` | `totals.sale_savings_total_incl` | `totals.sale_savings_total_excl` |
| `totals.total_saved` | `totals.total_saved_incl` | `totals.total_saved_excl` |
| `totals.total` | `totals.total_incl` | `totals.total_excl` |

### Datumsobjekte {#date-objects}

Datumsfelder sind Objekte mit mehreren vorformatierten Varianten. Dadurch entfällt die Datumsformatierung innerhalb von Mustache.

| Feld | Beschreibung |
|-------|-------------|
| `datetime`, `date`, `time` | Standard-Datums-/Zeitzeichenfolgen |
| `datetime_short`, `datetime_long`, `datetime_full` | Gebietsschemaabhängige kombinierte Formate |
| `date_short`, `date_long`, `date_full` | Gebietsschemaabhängige reine Datumsformate |
| `date_ymd`, `date_dmy`, `date_mdy` | Datumsformate mit fester Reihenfolge |
| `weekday_short`, `weekday_long` | Tagesnamen |
| `day`, `month`, `month_short`, `month_long`, `year` | Einzelne Datumsbestandteile |

Verfügbare Datumsobjekte: `order.created`, `order.paid`, `order.completed`, `order.printed` und `refunds[].date`. `order.printed` wird beim Rendern aktualisiert, was für Nachdrucke nützlich ist.

## Abschnitte der obersten Ebene {#top-level-sections}

| Abschnitt | Typ | Beschreibung |
|---------|------|-------------|
| `order` | Objekt | Bestellkennung, Status, Datumsangaben, Notiz und Informationen zur Zahlungs-URL |
| `store` | Objekt | Filialidentität, Adresse, Kontaktdaten, Steuer-IDs, Logo, Öffnungszeiten und Fußzeilentext |
| `cashier` | Objekt | Benutzer, der die Bestellung verarbeitet hat |
| `customer` | Objekt | Kundenanzeigename, Adressen und Steuer-IDs |
| `lines` | Array | Produktpositionen |
| `fees` | Array | Gebührzeilen |
| `shipping` | Array | Versandzeilen |
| `discounts` | Array | Coupon-/Rabattzeilen |
| `totals` | Objekt | Bestellsummen, Zahlungssummen, Rückerstattungsübersicht und Artikelanzahl |
| `tax` | Objekt | Flags für den Steuermodus zur Steuerung von Abschnitten |
| `tax_summary` | Array | Steuerübersichtszeilen je Steuersatz |
| `has_tax_summary` | Boolesch | Hilfsprüfung für `tax_summary` |
| `payments` | Array | Zahlungszeilen |
| `refunds` | Array | Auf die Bestellung angewendete Rückerstattungsdatensätze |
| `fiscal` | Objekt | Felder der Fiskalübersicht, die von Fiskalintegrationen befüllt werden |
| `presentation_hints` | Objekt | Hinweise zur Formatierung und zum Renderer |
| `i18n` | Objekt | Übersetzte Beschriftungen für gebündelte und benutzerdefinierte Vorlagen |

## Zusammenhang der Preise {#price-model}

Eine Produktposition enthält mehrere Preise. Die Bezeichnungen werden verständlich, wenn sie direkt am Geldbetrag dargestellt werden. Dies ist die Espresso-Position aus der Beispielbestellung: ein Produkt zum regulären Preis von 17,00 $, im Angebot für 14,50 $, mit einem zusätzlichen Coupon über 10 %.

<PriceFlow
  caption="Espressobohnen — eine Einheit (die Bestellung enthält zwei)"
  scaleLabel="regulärer Preis 17.00"
  segments={[
    { kind: 'paid', amount: 13.05, label: "13.05 bezahlt" },
    { kind: 'coupon', amount: 1.45, label: "1.45 Coupon" },
    { kind: 'savings', amount: 2.5, label: "2.50 Ersparnis" },
  ]}
  keys={[
    { kind: 'paid', title: "Vom Kunden bezahlt", fields: 'unit_price → 13.05 · line_total → 26.10' },
    { kind: 'coupon', title: "Coupon-Rabatt (SUMMER10)", fields: 'lines[].discounts → 2.90 für diese Position' },
    { kind: 'savings', title: "Ersparnis gegenüber dem regulären Preis", fields: 'unit_savings → 2.50 · line_savings → 5.00' },
  ]}
  summary={[
    { label: "Alle Ersparnisse der Bestellung", value: '`totals.sale_savings_total` = 5.00' },
    { label: "Alle Coupon-Rabatte der Bestellung", value: '`totals.discount_total` = 5.28' },
    { label: "Ersparnisse und Rabatte ohne ältere Überschneidungen", value: '`totals.total_saved` = 10.28' },
  ]}
/>

Ersparnisse und Coupon-Rabatte sind nicht dasselbe: Ersparnisse vergleichen den Verkaufspreis mit dem erfassten regulären Preis, während `discounts` die Coupon-Berechnung von WooCommerce bezeichnet. WooCommerce-Bestellsummen und -Berichte enthalten nur den Coupon-Anteil. Deshalb ist `total_saved` der kundenorientierte Gesamtwert, der beide Beträge kombiniert.

## Feldreferenz {#field-reference}

Die vollständige Feldreferenz ist unten nach Abschnitten gruppiert. Standardmäßig sind alle Bereiche eingeklappt – erweitern Sie die benötigte Gruppe.

<Accordion>
  <AccordionItem question="Bestellung — Identität, Status, Datumsangaben">

### Bestellung {#order}

| Feld | Typ | Beispiel / Beschreibung |
|-------|------|-----------------------|
| `order.id` | Zahl | `1234` |
| `order.number` | Zeichenkette | Für Menschen lesbare Bestellnummer, z. B. `"10045"` |
| `order.currency` | Zeichenkette | ISO-Währungscode, z. B. `"USD"` |
| `order.customer_note` | Zeichenkette | Kunden-/Bestellnotiz |
| `order.wc_status` | Zeichenkette | Roher WooCommerce-Status-Slug, z. B. `"processing"` |
| `order.status_label` | Zeichenkette | Lokalisierte Statusbezeichnung, einschließlich benutzerdefinierter Status |
| `order.created_via` | Zeichenkette | Quelle/Kanal, z. B. `"woocommerce-pos"` |
| `order.needs_payment` | Boolescher Wert | Ob ein Zahlungsabschnitt angezeigt werden soll |
| `order.payment_url` | Zeichenkette | Zahlungs-URL der Bestellung, sofern verfügbar |
| `order.created` | Datumsobjekt | Erstellungsdatum der Bestellung |
| `order.paid` | Datumsobjekt | Zahlungsdatum; bei nicht bezahlten Bestellungen leere Zeichenketten |
| `order.completed` | Datumsobjekt | Abschlussdatum; bei nicht abgeschlossenen Bestellungen leere Zeichenketten |
| `order.printed` | Datumsobjekt | Zeitstempel für Druck/erneuten Druck zum Renderzeitpunkt |

  </AccordionItem>

  <AccordionItem question="Filiale — Identität, Adresse, Kontakt, Öffnungszeiten">

### Filiale {#store}

| Feld | Typ | Beispiel / Beschreibung |
|-------|------|-----------------------|
| `store.id` | Zahl | Filial-ID oder historische ID für gelöschte Filialen |
| `store.name` | Zeichenfolge | Anzeigename der Filiale |
| `store.address.address_1` | Zeichenfolge | Erste Zeile der Straßenadresse |
| `store.address.address_2` | Zeichenfolge | Zeile für Suite/Einheit |
| `store.address.city` | Zeichenfolge | Stadt/Ort |
| `store.address.state` | Zeichenfolge | Bundesland/Region |
| `store.address.postcode` | Zeichenfolge | Postleitzahl |
| `store.address.country` | Zeichenfolge | ISO-Ländercode |
| `store.address_lines` | Array | Vorformatierte Adresszeilen; für die meisten Vorlagen empfohlen |
| `store.tax_ids` | Array | Strukturierte Steuer-IDs des Unternehmens; diese anstelle einer einzelnen Steuer-ID durchlaufen |
| `store.phone` | Zeichenfolge | Telefonnummer der Filiale |
| `store.email` | Zeichenfolge | E-Mail-Adresse der Filiale |
| `store.logo` | Zeichenfolge/null | Logo-URL oder Daten-URI der Filiale |
| `store.opening_hours` | Zeichenfolge/null | Kompakter Text zu den Öffnungszeiten |
| `store.opening_hours_vertical` | Zeichenfolge/null | Mehrzeiliger Block mit Öffnungszeiten |
| `store.opening_hours_inline` | Zeichenfolge/null | Durch Kommas getrennter Text zu den Öffnungszeiten |
| `store.opening_hours_notes` | Zeichenfolge/null | Freitextnotizen zu den Öffnungszeiten |
| `store.personal_notes` | Zeichenfolge/null | Fußzeile des Belegs/persönliche Notiz |
| `store.policies_and_conditions` | Zeichenfolge/null | Text zu Rückerstattungen, Rückgaben oder Bedingungen |
| `store.footer_imprint` | Zeichenfolge/null | Rechtliches Impressum in der Fußzeile |

### Steuer-ID-Objekte {#tax-id-objects}

`store.tax_ids` und `customer.tax_ids` enthalten Objekte mit derselben Struktur:

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `type` | Zeichenfolge | Kennung wie `eu_vat`, `de_steuernummer`, `au_abn`, `br_cpf`, `us_ein` oder `other` |
| `value` | Zeichenfolge | Zu druckender Steuer-ID-Wert |
| `country` | Zeichenfolge/null | ISO-Ländercode, sofern bekannt |
| `label` | Zeichenfolge/null | Lokalisierte Anzeigenbezeichnung, wird vor der Darstellung aufgelöst |

Beispiel:

```mustache
{{#store.tax_ids}}
  {{label}}: {{value}}
{{/store.tax_ids}}
```

  </AccordionItem>

  <AccordionItem question="Kassierer — Benutzer, der die Bestellung bearbeitet hat">

### Kassierer {#cashier}

| Feld | Typ | Beispiel / Beschreibung |
|-------|------|-----------------------|
| `cashier.id` | Zahl | WordPress-Benutzer-ID, `0` falls unbekannt |
| `cashier.name` | Zeichenfolge | Anzeigename des Kassierers |

  </AccordionItem>

  <AccordionItem question="Kunde — Name, Adressen, Steuer-IDs">

### Kunde {#customer}

| Feld | Typ | Beispiel / Beschreibung |
|-------|------|-----------------------|
| `customer.id` | Zahl/null | Kunden-ID oder `null` für Gäste |
| `customer.name` | Zeichenfolge | Anzeigename des Kunden oder Gastbezeichnung |
| `customer.billing_address.*` | Objekt | WooCommerce-Felder für die Rechnungsadresse |
| `customer.shipping_address.*` | Objekt | WooCommerce-Felder für die Lieferadresse |
| `customer.tax_ids` | Array | Strukturierte Steuer-IDs des Kunden, die aus der Bestellung übernommen wurden |

Gängige Adressschlüssel sind `first_name`, `last_name`, `company`, `address_1`, `address_2`, `city`, `state`, `postcode`, `country`, `email` und `phone`.

  </AccordionItem>

  <AccordionItem question="Zeilen — Produktpositionen">

### Zeilen {#lines}

Mit `{{#lines}}...{{/lines}}` durchlaufen. Die Beispielwerte stammen von der Espresso-Position aus [der Beispielbestellung](#receipt-map).

<FieldIndex
  note="Jedes Geldfeld kommt als Paar: die Zahl (in `{{#…}}`-Prüfungen verwenden — null und `null` blenden den Abschnitt aus, und ein `null`-Feld hat überhaupt kein `_display`-Gegenstück) und die zugehörige `_display`-Zeichenfolge (zum Ausgeben verwenden). Mit incl/excl gekennzeichnete Felder liefern zusätzlich `_incl`- und `_excl`-Varianten, jeweils mit eigenem `_display`."
  placeholder="Felder filtern … versuchen Sie „savings“"
  emptyText="Keine Felder gefunden — setzen Sie den Filter zurück, um alle Felder von `lines[]` zu sehen."
  fields={[
    { name: 'key', type: 'string', desc: "Stabiler Zeilenschlüssel / Bestellpositions-ID.", sample: [{ k: 'key', v: '"412"', str: true }] },
    { name: 'sku', type: 'string', desc: "Produkt-SKU.", sample: [{ k: 'sku', v: '"ESP-500"', str: true }] },
    { name: 'name', type: 'string', desc: "Anzeigename des Produkts oder der Position.", sample: [{ k: 'name', v: '"Espresso Beans"', str: true }] },
    { name: 'qty', type: 'number', desc: "Verkaufte Menge.", sample: [{ k: 'qty', v: '2' }] },
    { name: 'qty_refunded', type: 'number', desc: "Für diese Position erstattete Menge.", sample: [{ k: 'qty_refunded', v: '0' }] },
    {
      name: 'regular_price', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Erfasster regulärer Stückpreis. Bei Bestellungen ohne POS-Preishistorie `null` — schließen Sie ihn in einen Abschnitt ein.",
      sample: [{ k: 'regular_price', v: '17' }, { k: 'regular_price_display', v: '"$17.00"', str: true }],
    },
    {
      name: 'selling_price', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Verkaufseinzelpreis vor Gutschein — der erfasste POS-Preis, sofern verfügbar, andernfalls aus der gespeicherten Zwischensumme der Position abgeleitet.",
      sample: [{ k: 'selling_price', v: '14.5' }, { k: 'selling_price_display', v: '"$14.50"', str: true }],
    },
    {
      name: 'unit_savings', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Regulärer Preis minus Verkaufspreis, auf null begrenzt. Null ist falsy, sodass abgesicherte Zeilen sich selbst ausblenden.",
      sample: [{ k: 'unit_savings', v: '2.5' }, { k: 'unit_savings_display', v: '"$2.50"', str: true }],
    },
    {
      name: 'line_regular_total', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Erfasster regulärer Preis multipliziert mit der Menge.",
      sample: [{ k: 'line_regular_total', v: '34' }, { k: 'line_regular_total_display', v: '"$34.00"', str: true }],
    },
    {
      name: 'line_selling_total', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Verkaufspreis vor Gutscheinen multipliziert mit der Menge.",
      sample: [{ k: 'line_selling_total', v: '29' }, { k: 'line_selling_total_display', v: '"$29.00"', str: true }],
    },
    {
      name: 'line_savings', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Gesamtersparnis dieser Position zwischen regulärem Preis und Verkaufspreis.",
      sample: [{ k: 'line_savings', v: '5' }, { k: 'line_savings_display', v: '"$5.00"', str: true }],
    },
    {
      name: 'savings_in_discounts', type: 'boolean',
      desc: "Wahr bei Bestellungen vor v1.9.0, deren Ersparnis bereits in `discounts` enthalten ist — das Kennzeichen gegen Doppelzählung.",
      sample: [{ k: 'savings_in_discounts', v: 'false' }],
    },
    {
      name: 'unit_subtotal', type: 'number', badges: ['money', 'variants'],
      desc: "WooCommerce-Stückwert vor Rabatt. Nicht als regulären Preis verwenden.",
      sample: [{ k: 'unit_subtotal', v: '14.5' }, { k: 'unit_subtotal_display', v: '"$14.50"', str: true }],
    },
    {
      name: 'unit_price', type: 'number', badges: ['money', 'variants'],
      desc: "Endgültiger Stückwert nach WooCommerce-Rabatten.",
      sample: [{ k: 'unit_price', v: '13.05' }, { k: 'unit_price_display', v: '"$13.05"', str: true }],
    },
    {
      name: 'line_subtotal', type: 'number', badges: ['money', 'variants'],
      desc: "WooCommerce-Zwischensumme der Zeile vor Rabatt.",
      sample: [{ k: 'line_subtotal', v: '29' }, { k: 'line_subtotal_display', v: '"$29.00"', str: true }],
    },
    {
      name: 'discounts', type: 'number', badges: ['money', 'variants'],
      desc: "Nativer WooCommerce-Zeilenrabatt als positiver Wert — bei aktuellen Bestellungen nur Gutscheine.",
      sample: [{ k: 'discounts', v: '2.9' }, { k: 'discounts_display', v: '"$2.90"', str: true }],
    },
    {
      name: 'line_total', type: 'number', badges: ['money', 'variants'],
      desc: "Endgültiger Zeilenbetrag nach Gutscheinen — was in der Betragsspalte gedruckt wird.",
      sample: [{ k: 'line_total', v: '26.1' }, { k: 'line_total_display', v: '"$26.10"', str: true }],
    },
    {
      name: 'total_refunded', type: 'number', badges: ['money'],
      desc: "Insgesamt für diese Zeile erstatteter Betrag als positiver Wert.",
      sample: [{ k: 'total_refunded', v: '0' }, { k: 'total_refunded_display', v: '"$0.00"', str: true }],
    },
    { name: 'taxes', type: 'array', desc: "Steuerzeilen je Steuersatz für diese Zeile." },
    { name: 'meta', type: 'array', desc: "Metadaten der Bestellposition als `{key, value}`-Paare." },
    { name: 'attributes', type: 'array', desc: "Produkt-/Variationsattribute als `{key, value}`-Paare." },
  ]}
/>

### Regulären Preis und Ersparnis anzeigen {#displaying-regular-price-and-savings}

Für ein Layout mit Stückpreisen werden der erfasste reguläre Preis, Verkaufspreis und die Ersparnis wie folgt angezeigt:

```mustache
{{#lines}}
  {{#unit_savings}}
    <span style="text-decoration: line-through;">{{regular_price_display}}</span>
    <span>{{selling_price_display}}</span>
    <span>{{i18n.savings}}: -{{unit_savings_display}}</span>
  {{/unit_savings}}
{{/lines}}
```

Verwenden Sie die Felder für die Zeilensumme, wenn die Menge relevant ist:

```mustache
{{#lines}}
  {{#line_savings}}
    <span style="text-decoration: line-through;">{{line_regular_total_display}}</span>
    <span>{{i18n.savings}}: -{{line_savings_display}}</span>
  {{/line_savings}}
{{/lines}}
```

`selling_price` ist der Preis vor Gutscheinen. Verwenden Sie `unit_price` oder `line_total` für den Endbetrag nach WooCommerce-Rabatten.

:::note Erneutes Drucken von vor v1.9.0 erstellten Bestellungen
Ältere WCPOS-Bestellungen können die Reduzierung vom regulären Preis zum Verkaufspreis bereits in `discounts` enthalten. Wenn Ihre benutzerdefinierte Vorlage sowohl Zeilenersparnisse als auch Zeilenrabatte ausgibt, verwenden Sie `savings_in_discounts`, damit dieselbe Reduzierung nicht zweimal angezeigt wird:

```mustache
{{#lines}}
  {{#line_savings}}
    <span style="text-decoration: line-through;">{{line_regular_total_display}}</span>
    {{^savings_in_discounts}}
      <span>{{i18n.savings}}: -{{line_savings_display}}</span>
    {{/savings_in_discounts}}
  {{/line_savings}}
  {{#discounts}}
    <span>{{i18n.discount}}: -{{discounts_display}}</span>
  {{/discounts}}
{{/lines}}
```

Wenn die Vorlage keine `discounts`-Zeile auf Zeilenebene ausgibt, zeigen Sie `line_savings` weiterhin an, auch wenn `savings_in_discounts` wahr ist. Die mitgelieferten Vorlagen berücksichtigen diesen Unterschied automatisch.
:::

  </AccordionItem>

  <AccordionItem question="Gebühren und Versand">

### Gebühren und Versand {#fees-and-shipping}

Durchlaufen Sie die Schleife mit `{{#fees}}...{{/fees}}` und `{{#shipping}}...{{/shipping}}`.

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `label` | Zeichenfolge | Gebührenbezeichnung oder Name der Versandart |
| `method_id` | Zeichenfolge | ID der Versandart (nur Versand) |
| `total` / `_incl` / `_excl` | Zahl | Gesamtsummen für Anzeige, inklusive und exklusive |
| `taxes` | Array | Steuerzeilen je Steuersatz |
| `meta` | Array | `{key, value}`-Metapaare |

Formatierte Varianten: `total_display`, `total_incl_display` und `total_excl_display`.

  </AccordionItem>

  <AccordionItem question="Rabatte — Coupon-/Rabattzeilen">

### Rabatte {#discounts}

Schleife mit `{{#discounts}}...{{/discounts}}`.

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `label` | Zeichenfolge | Coupon-Beschreibung oder Fallback auf den Code |
| `code` | Zeichenfolge | Coupon-Code |
| `total` / `_incl` / `_excl` | Zahl | Rabattbetrag als positiver Wert |

Formatierte Varianten: `total_display`, `total_incl_display` und `total_excl_display`. Fügen Sie in der Vorlage ein eigenes Minuszeichen hinzu, wenn Rabatte als negative Zeilen angezeigt werden sollen.

  </AccordionItem>

  <AccordionItem question="Summen — Bestell-, Zahlungs-, Rückerstattungs- und Artikelsummen">

### Summen {#totals}

Die Beispielwerte stammen aus [der Beispielbestellung](#receipt-map), für die später eine Teilrückerstattung über 7,25 $ erfolgte.

<FieldIndex
  note="Geldfelder folgen derselben Paarung wie Positionen: eine reine Zahl für Prüfungen und eine `_display`-Zeichenfolge zur Ausgabe, mit `_incl`-/`_excl`-Varianten, wo gekennzeichnet."
  placeholder="Felder filtern … versuchen Sie „saved“"
  emptyText="Keine Felder gefunden — setzen Sie den Filter zurück, um alle Felder von `totals` zu sehen."
  fields={[
    {
      name: 'subtotal', type: 'number', badges: ['money', 'variants'],
      desc: "Bestellzwischensumme vor Rabatten.",
      sample: [{ k: 'subtotal', v: '52.75' }, { k: 'subtotal_display', v: '"$52.75"', str: true }],
    },
    {
      name: 'discount_total', type: 'number', badges: ['money', 'variants'],
      desc: "Gesamtrabatt der Bestellung als positiver Wert — bei aktuellen Bestellungen nur Gutscheine.",
      sample: [{ k: 'discount_total', v: '5.28' }, { k: 'discount_total_display', v: '"$5.28"', str: true }],
    },
    {
      name: 'sale_savings_total', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Gesamte erfasste Ersparnisse zwischen regulärem Preis und Verkaufspreis über alle Produktzeilen hinweg, einschließlich Katalogangeboten und Preisänderungen an der Kasse.",
      sample: [{ k: 'sale_savings_total', v: '5' }, { k: 'sale_savings_total_display', v: '"$5.00"', str: true }],
    },
    {
      name: 'total_saved', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "Kombinierte WooCommerce-Rabatte und Ersparnisse gegenüber dem regulären Preis, ohne Überschneidungen aus älteren Versionen. Die eine kundenorientierte Angabe für alle Ersparnisse.",
      sample: [{ k: 'total_saved', v: '10.28' }, { k: 'total_saved_display', v: '"$10.28"', str: true }],
    },
    {
      name: 'total_saved_complete', type: 'boolean',
      desc: "Ob der Beleg genügend erfasste Preisdaten enthält, um die vollständigen Ersparnissummen zu berechnen.",
      sample: [{ k: 'total_saved_complete', v: 'true' }],
    },
    {
      name: 'tax_total', type: 'number', badges: ['money'],
      desc: "Gesamtsteuerbetrag.",
      sample: [{ k: 'tax_total', v: '0' }, { k: 'tax_total_display', v: '"$0.00"', str: true }],
    },
    {
      name: 'total', type: 'number', badges: ['money', 'variants'],
      desc: "Gesamtsumme der Bestellung.",
      sample: [{ k: 'total', v: '59.97' }, { k: 'total_display', v: '"$59.97"', str: true }],
    },
    {
      name: 'paid_total', type: 'number', badges: ['money'],
      desc: "Gezahlter/angerechneter Betrag.",
      sample: [{ k: 'paid_total', v: '59.97' }, { k: 'paid_total_display', v: '"$59.97"', str: true }],
    },
    {
      name: 'change_total', type: 'number', badges: ['money'],
      desc: "An den Kunden ausgezahltes Rückgeld.",
      sample: [{ k: 'change_total', v: '0' }, { k: 'change_total_display', v: '"$0.00"', str: true }],
    },
    {
      name: 'refund_total', type: 'number', badges: ['money'],
      desc: "Gesamter Rückerstattungsbetrag als positiver Wert.",
      sample: [{ k: 'refund_total', v: '7.25' }, { k: 'refund_total_display', v: '"$7.25"', str: true }],
    },
    {
      name: 'net_total', type: 'number', badges: ['money'],
      desc: "Gesamtsumme abzüglich Rückerstattungen, auf null begrenzt.",
      sample: [{ k: 'net_total', v: '52.72' }, { k: 'net_total_display', v: '"$52.72"', str: true }],
    },
    { name: 'total_qty', type: 'number', desc: "Summe der Artikelmengen.", sample: [{ k: 'total_qty', v: '4' }] },
    { name: 'line_count', type: 'number', desc: "Anzahl der Produktzeilen.", sample: [{ k: 'line_count', v: '3' }] },
  ]}
/>

Verwenden Sie `total_saved`, wenn eine einzelne kundenorientierte Angabe für alle Ersparnisse benötigt wird. Berechnen Sie diesen Wert nicht durch Addition von `discount_total` und `sale_savings_total` in der Vorlage: Bestellungen, die vor v1.9.0 erstellt wurden, enthalten ihre POS-Preisersparnis möglicherweise bereits in der Rabattsumme von WooCommerce. Das kanonische Feld `total_saved` erkennt und entfernt diese Überschneidung.

Die Ersparnissummen sind `null` und `total_saved_complete` ist false, wenn für mindestens eine Produktzeile nicht genügend aufgezeichnete Preisdaten für eine zuverlässige Gesamtsumme vorhanden sind. Sichern Sie die Zeile mit `total_saved_complete` ab und verwenden Sie dann `total_saved`, um einen Nullwert auszublenden:

```mustache
{{#totals.total_saved_complete}}
  {{#totals.total_saved}}
    <span>{{i18n.total_saved}}: {{totals.total_saved_display}}</span>
  {{/totals.total_saved}}
{{/totals.total_saved_complete}}
```

Die gebündelten preisführenden Vorlagen verwenden dieselbe Prüfstruktur mit den steuerinklusiven Varianten (`total_saved_incl` und `total_saved_incl_display`) und zeigen **Gesamtersparnis** nur an, wenn der vollständige Wert größer als null ist.

  </AccordionItem>

  <AccordionItem question="tax and tax_summary — Anzeige-Bedingungen und Zeilen je Steuersatz">

### tax und tax_summary {#tax-and-tax_summary}

Verwenden Sie `tax` für Prüfungen des Anzeigemodus und `tax_summary` für einzelne Steuersatzzeilen.

| Steuerfeld | Typ | Beschreibung |
|-----------|------|-------------|
| `tax.display` | Zeichenfolge | `incl` oder `excl` |
| `tax.display_incl` | Boolescher Wert | Wahr, wenn Preise einschließlich Steuer angezeigt werden |
| `tax.display_excl` | Boolescher Wert | Wahr, wenn Preise ohne Steuer angezeigt werden |
| `tax.breakdown` | Zeichenfolge | `hidden`, `single` oder `itemized` |
| `tax.breakdown_hidden` | Boolescher Wert | Wahr, wenn Steuerzeilen ausgeblendet werden sollen |
| `tax.breakdown_single` | Boolescher Wert | Wahr, wenn ein einzelner Steuerbetrag bevorzugt wird |
| `tax.breakdown_itemized` | Boolescher Wert | Wahr, wenn Zeilen pro Steuersatz bevorzugt werden |
| `has_tax_summary` | Boolescher Wert | Wahr, wenn `tax_summary` Zeilen enthält |

Durchlaufen Sie `tax_summary` mit `{{#tax_summary}}...{{/tax_summary}}`.

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `code` | Zeichenfolge | Steuersatz-ID/-Code |
| `rate` | Zahl/null | Steuersatz in Prozent, sofern aufgelöst |
| `label` | Zeichenfolge | Bezeichnung des Steuersatzes |
| `compound` | boolescher Wert | Ob der Steuersatz zusammengesetzt ist |
| `taxable_amount_excl` | Zahl/null | Steuerpflichtige Bemessungsgrundlage ohne Steuer |
| `tax_amount` | Zahl | Erhobene Steuer |
| `taxable_amount_incl` | Zahl/null | Steuerpflichtige Bemessungsgrundlage einschließlich Steuer |

Formatierte Varianten: `taxable_amount_excl_display`, `tax_amount_display` und `taxable_amount_incl_display`.

  </AccordionItem>

  <AccordionItem question="payments — Zahlungszeilen">

### Zahlungen {#payments}

Schleife mit `{{#payments}}...{{/payments}}`.

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `method_id` | Zeichenfolge | Kennung der Zahlungsmethode |
| `method_title` | Zeichenfolge | Angezeigter Titel der Zahlungsmethode |
| `amount` | Zahl | Auf die Bestellung angewendeter Betrag |
| `transaction_id` | Zeichenfolge | Transaktions-ID des Zahlungsgateways |
| `tendered` | Zahl | Erhaltener Bargeldbetrag, falls vorhanden |
| `change` | Zahl | Ausgezahltes Rückgeld, falls vorhanden |

Formatierte Varianten: `amount_display`, `tendered_display` und `change_display`.

  </AccordionItem>

  <AccordionItem question="refunds — Rückerstattungsdatensätze">

### Rückerstattungen {#refunds}

Mit `{{#refunds}}...{{/refunds}}` wiederholen. Rückerstattungsbeträge sind positive Werte; Vorlagen entscheiden, ob ein Minuszeichen vorangestellt oder ein separater Block für zurückgegebene Artikel gerendert wird.

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `id` | Zahl | ID des Rückerstattungsdatensatzes |
| `date` | Datumsobjekt | Erstellungsdatum der Rückerstattung |
| `amount` | Zahl | Gesamtsumme der Rückerstattung |
| `subtotal` | Zahl | Zwischensumme der erstatteten Position |
| `tax_total` | Zahl | Erstattete Steuer |
| `shipping_total` | Zahl | Erstatteter Versandbetrag |
| `shipping_tax` | Zahl | Erstattete Versandsteuer |
| `reason` | Zeichenfolge | Grund für die Rückerstattung |
| `refunded_by_id` | Zahl/null | Benutzer-ID, die die Rückerstattung ausgeführt hat |
| `refunded_by_name` | Zeichenfolge | Angezeigter Benutzername, der die Rückerstattung ausgeführt hat |
| `refunded_payment` | boolesch | Ob die Zahlung über das Zahlungsgateway erstattet wurde |
| `destination` | Zeichenfolge | `original_method`, `cash` oder `manual` |
| `gateway_id` | Zeichenfolge | Zahlungsgateway-ID für die Rückerstattung |
| `gateway_title` | Zeichenfolge | Angezeigter Titel des Zahlungsgateways |
| `processing_mode` | Zeichenfolge | Verarbeitungsmodus durch Anbieter/manuell |
| `lines` | Liste | Rückerstattete Produktzeilen |
| `fees` | Liste | Rückerstattete Gebührenzeilen |
| `shipping` | Liste | Rückerstattete Versandzeilen |

Felder für Rückerstattungszeilen umfassen `name`, `sku`, `qty`, `total`, `total_incl`, `total_excl`, `line_total`, `unit_total` und `taxes`. Rückerstattete Gebühren- und Versandzeilen verwenden `label`, `total`, `total_incl`, `total_excl` und `taxes`. Für Summen und Steuerbeträge werden Anzeigevarianten hinzugefügt.

  </AccordionItem>

  <AccordionItem question="fiscal — Momentaufnahme der Fiskalintegration">

### Fiskal {#fiscal}

Fiskalische Felder sind standardmäßig leer und werden durch Fiskalintegrationen oder die Snapshot-Anreicherung von WCPOS Pro ausgefüllt.

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `fiscal.immutable_id` | Zeichenfolge | Unveränderliche fiskalische Kennung |
| `fiscal.receipt_number` | Zeichenfolge | Fiskalbelegnummer |
| `fiscal.sequence` | Zahl/null | Sequenzzähler |
| `fiscal.hash` | Zeichenfolge | Hash-/Signaturwert |
| `fiscal.qr_payload` | Zeichenfolge | QR-Nutzdaten zur fiskalischen Verifizierung |
| `fiscal.tax_agency_code` | Zeichenfolge | Code der Steuerbehörde |
| `fiscal.signed_at` | Zeichenfolge | Zeitstempel der fiskalischen Signierung |
| `fiscal.signature_excerpt` | Zeichenfolge | Gekürzte Signatur zur Anzeige |
| `fiscal.document_label` | Zeichenfolge | Dokumentbezeichnung, z. B. Steuerrechnung |
| `fiscal.is_reprint` | Boolesch | Gibt an, ob diese Ausgabe ein Nachdruck ist |
| `fiscal.reprint_count` | Zahl | Anzahl der Nachdrucke |
| `fiscal.extra_fields` | Array/Objekt | Rechtsprechungsspezifische Werte |

  </AccordionItem>

  <AccordionItem question="presentation_hints — Hinweise zur Formatierung und zum Rendern">

### presentation_hints {#presentation_hints}

Diese Felder werden hauptsächlich vom Renderer und Formatierer verwendet. Sie stehen Vorlagen bei Bedarf zur Verfügung.

| Feld | Typ | Beschreibung |
|-------|------|-------------|
| `presentation_hints.display_tax` | Zeichenfolge | `incl`, `excl`, `hidden`, `itemized` oder `single` |
| `presentation_hints.prices_entered_with_tax` | boolescher Wert | Ob Katalogpreise Steuern enthalten |
| `presentation_hints.rounding_mode` | Zeichenfolge | WooCommerce-Einstellung für die Steuerrundung |
| `presentation_hints.locale` | Zeichenfolge | Für die Formatierung verwendetes Gebietsschema |
| `presentation_hints.timezone` | Zeichenfolge | Zeitzone des Belegs |
| `presentation_hints.currency_position` | Zeichenfolge | Position des Währungssymbols |
| `presentation_hints.currency_symbol` | Zeichenfolge | Währungssymbol |
| `presentation_hints.price_thousand_separator` | Zeichenfolge | Tausendertrennzeichen |
| `presentation_hints.price_decimal_separator` | Zeichenfolge | Dezimaltrennzeichen |
| `presentation_hints.price_num_decimals` | Zahl | Dezimalstellen |
| `presentation_hints.price_display_suffix` | Zeichenfolge | WooCommerce-Preisanzeigesuffix |
| `presentation_hints.order_barcode_type` | Zeichenfolge | Barcode-Typ, der von Galerievorlagen verwendet wird |

  </AccordionItem>

  <AccordionItem question="i18n — übersetzte Beschriftungen">

### i18n {#i18n}

Verwenden Sie nach Möglichkeit `i18n`-Beschriftungen, statt Text fest zu codieren:

```mustache
{{i18n.order}} #{{order.number}}
{{i18n.cashier}}: {{cashier.name}}
{{i18n.total}}: {{totals.total_display}}
```

Zu den gängigen Schlüsseln gehören `order`, `date`, `cashier`, `customer`, `item`, `sku`, `qty`, `unit_price`, `regular_price`, `selling_price`, `savings`, `discount`, `subtotal`, `total_saved`, `total`, `tax`, `paid`, `tendered`, `change`, `tax_summary`, `refunded`, `net_total`, `customer_note`, `thank_you_purchase`, `opening_hours` sowie die Steuernummer-Beschriftungsschlüssel wie `store_tax_id_label_eu_vat` und `customer_tax_id_label_other`. Erweiterungen können zusätzliche Schlüssel hinzufügen.

  </AccordionItem>
</Accordion>
