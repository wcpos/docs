---
title: مرجع بيانات الإيصال
sidebar_label: بيانات الإيصال
sidebar_position: 5
description: "مرجع شامل لعقد البيانات القياسي المتاح في قوالب إيصالات WCPOS."
---

import Accordion from '@site/src/components/Accordion';
import AccordionItem from '@site/src/components/AccordionItem';
import { ReceiptAnatomy, PriceFlow, FieldIndex, Recipes, Recipe } from '@site/src/components/ReceiptData';

تُعرَض قوالب HTML بلا منطق وقوالب XML الحرارية في WCPOS من حمولة بيانات الإيصال القياسية نفسها. استخدم مسارات Mustache النقطية مثل `{{order.number}}` و`{{store.name}}` و`{{totals.total_display}}`. تُعرَض المصفوفات باستخدام الأقسام:

```mustache
{{#lines}}
  {{name}} x {{qty}} — {{line_total_display}}
{{/lines}}
```

يُنتَج العقد القياسي بواسطة منشئ بيانات الإيصال في WCPOS على الخادم ويُنسَخ بواسطة عارض الإيصالات في وضع عدم الاتصال داخل التطبيق. تُفتح الإيصالات فورًا من البيانات المحلية، ثم تُحدَّث باستجابة الخادم عند توفرها، لذا يجب أن تستخدم القوالب المخصّصة الحقول أدناه بدلًا من دوال طلبات PHP.

تضيف بيانات الإيصال v1.1 حقولًا مسجَّلة للسعر الاعتيادي وسعر البيع قبل القسيمة وقيمة التوفير لكل بند منتج. كما توفّر إجماليات توفير على مستوى الطلب، بما فيها قيمة **إجمالي ما تم توفيره** التي تجمع التوفير مقابل السعر الاعتيادي مع خصومات WooCommerce دون تغيير إجماليات الطلب أو التقارير.

## الإيصال، مُخطَّطًا {#receipt-map}

كل جزء من الإيصال المطبوع يأتي من قسم واحد من عقد البيانات. مرّر المؤشر على أي من الجانبين لرؤية التطابق؛ وانقر للانتقال إلى حقول ذلك القسم. وتستخدم جميع الأمثلة في هذه الصفحة الطلب النموذجي نفسه.

<ReceiptAnatomy
  facsimile={[
    {
      id: 'store',
      rows: [
        { left: "COFFEE MONSTER", center: true, bold: true, store: true },
        { left: "12 Roast Lane, Portland OR", center: true, sub: true },
        { left: "VAT: US-998877", center: true, sub: true },
      ],
    },
    {
      id: 'order',
      rows: [
        { left: "إيصال رقم 1042", right: "13 يوليو 2026" },
        { left: "أمين الصندوق: Ada", right: "العميل: Nia", sub: true },
      ],
    },
    {
      id: 'lines',
      rows: [
        { left: "2 × Espresso Beans", right: "29.00" },
        { left: "~34.00~ · توفير −5.00", sub: true },
        { left: "1 × Ceramic Mug", right: "18.00" },
        { left: "1 × Croissant", right: "5.75" },
      ],
    },
    {
      id: 'totals',
      rows: [
        { left: "المجموع الفرعي", right: "52.75" },
        { left: "قسيمة SUMMER10", right: "−5.28" },
        { left: "تغليف الهدايا", right: "2.50" },
        { left: "الشحن", right: "10.00" },
        { left: "الإجمالي", right: "59.97", bold: true },
        { left: "إجمالي ما تم توفيره", right: "10.28", sub: true },
      ],
    },
    {
      id: 'payments',
      rows: [{ left: "بطاقة", right: "59.97" }],
    },
    {
      id: 'footer',
      rows: [{ left: "شكرًا لشرائك!", center: true, sub: true }],
    },
  ]}
  zones={[
    {
      id: 'store',
      anchor: '#store',
      label: "ترويسة المتجر",
      fields: ['store.name', 'store.address_lines', 'store.tax_ids', 'store.logo'],
    },
    {
      id: 'order',
      anchor: '#order',
      label: "الطلب والأشخاص",
      fields: ['order.number', 'order.created', 'cashier.name', 'customer.name'],
    },
    {
      id: 'lines',
      anchor: '#lines',
      label: "بنود المنتجات",
      fields: ['lines[]', 'line_savings', 'savings_in_discounts'],
    },
    {
      id: 'totals',
      anchor: '#totals',
      label: "كتلة الإجماليات",
      fields: ['totals.*', 'discounts[]', 'fees[]', 'shipping[]', 'totals.total_saved'],
    },
    {
      id: 'payments',
      anchor: '#payments',
      label: "المدفوعات",
      fields: ['payments[]'],
    },
    {
      id: 'footer',
      anchor: '#store',
      label: "التذييل",
      fields: ['store.personal_notes', 'store.policies_and_conditions', 'i18n.thank_you_purchase'],
    },
  ]}
/>

## وصفات شائعة {#common-recipes}

معظم تعديلات القوالب هي واحدة من هذه الثلاث. تعرض كل بطاقة النتيجة المطبوعة أولًا، ثم الترميز الدقيق — انسخه إلى أي قالب HTML بلا منطق أو قالب XML حراري. وتعرض البطاقة الثالثة طلبًا قديمًا سابقًا للإصدار 1.9 بدلًا من الطلب النموذجي أعلاه.

<Recipes>
  <Recipe
    title="شطب السعر الاعتيادي"
    why="يخفي حارس القسم كل شيء في الطلبات التي لا تحمل سجل أسعار مسجَّلًا."
    preview={[
      { left: "2 × Espresso Beans", right: "29.00" },
      { left: "~34.00~ · توفير −5.00", sub: true },
    ]}
    snippet={`{{#lines}}
  {{#line_savings}}
    <s>{{line_regular_total_display}}</s>
    {{i18n.savings}}: -{{line_savings_display}}
  {{/line_savings}}
{{/lines}}`}
  />
  <Recipe
    title="إضافة صف إجمالي ما تم توفيره"
    why="حارسان: يختفي الصف عندما يكون سجل الأسعار ناقصًا أو عندما لا يتحقق أي توفير."
    preview={[
      { left: "الإجمالي", right: "59.97", bold: true },
      { left: "إجمالي ما تم توفيره", right: "10.28", sub: true },
    ]}
    snippet={`{{#totals.total_saved_complete}}
  {{#totals.total_saved}}
    {{i18n.total_saved}}:
    {{totals.total_saved_display}}
  {{/totals.total_saved}}
{{/totals.total_saved_complete}}`}
  />
  <Recipe
    title="إعادة طباعة طلب سابق للإصدار 1.9 بأمان"
    why="الطلبات القديمة تحمل التوفير داخل الخصم — وهذا يمنع طباعته مرتين."
    preview={[
      { left: "1 × Espresso Beans", right: "14.50" },
      { left: "~17.00~  خصم −2.50", sub: true },
    ]}
    snippet={`{{#lines}}
  {{#line_savings}}
    {{^savings_in_discounts}}
      {{i18n.savings}}: -{{line_savings_display}}
    {{/savings_in_discounts}}
  {{/line_savings}}
{{/lines}}`}
  />
</Recipes>

## قواعد العرض {#rendering-rules}

### حقول العملة {#currency-fields}

تُحفظ حقول المبالغ الرقمية كأرقام، ويضيف العارض حقول `_display` المتوافقة مع الإعدادات المحلية لمخرجات القالب:

| الحقل الرقمي | حقل العرض |
|---------------|---------------|
| `totals.total` | `totals.total_display` |
| `lines[].line_total` | `lines[].line_total_display` |
| `payments[].amount` | `payments[].amount_display` |
| `tax_summary[].tax_amount` | `tax_summary[].tax_amount_display` |

يُفضَّل استخدام حقول `_display` عند طباعة الإيصالات. تُستخدم الحقول الرقمية فقط للأقسام الشرطية أو المخرجات القابلة للقراءة آليًا.

### الحقول المتوافقة مع عرض الضريبة {#tax-display-aware-fields}

تتوفّر لعدة حقول صيغ شاملة وحصرية للضريبة بالإضافة إلى قيمة عرض مُيسَّرة. تتبع قيمة العرض المُيسَّرة إعداد عرض ضريبة السلة في المتجر.

| الحقل المُيسَّر | الحقل الشامل للضريبة | الحقل الحصري للضريبة |
|-------------------|-----------------|-----------------|
| `lines[].regular_price` | `lines[].regular_price_incl` | `lines[].regular_price_excl` |
| `lines[].selling_price` | `lines[].selling_price_incl` | `lines[].selling_price_excl` |
| `lines[].unit_savings` | `lines[].unit_savings_incl` | `lines[].unit_savings_excl` |
| `lines[].line_regular_total` | `lines[].line_regular_total_incl` | `lines[].line_regular_total_excl` |
| `lines[].line_selling_total` | `lines[].line_selling_total_incl` | `lines[].line_selling_total_excl` |
| `lines[].line_savings` | `lines[].line_savings_incl` | `lines[].line_savings_excl` |
| `lines[].unit_price` | `lines[].unit_price_incl` | `lines[].unit_price_excl` |
| `lines[].unit_subtotal` | `lines[].unit_subtotal_incl` | `lines[].unit_subtotal_excl` |
| `lines[].line_subtotal` | `lines[].line_subtotal_incl` | `lines[].line_subtotal_excl` |
| `lines[].discounts` | `lines[].discounts_incl` | `lines[].discounts_excl` |
| `lines[].line_total` | `lines[].line_total_incl` | `lines[].line_total_excl` |
| `fees[].total` | `fees[].total_incl` | `fees[].total_excl` |
| `shipping[].total` | `shipping[].total_incl` | `shipping[].total_excl` |
| `discounts[].total` | `discounts[].total_incl` | `discounts[].total_excl` |
| `totals.subtotal` | `totals.subtotal_incl` | `totals.subtotal_excl` |
| `totals.discount_total` | `totals.discount_total_incl` | `totals.discount_total_excl` |
| `totals.sale_savings_total` | `totals.sale_savings_total_incl` | `totals.sale_savings_total_excl` |
| `totals.total_saved` | `totals.total_saved_incl` | `totals.total_saved_excl` |
| `totals.total` | `totals.total_incl` | `totals.total_excl` |

### كائنات التاريخ {#date-objects}

حقول التاريخ هي كائنات بعدة صيغ منسّقة مسبقًا. يتيح ذلك تجنّب تنسيق التاريخ داخل Mustache.

| الحقل | الوصف |
|-------|-------------|
| `datetime`، `date`، `time` | سلاسل التاريخ/الوقت الافتراضية |
| `datetime_short`، `datetime_long`، `datetime_full` | صيغ مُجمَّعة مراعية للإعدادات المحلية |
| `date_short`, `date_long`, `date_full` | تنسيقات التاريخ فقط حسب الإعدادات المحلية |
| `date_ymd`, `date_dmy`, `date_mdy` | تنسيقات التاريخ بترتيب ثابت |
| `weekday_short`, `weekday_long` | أسماء الأيام |
| `day`, `month`, `month_short`, `month_long`, `year` | أجزاء التاريخ الفردية |

كائنات التاريخ المتاحة: `order.created` و`order.paid` و`order.completed` و`order.printed` و`refunds[].date`. يُحدَّث `order.printed` في وقت العرض، وهو مفيد لإعادة الطباعة.

## الأقسام الرئيسية {#top-level-sections}

| القسم | النوع | الوصف |
|---------|------|-------------|
| `order` | كائن | هوية الطلب وحالته وتواريخه وملاحظاته ومعلومات رابط الدفع |
| `store` | كائن | هوية المتجر وعنوانه وبيانات الاتصال والأرقام الضريبية والشعار وساعات العمل ونص التذييل |
| `cashier` | كائن | المستخدم الذي عالج الطلب |
| `customer` | كائن | الاسم المعروض للعميل والعناوين والأرقام الضريبية |
| `lines` | array | بنود المنتجات |
| `fees` | array | صفوف الرسوم |
| `shipping` | array | صفوف الشحن |
| `discounts` | array | صفوف القسائم/الخصومات |
| `totals` | object | إجماليات الطلب، وإجماليات الدفع، وملخص الاسترداد، وعدد العناصر |
| `tax` | object | علامات وضع عرض الضريبة لحراسة الأقسام |
| `tax_summary` | array | صفوف ملخص الضريبة حسب المعدل |
| `has_tax_summary` | boolean | حارس مختصر لـ `tax_summary` |
| `payments` | array | صفوف المدفوعات |
| `refunds` | array | سجلات الاسترداد المطبّقة على الطلب |
| `fiscal` | object | حقول اللقطة المالية التي تملؤها تكاملات الضرائب |
| `presentation_hints` | object | تلميحات التنسيق والعرض |
| `i18n` | object | تسميات مترجمة للقوالب المضمّنة والمخصّصة |

## كيف ترتبط الأسعار ببعضها {#price-model}

يحمل بند المنتج الواحد عدة أسعار، وتصبح التسميات مفهومة بمجرد رؤيتها على المبالغ نفسها. هذا هو بند الإسبريسو من الطلب النموذجي — منتج بسعر 17.00 دولارًا معروض بـ 14.50 دولارًا، مع قسيمة بنسبة 10% فوق ذلك:

<PriceFlow
  caption="Espresso Beans — وحدة واحدة (الطلب يحتوي على وحدتين)"
  scaleLabel="السعر الاعتيادي 17.00"
  segments={[
    { kind: 'paid', amount: 13.05, label: "13.05 مدفوعة" },
    { kind: 'coupon', amount: 1.45, label: "1.45" },
    { kind: 'savings', amount: 2.5, label: "2.50" },
  ]}
  keys={[
    { kind: 'paid', title: "ما يدفعه العميل", fields: 'unit_price → 13.05 · line_total → 26.10' },
    { kind: 'coupon', title: "خصم القسيمة (SUMMER10)", fields: 'lines[].discounts → 2.90 on this line' },
    { kind: 'savings', title: "التوفير مقابل السعر الاعتيادي", fields: 'unit_savings → 2.50 · line_savings → 5.00' },
  ]}
  summary={[
    { label: "توفير العروض عبر الطلب", value: '`totals.sale_savings_total` = 5.00' },
    { label: "جميع خصومات القسائم عبر الطلب", value: '`totals.discount_total` = 5.28' },
    { label: "التوفير + الخصومات، بعد إزالة التداخل القديم", value: '`totals.total_saved` = 10.28' },
  ]}
/>

توفير العروض وخصومات القسائم شيئان مختلفان: فتوفير العروض يقارن سعر البيع بالسعر الاعتيادي المسجَّل، بينما `discounts` هي حسابات القسائم الخاصة بـ WooCommerce. ولا تتضمّن إجماليات طلبات WooCommerce وتقاريرها سوى جزء القسائم — ولهذا يوجد `total_saved` بوصفه الرقم الوحيد الموجَّه للعميل الذي يجمع الاثنين.

## مرجع الحقول {#field-reference}

مرجع الحقول الكامل مجمّع حسب القسم أدناه. كل شيء مطوي افتراضيًا — وسّع المجموعة التي تحتاجها.

<Accordion>
  <AccordionItem question="order — الهوية والحالة والتواريخ">

### order {#order}

| الحقل | النوع | مثال / وصف |
|-------|------|-----------------------|
| `order.id` | number | `1234` |
| `order.number` | string | رقم الطلب الظاهر للعميل، مثل `"10045"` |
| `order.currency` | string | رمز العملة وفق معيار ISO، مثل `"USD"` |
| `order.customer_note` | string | ملاحظة العميل/الطلب |
| `order.wc_status` | string | رمز حالة WooCommerce الأصلي، مثل `"processing"` |
| `order.status_label` | string | تسمية الحالة المترجمة، بما في ذلك الحالات المخصّصة |
| `order.created_via` | string | المصدر/القناة، مثل `"woocommerce-pos"` |
| `order.needs_payment` | boolean | ما إذا كان يجب عرض قسم الدفع |
| `order.payment_url` | string | رابط دفع الطلب عند توفّره |
| `order.created` | date object | تاريخ إنشاء الطلب |
| `order.paid` | date object | تاريخ الدفع، سلاسل نصية فارغة عند عدم الدفع |
| `order.completed` | date object | تاريخ الإكمال، سلاسل نصية فارغة عند عدم الإكمال |
| `order.printed` | date object | الطابع الزمني للطباعة/إعادة الطباعة عند العرض |

  </AccordionItem>

  <AccordionItem question="store — الهوية والعنوان والاتصال وساعات العمل">

### store {#store}

| الحقل | النوع | مثال / وصف |
|-------|------|-----------------------|
| `store.id` | number | معرّف المتجر، أو المعرّف السابق للمتاجر المحذوفة |
| `store.name` | string | اسم المتجر المعروض |
| `store.address.address_1` | string | سطر العنوان 1 |
| `store.address.address_2` | string | سطر الجناح/الوحدة |
| `store.address.city` | string | المدينة/المنطقة المحلية |
| `store.address.state` | string | الولاية/المنطقة |
| `store.address.postcode` | string | الرمز البريدي |
| `store.address.country` | string | رمز الدولة بتنسيق ISO |
| `store.address_lines` | array | أسطر العنوان المنسّقة مسبقًا؛ يُوصى باستخدامها في معظم القوالب |
| `store.tax_ids` | array | معرّفات الضريبة المنظّمة للنشاط التجاري؛ كرّر عبر هذه المصفوفة بدلًا من استخدام معرّف ضريبي واحد |
| `store.phone` | string | هاتف المتجر |
| `store.email` | string | البريد الإلكتروني للمتجر |
| `store.logo` | string/null | عنوان URL لشعار المتجر أو معرّف URI للبيانات |
| `store.opening_hours` | string/null | نص مختصر لساعات العمل |
| `store.opening_hours_vertical` | string/null | كتلة ساعات العمل متعددة الأسطر |
| `store.opening_hours_inline` | string/null | نص ساعات العمل مفصول بفواصل |
| `store.opening_hours_notes` | string/null | ملاحظات نصية حرة حول ساعات العمل |
| `store.personal_notes` | string/null | ملاحظة شخصية / تذييل الإيصال |
| `store.policies_and_conditions` | string/null | نص الاسترجاع أو الإرجاع أو الشروط |
| `store.footer_imprint` | string/null | البصمة القانونية في التذييل |

### كائنات المعرّف الضريبي {#tax-id-objects}

يحتوي `store.tax_ids` و`customer.tax_ids` على كائنات بالبنية نفسها:

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `type` | string | معرّف مثل `eu_vat` أو `de_steuernummer` أو `au_abn` أو `br_cpf` أو `us_ein` أو `other` |
| `value` | string | قيمة المعرّف الضريبي المراد طباعتها |
| `country` | string/null | رمز الدولة وفق معيار ISO عند توفّره |
| `label` | string/null | تسمية العرض المحلية، تُحَلّ قبل العرض |

مثال:

```mustache
{{#store.tax_ids}}
  {{label}}: {{value}}
{{/store.tax_ids}}
```

  </AccordionItem>

  <AccordionItem question="cashier — المستخدم الذي عالج الطلب">

### cashier {#cashier}

| الحقل | النوع | مثال / وصف |
|-------|------|-----------------------|
| `cashier.id` | number | معرّف مستخدم WordPress، `0` عندما يكون غير معروف |
| `cashier.name` | string | الاسم المعروض للصراف |

  </AccordionItem>

  <AccordionItem question="customer — الاسم والعناوين والأرقام الضريبية">

### customer {#customer}

| الحقل | النوع | مثال / وصف |
|-------|------|-----------------------|
| `customer.id` | number/null | معرّف العميل، أو `null` للزوار |
| `customer.name` | string | الاسم المعروض للعميل، أو تسمية الزائر |
| `customer.billing_address.*` | object | حقول عنوان الفوترة في WooCommerce |
| `customer.shipping_address.*` | object | حقول عنوان الشحن في WooCommerce |
| `customer.tax_ids` | array | معرّفات الضريبة المنظّمة للعميل المأخوذة كلقطة من الطلب |

تشمل مفاتيح العنوان الشائعة `first_name` و`last_name` و`company` و`address_1` و`address_2` و`city` و`state` و`postcode` و`country` و`email` و`phone`.

  </AccordionItem>

  <AccordionItem question="lines — بنود المنتجات">

### lines {#lines}

كرّر باستخدام `{{#lines}}...{{/lines}}`. القيم النموذجية هي بند الإسبريسو من [الطلب النموذجي](#receipt-map).

<FieldIndex
  note="يأتي كل حقل مالي في صورة زوج: الرقم (استخدمه في حراس `{{#…}}` — إذ يخفي الصفر و`null` القسم، ولا يملك الحقل ذو القيمة `null` توأمًا بامتداد `_display` أصلًا) وسلسلة `_display` الخاصة به (استخدمها للطباعة). أما الحقول الموسومة بـ incl/excl فتأتي أيضًا بصيغتَي `_incl` و`_excl`، ولكل منهما `_display` خاص بها."
  placeholder="تصفية الحقول… جرّب savings"
  emptyText="لا حقول مطابقة — امسح عامل التصفية لعرض كل `lines[]`."
  fields={[
    { name: 'key', type: 'string', desc: "مفتاح البند الثابت / معرّف عنصر الطلب.", sample: [{ k: 'key', v: '"412"', str: true }] },
    { name: 'sku', type: 'string', desc: "رمز SKU للمنتج.", sample: [{ k: 'sku', v: '"ESP-500"', str: true }] },
    { name: 'name', type: 'string', desc: "اسم العرض للمنتج أو البند.", sample: [{ k: 'name', v: '"Espresso Beans"', str: true }] },
    { name: 'qty', type: 'number', desc: "الكمية المباعة.", sample: [{ k: 'qty', v: '2' }] },
    { name: 'qty_refunded', type: 'number', desc: "الكمية المستردة لهذا البند.", sample: [{ k: 'qty_refunded', v: '0' }] },
    {
      name: 'regular_price', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "سعر الوحدة الاعتيادي المسجَّل. يكون `null` في الطلبات التي لا تحمل سجل أسعار من نقطة البيع — ضعه داخل قسم.",
      sample: [{ k: 'regular_price', v: '17' }, { k: 'regular_price_display', v: '"$17.00"', str: true }],
    },
    {
      name: 'selling_price', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "سعر الوحدة المعروض قبل القسائم — وهو سعر نقطة البيع المسجَّل عند توفره، وإلا فيُشتق من المجموع الفرعي المخزَّن للبند.",
      sample: [{ k: 'selling_price', v: '14.5' }, { k: 'selling_price_display', v: '"$14.50"', str: true }],
    },
    {
      name: 'unit_savings', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "الاعتيادي ناقص سعر البيع، مع تقييد الحد الأدنى بالصفر. والصفر قيمة خاطئة منطقيًا، لذا تخفي الصفوف المحروسة نفسها.",
      sample: [{ k: 'unit_savings', v: '2.5' }, { k: 'unit_savings_display', v: '"$2.50"', str: true }],
    },
    {
      name: 'line_regular_total', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "السعر الاعتيادي المسجَّل مضروبًا في الكمية.",
      sample: [{ k: 'line_regular_total', v: '34' }, { k: 'line_regular_total_display', v: '"$34.00"', str: true }],
    },
    {
      name: 'line_selling_total', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "سعر البيع قبل القسائم مضروبًا في الكمية.",
      sample: [{ k: 'line_selling_total', v: '29' }, { k: 'line_selling_total_display', v: '"$29.00"', str: true }],
    },
    {
      name: 'line_savings', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "إجمالي التوفير من السعر الاعتيادي إلى سعر البيع لهذا البند.",
      sample: [{ k: 'line_savings', v: '5' }, { k: 'line_savings_display', v: '"$5.00"', str: true }],
    },
    {
      name: 'savings_in_discounts', type: 'boolean',
      desc: "تكون صحيحة في الطلبات السابقة للإصدار 1.9.0 التي يوجد توفيرها بالفعل داخل `discounts` — وهي علامة منع الاحتساب المزدوج.",
      sample: [{ k: 'savings_in_discounts', v: 'false' }],
    },
    {
      name: 'unit_subtotal', type: 'number', badges: ['money', 'variants'],
      desc: "قيمة الوحدة قبل الخصم في WooCommerce. لا تستخدمها بوصفها السعر الاعتيادي.",
      sample: [{ k: 'unit_subtotal', v: '14.5' }, { k: 'unit_subtotal_display', v: '"$14.50"', str: true }],
    },
    {
      name: 'unit_price', type: 'number', badges: ['money', 'variants'],
      desc: "قيمة الوحدة النهائية بعد خصومات WooCommerce.",
      sample: [{ k: 'unit_price', v: '13.05' }, { k: 'unit_price_display', v: '"$13.05"', str: true }],
    },
    {
      name: 'line_subtotal', type: 'number', badges: ['money', 'variants'],
      desc: "المجموع الفرعي للبند قبل الخصم في WooCommerce.",
      sample: [{ k: 'line_subtotal', v: '29' }, { k: 'line_subtotal_display', v: '"$29.00"', str: true }],
    },
    {
      name: 'discounts', type: 'number', badges: ['money', 'variants'],
      desc: "خصم البند الأصلي في WooCommerce كقيمة موجبة — قسائم فقط في الطلبات الحالية.",
      sample: [{ k: 'discounts', v: '2.9' }, { k: 'discounts_display', v: '"$2.90"', str: true }],
    },
    {
      name: 'line_total', type: 'number', badges: ['money', 'variants'],
      desc: "إجمالي البند النهائي بعد القسائم — وهو ما يُطبع في عمود المبلغ.",
      sample: [{ k: 'line_total', v: '26.1' }, { k: 'line_total_display', v: '"$26.10"', str: true }],
    },
    {
      name: 'total_refunded', type: 'number', badges: ['money'],
      desc: "إجمالي المبلغ المسترد لهذا البند كقيمة موجبة.",
      sample: [{ k: 'total_refunded', v: '0' }, { k: 'total_refunded_display', v: '"$0.00"', str: true }],
    },
    { name: 'taxes', type: 'array', desc: "صفوف الضريبة حسب المعدل لهذا البند." },
    { name: 'meta', type: 'array', desc: "بيانات وصفية لعنصر الطلب كأزواج `{key, value}`." },
    { name: 'attributes', type: 'array', desc: "سمات المنتج/المتغيّر كأزواج `{key, value}`." },
  ]}
/>

### عرض السعر الاعتيادي والتوفير {#displaying-regular-price-and-savings}

لتخطيط قائم على سعر الوحدة، اعرض السعر الاعتيادي المسجَّل وسعر البيع والتوفير هكذا:

```mustache
{{#lines}}
  {{#unit_savings}}
    <span style="text-decoration: line-through;">{{regular_price_display}}</span>
    <span>{{selling_price_display}}</span>
    <span>{{i18n.savings}}: -{{unit_savings_display}}</span>
  {{/unit_savings}}
{{/lines}}
```

استخدم حقول إجمالي البند عندما تكون الكمية مهمة:

```mustache
{{#lines}}
  {{#line_savings}}
    <span style="text-decoration: line-through;">{{line_regular_total_display}}</span>
    <span>{{i18n.savings}}: -{{line_savings_display}}</span>
  {{/line_savings}}
{{/lines}}
```

إن `selling_price` هو السعر قبل القسائم. استخدم `unit_price` أو `line_total` للمبلغ النهائي بعد خصومات WooCommerce.

:::note إعادة طباعة طلبات أُنشئت قبل الإصدار 1.9.0
قد تتضمّن طلبات WCPOS الأقدم بالفعل التخفيض من السعر الاعتيادي إلى سعر البيع داخل `discounts`. فإذا كان قالبك المخصّص يطبع توفير البند وخصم البند معًا، فاستخدم `savings_in_discounts` لتجنّب عرض التخفيض نفسه مرتين:

```mustache
{{#lines}}
  {{#line_savings}}
    <span style="text-decoration: line-through;">{{line_regular_total_display}}</span>
    {{^savings_in_discounts}}
      <span>{{i18n.savings}}: -{{line_savings_display}}</span>
    {{/savings_in_discounts}}
  {{/line_savings}}
  {{#discounts}}
    <span>{{i18n.discount}}: -{{discounts_display}}</span>
  {{/discounts}}
{{/lines}}
```

وإذا كان القالب لا يطبع صف `discounts` على مستوى البند، فاستمر في عرض `line_savings` حتى عندما تكون `savings_in_discounts` صحيحة. وتتعامل القوالب المضمّنة مع هذا التمييز تلقائيًا.
:::

  </AccordionItem>

  <AccordionItem question="الرسوم والشحن">

### الرسوم والشحن {#fees-and-shipping}

كرّر باستخدام `{{#fees}}...{{/fees}}` و`{{#shipping}}...{{/shipping}}`.

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `label` | string | تسمية الرسوم أو اسم طريقة الشحن |
| `method_id` | string | معرّف طريقة الشحن (للشحن فقط) |
| `total` / `_incl` / `_excl` | number | الإجماليات المعروضة، والشاملة، والحصرية للضريبة |
| `taxes` | array | صفوف الضرائب حسب المعدل |
| `meta` | array | أزواج بيانات وصفية بصيغة `{key, value}` |

الصيغ المنسّقة: `total_display` و`total_incl_display` و`total_excl_display`.

  </AccordionItem>

  <AccordionItem question="discounts — صفوف القسائم/الخصومات">

### discounts {#discounts}

كرّر باستخدام `{{#discounts}}...{{/discounts}}`.

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `label` | string | وصف القسيمة أو الرمز كقيمة بديلة |
| `code` | string | رمز القسيمة |
| `total` / `_incl` / `_excl` | number | مبلغ الخصم كقيمة موجبة |

الصيغ المنسّقة: `total_display` و`total_incl_display` و`total_excl_display`. أضف علامة السالب الخاصة بك في القالب إذا أردت عرض الخصومات كصفوف سالبة.

  </AccordionItem>

  <AccordionItem question="totals — إجماليات الطلب والدفع والاسترداد والعناصر">

### totals {#totals}

القيم النموذجية مأخوذة من [الطلب النموذجي](#receipt-map)، الذي تلقّى لاحقًا استردادًا جزئيًا بقيمة 7.25 دولارًا.

<FieldIndex
  note="تتبع الحقول المالية التزاوج نفسه المتّبع في بنود المنتجات: رقم مجرّد للحراس وسلسلة `_display` للطباعة، مع صيغتَي `_incl`/`_excl` حيثما وُسمت بذلك."
  placeholder="تصفية الحقول… جرّب saved"
  emptyText="لا حقول مطابقة — امسح عامل التصفية لعرض كل `totals`."
  fields={[
    {
      name: 'subtotal', type: 'number', badges: ['money', 'variants'],
      desc: "المجموع الفرعي للطلب قبل الخصومات.",
      sample: [{ k: 'subtotal', v: '52.75' }, { k: 'subtotal_display', v: '"$52.75"', str: true }],
    },
    {
      name: 'discount_total', type: 'number', badges: ['money', 'variants'],
      desc: "إجمالي خصم الطلب كقيمة موجبة — قسائم فقط في الطلبات الحالية.",
      sample: [{ k: 'discount_total', v: '5.28' }, { k: 'discount_total_display', v: '"$5.28"', str: true }],
    },
    {
      name: 'sale_savings_total', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "إجمالي التوفير المسجَّل من السعر الاعتيادي إلى سعر البيع عبر بنود المنتجات، بما في ذلك عروض الكتالوج وتغييرات الأسعار عند نقطة البيع.",
      sample: [{ k: 'sale_savings_total', v: '5' }, { k: 'sale_savings_total_display', v: '"$5.00"', str: true }],
    },
    {
      name: 'total_saved', type: 'number', badges: ['money', 'nullable', 'variants'],
      desc: "خصومات WooCommerce والتوفير مقابل السعر الاعتيادي مجتمعَين، بعد إزالة التداخل القديم. وهو الرقم الوحيد الموجَّه للعميل لكل ما تم توفيره.",
      sample: [{ k: 'total_saved', v: '10.28' }, { k: 'total_saved_display', v: '"$10.28"', str: true }],
    },
    {
      name: 'total_saved_complete', type: 'boolean',
      desc: "ما إذا كان لدى الإيصال بيانات أسعار مسجَّلة كافية لحساب إجماليات التوفير الكاملة.",
      sample: [{ k: 'total_saved_complete', v: 'true' }],
    },
    {
      name: 'tax_total', type: 'number', badges: ['money'],
      desc: "إجمالي مبلغ الضريبة.",
      sample: [{ k: 'tax_total', v: '0' }, { k: 'tax_total_display', v: '"$0.00"', str: true }],
    },
    {
      name: 'total', type: 'number', badges: ['money', 'variants'],
      desc: "المجموع الإجمالي للطلب.",
      sample: [{ k: 'total', v: '59.97' }, { k: 'total_display', v: '"$59.97"', str: true }],
    },
    {
      name: 'paid_total', type: 'number', badges: ['money'],
      desc: "المبلغ المدفوع/المُطبَّق.",
      sample: [{ k: 'paid_total', v: '59.97' }, { k: 'paid_total_display', v: '"$59.97"', str: true }],
    },
    {
      name: 'change_total', type: 'number', badges: ['money'],
      desc: "الباقي المُعاد للعميل.",
      sample: [{ k: 'change_total', v: '0' }, { k: 'change_total_display', v: '"$0.00"', str: true }],
    },
    {
      name: 'refund_total', type: 'number', badges: ['money'],
      desc: "إجمالي المبلغ المسترد كقيمة موجبة.",
      sample: [{ k: 'refund_total', v: '7.25' }, { k: 'refund_total_display', v: '"$7.25"', str: true }],
    },
    {
      name: 'net_total', type: 'number', badges: ['money'],
      desc: "الإجمالي ناقص المبالغ المستردة، مع تقييد الحد الأدنى بالصفر.",
      sample: [{ k: 'net_total', v: '52.72' }, { k: 'net_total_display', v: '"$52.72"', str: true }],
    },
    { name: 'total_qty', type: 'number', desc: "مجموع كميات بنود الطلب.", sample: [{ k: 'total_qty', v: '4' }] },
    { name: 'line_count', type: 'number', desc: "عدد صفوف بنود المنتجات.", sample: [{ k: 'line_count', v: '3' }] },
  ]}
/>

استخدم `total_saved` عندما تريد رقمًا واحدًا موجَّهًا للعميل لكل ما تم توفيره. ولا تحسبه بجمع `discount_total` و`sale_savings_total` داخل القالب: فالطلبات التي أُنشئت قبل الإصدار 1.9.0 قد تتضمّن بالفعل توفير سعر نقطة البيع داخل إجمالي خصم WooCommerce. أما حقل `total_saved` القياسي فيكتشف ذلك التداخل ويزيله.

تكون إجماليات التوفير `null` وتكون `total_saved_complete` خاطئة عندما يفتقر أي بند منتج إلى بيانات أسعار مسجَّلة كافية لإجمالي موثوق. احرس الصف بـ `total_saved_complete`، ثم استخدم `total_saved` لإخفاء القيمة الصفرية:

```mustache
{{#totals.total_saved_complete}}
  {{#totals.total_saved}}
    <span>{{i18n.total_saved}}: {{totals.total_saved_display}}</span>
  {{/totals.total_saved}}
{{/totals.total_saved_complete}}
```

تستخدم القوالب المضمّنة التي تعرض الأسعار بنية الحراسة نفسها مع الصيغ الشاملة للضريبة (`total_saved_incl` و`total_saved_incl_display`)، ولا تعرض **إجمالي ما تم توفيره** إلا عندما تكون القيمة الكاملة أكبر من الصفر.

  </AccordionItem>

  <AccordionItem question="tax and tax_summary — حراس العرض وصفوف كل معدل">

### tax and tax_summary {#tax-and-tax_summary}

استخدم `tax` لحراس وضع العرض و`tax_summary` لصفوف معدلات الضريبة المفصّلة.

| حقل الضريبة | النوع | الوصف |
|-----------|------|-------------|
| `tax.display` | string | `incl` أو `excl` |
| `tax.display_incl` | boolean | صحيح عندما تُعرض الأسعار شاملة الضريبة |
| `tax.display_excl` | boolean | صحيح عندما تُعرض الأسعار غير شاملة الضريبة |
| `tax.breakdown` | string | `hidden` أو `single` أو `itemized` |
| `tax.breakdown_hidden` | boolean | صحيح عندما يجب إخفاء صفوف الضريبة |
| `tax.breakdown_single` | boolean | صحيح عندما يُفضَّل عرض إجمالي ضريبة واحد |
| `tax.breakdown_itemized` | boolean | صحيح عندما تُفضَّل صفوف كل معدل |
| `has_tax_summary` | boolean | صحيح عندما يحتوي `tax_summary` على صفوف |

كرّر `tax_summary` باستخدام `{{#tax_summary}}...{{/tax_summary}}`.

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `code` | string | معرّف/رمز معدل الضريبة |
| `rate` | number/null | النسبة المئوية للمعدل عند تحديدها |
| `label` | string | تسمية معدل الضريبة |
| `compound` | boolean | ما إذا كان المعدل مركبًا |
| `taxable_amount_excl` | number/null | الوعاء الضريبي بدون الضريبة |
| `tax_amount` | number | الضريبة المحصّلة |
| `taxable_amount_incl` | number/null | الوعاء الضريبي شاملًا الضريبة |

الصيغ المنسّقة: `taxable_amount_excl_display` و`tax_amount_display` و`taxable_amount_incl_display`.

  </AccordionItem>

  <AccordionItem question="payments — صفوف المدفوعات">

### payments {#payments}

كرّر باستخدام `{{#payments}}...{{/payments}}`.

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `method_id` | string | معرّف طريقة الدفع |
| `method_title` | string | العنوان المعروض لطريقة الدفع |
| `amount` | number | المبلغ المطبّق على الطلب |
| `transaction_id` | string | معرّف معاملة البوابة |
| `tendered` | number | المبلغ النقدي المقدَّم عند توفّره |
| `change` | number | باقي المبلغ النقدي المُعاد عند توفّره |

الصيغ المنسّقة: `amount_display` و`tendered_display` و`change_display`.

  </AccordionItem>

  <AccordionItem question="refunds — سجلات الاسترداد">

### refunds {#refunds}

كرّر باستخدام `{{#refunds}}...{{/refunds}}`. مبالغ الاسترداد هي قيم مطلقة موجبة؛ تحدّد القوالب ما إذا كان يُضاف رمز سالب أو يُعرَض قسم منفصل للأصناف المُعادة.

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `id` | number | معرّف سجلّ الاسترداد |
| `date` | date object | تاريخ إنشاء الاسترداد |
| `amount` | number | إجمالي الاسترداد |
| `subtotal` | number | المجموع الفرعي للبند المسترد |
| `tax_total` | number | الضريبة المستردة |
| `shipping_total` | number | مبلغ الشحن المسترد |
| `shipping_tax` | number | ضريبة الشحن المستردة |
| `reason` | string | سبب الاسترداد |
| `refunded_by_id` | number/null | معرّف المستخدم الذي أصدر الاسترداد |
| `refunded_by_name` | string | الاسم المعروض للمستخدم الذي أصدر الاسترداد |
| `refunded_payment` | boolean | ما إذا تم استرداد الدفعة عبر البوابة |
| `destination` | string | `original_method` أو `cash` أو `manual` |
| `gateway_id` | string | معرّف البوابة المستخدمة للاسترداد |
| `gateway_title` | string | العنوان المعروض للبوابة |
| `processing_mode` | string | وضع المعالجة عبر المزوّد/يدوي |
| `lines` | array | صفوف المنتجات المستردة |
| `fees` | array | صفوف الرسوم المستردة |
| `shipping` | array | صفوف الشحن المستردة |

تتضمّن حقول بند الاسترداد `name` و`sku` و`qty` و`total` و`total_incl` و`total_excl` و`line_total` و`unit_total` و`taxes`. تستخدم صفوف الرسوم والشحن المستردة `label` و`total` و`total_incl` و`total_excl` و`taxes`. تُضاف صيغ عرض للإجماليات ومبالغ الضرائب.

  </AccordionItem>

  <AccordionItem question="fiscal — لقطة التكامل المالي">

### fiscal {#fiscal}

تكون الحقول المالية فارغة افتراضيًا وتملؤها التكاملات المالية أو إثراء لقطات WCPOS Pro.

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `fiscal.immutable_id` | string | المعرّف المالي الثابت |
| `fiscal.receipt_number` | string | رقم الإيصال المالي |
| `fiscal.sequence` | number/null | عدّاد التسلسل |
| `fiscal.hash` | string | قيمة التجزئة/التوقيع |
| `fiscal.qr_payload` | string | حمولة رمز QR للتحقق المالي |
| `fiscal.tax_agency_code` | string | رمز الجهة الضريبية |
| `fiscal.signed_at` | string | الطابع الزمني للتوقيع المالي |
| `fiscal.signature_excerpt` | string | مقتطف التوقيع المختصر للعرض |
| `fiscal.document_label` | string | تسمية المستند، مثل فاتورة ضريبية |
| `fiscal.is_reprint` | boolean | ما إذا كان هذا العرض إعادة طباعة |
| `fiscal.reprint_count` | number | عدد مرات إعادة الطباعة |
| `fiscal.extra_fields` | array/object | قيم خاصة بالولاية القضائية |

  </AccordionItem>

  <AccordionItem question="presentation_hints — تلميحات التنسيق والعرض">

### presentation_hints {#presentation_hints}

تُستهلك هذه الحقول بشكل أساسي بواسطة المُصيِّر والمُنسِّق. وهي متاحة للقوالب عند الحاجة.

| الحقل | النوع | الوصف |
|-------|------|-------------|
| `presentation_hints.display_tax` | string | `incl` أو `excl` أو `hidden` أو `itemized` أو `single` |
| `presentation_hints.prices_entered_with_tax` | boolean | ما إذا كانت أسعار الكتالوج تشمل الضريبة |
| `presentation_hints.rounding_mode` | string | إعداد تقريب الضريبة في WooCommerce |
| `presentation_hints.locale` | string | الإعدادات المحلية المستخدمة للتنسيق |
| `presentation_hints.timezone` | string | المنطقة الزمنية للإيصال |
| `presentation_hints.currency_position` | string | موضع رمز العملة |
| `presentation_hints.currency_symbol` | string | رمز العملة |
| `presentation_hints.price_thousand_separator` | string | فاصل الآلاف |
| `presentation_hints.price_decimal_separator` | string | الفاصل العشري |
| `presentation_hints.price_num_decimals` | number | عدد المنازل العشرية |
| `presentation_hints.price_display_suffix` | string | لاحقة عرض السعر في WooCommerce |
| `presentation_hints.order_barcode_type` | string | نوع الباركود المستخدم في قوالب المعرض |

  </AccordionItem>

  <AccordionItem question="i18n — التسميات المترجمة">

### i18n {#i18n}

استخدم تسميات `i18n` بدلًا من كتابة النصوص مباشرةً كلما أمكن:

```mustache
{{i18n.order}} #{{order.number}}
{{i18n.cashier}}: {{cashier.name}}
{{i18n.total}}: {{totals.total_display}}
```

تتضمّن المفاتيح الشائعة `order` و`date` و`cashier` و`customer` و`item` و`sku` و`qty` و`unit_price` و`discount` و`subtotal` و`total` و`tax` و`paid` و`tendered` و`change` و`tax_summary` و`refunded` و`net_total` و`customer_note` و`thank_you_purchase` و`opening_hours`، ومفاتيح تسميات المعرّف الضريبي مثل `store_tax_id_label_eu_vat` و`customer_tax_id_label_other`. يمكن أن تضيف الإضافات مفاتيح إضافية.

  </AccordionItem>
</Accordion>
