---
title: WCPOS WPML
sidebar_label: WPML
description: "تصفية منتجات WCPOS حسب لغة WPML وتجنّب ظهور المنتجات المترجمة المكرّرة في نقطة البيع."
---

import RequirementsList from '@site/src/components/RequirementsList';
import RequirementItem from '@site/src/components/RequirementItem';
import Steps from '@site/src/components/Steps';
import StepCard from '@site/src/components/StepCard';

# WCPOS WPML {#wcpos-wpml}

يدمج [WPML](https://wpml.org/) مع WCPOS بحيث لا تعرض نقطة البيع سوى منتجات لغة واحدة — فتتوقف النسخ المترجمة المكرّرة عن الظهور في بحث المنتجات وشبكة الكتالوج. في WCPOS Pro، يمكنك تثبيت لغة لكل متجر.

## ماذا يفعل {#what-it-does}

- يصفّي استعلامات REST الخاصة بالمنتجات والتنويعات في WCPOS بحيث تقتصر على لغة WPML واحدة.
- يعترض مسارات **المزامنة السريعة** في WCPOS (الطلبات الخفيفة التي تستخدمها نقطة البيع لتحديث فهرسها المحلي) بحيث لا تصل النسخ المترجمة المكرّرة إلى العميل أبدًا.
- تستخدم النسخة المجانية اللغة الافتراضية لـ WPML.
- تتيح نسخة Pro تجاوز اللغة لكل متجر من محرر المتجر.

## التثبيت {#installation}

<Steps>
  <StepCard number="1" title="تثبيت WPML">

قم بتثبيت وتهيئة [WPML](https://wpml.org/) بالطريقة المعتادة مع تعيين لغة واحدة على الأقل كلغة افتراضية.

  </StepCard>
  <StepCard number="2" title="تثبيت WCPOS WPML">

قم بالتثبيت من `WP Admin > نقطة البيع > الإعدادات > الإضافات`، أو قم بتنزيل أحدث إصدار من [صفحة الإصدارات على GitHub](https://github.com/wcpos/wcpos-wpml/releases) ورفعه عبر `إضافات > إضافة جديد > رفع إضافة`.

  </StepCard>
  <StepCard number="3" title="(Pro) تثبيت لغة لكل متجر" isLast>

في WCPOS Pro، قم بتحرير متجر ضمن `نقطة البيع > المتاجر` واختر لغته من قسم **اللغة** في الشريط الجانبي. اتركه على *الافتراضي* لاستخدام اللغة الافتراضية لـ WPML.

  </StepCard>
</Steps>

## مشكلات التوافق المعروفة مع WPML {#known-wpml-compatibility-issues}

هذه سلوكيات خاصة بـ WPML نفسه وليست بالتكامل — من المفيد معرفتها قبل نشر الدعم متعدد اللغات في بيئة الإنتاج:

- **الحقول المخصصة لنقطة البيع لا تُنقل عبر الترجمات.** يقوم WPML بترجمة المنتجات لكنه لا ينسخ الحقول المخصصة لـ WCPOS إلى النسخ المترجمة افتراضيًا. قد يفقد منتج مُعلَّم بـ "POS فقط" في اللغة الافتراضية هذا الإعداد في ترجماته. قم بتهيئة WPML لنسخ الحقول المخصصة لـ WCPOS أثناء الترجمة.
- **منتجات نقطة البيع فقط وأخطاء 404 على واجهة المتجر.** نظرًا لأن WPML يُنشئ صفحات واجهة المتجر لكل لغة، فقد تظهر المنتجات المخصصة لنقطة البيع فقط كأخطاء 404 عند الوصول إليها من الموقع الإلكتروني. هذا تفاعل معروف مع WPML وليس خللًا في WCPOS.

راجع [المنتجات المخصصة لنقطة البيع فقط](/products/pos-only-products) للاطلاع على عناصر التحكم في رؤية نقطة البيع ذات الصلة.

## المتطلبات {#requirements}

<RequirementsList>
  <RequirementItem label="WooCommerce">تثبيت وتفعيل WooCommerce</RequirementItem>
  <RequirementItem label="WPML">WPML مع إعداد لغة واحدة على الأقل</RequirementItem>
  <RequirementItem label="WCPOS">النسخة المجانية تعمل؛ اختيار اللغة لكل متجر يتطلب WCPOS Pro</RequirementItem>
</RequirementsList>

## ذو صلة {#related}

- [WCPOS Polylang](/extensions/polylang)
- [WCPOS WP Multilang](/extensions/wp-multilang)
- [تعدد المتاجر](/stores)
- المصدر: [github.com/wcpos/wcpos-wpml](https://github.com/wcpos/wcpos-wpml)
