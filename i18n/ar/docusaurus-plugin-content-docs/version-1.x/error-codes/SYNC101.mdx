---
title: "SYNC101: فشل الكتابة في قاعدة البيانات المحلية"
sidebar_label: SYNC101
description: "تعذّر حفظ هذا التغيير في قاعدة البيانات المحلية ولا يزال موجودًا على هذا الجهاز وحده."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## ماذا يعني هذا {#what-this-means}

تعذّر حفظ هذا التغيير في قاعدة البيانات المحلية ولا يزال موجودًا على هذا الجهاز وحده.

لا تمسح البيانات المحلية ولا تُعِد تحميلها. صدّر التشخيصات وتواصل مع الدعم قبل إعادة محاولة أي شيء أو إصلاحه.

## ما الذي ينبغي فعله {#what-to-do}

صدّر التشخيصات وتواصل مع الدعم. لا يعيد WCPOS محاولة هذا من تلقاء نفسه — أعد المحاولة عندما تكون مستعدًا.

## بياناتك {#your-data}

قد تكون البيانات على هذا الجهاز في خطر — لا تمسح البيانات المحلية. وإذا استمر هذا، فصدّر التشخيصات وتواصل مع دعم WCPOS.

## استكشاف الأخطاء {#troubleshoot}

1. إذا حدد إدخال السجل سجلًا معينًا، فدوّنه — فهو التغيير الذي لم يُحفظ. أما عمليات الكتابة الخاصة بالموقع وبيانات الاعتماد فقد تذكر العملية الفاشلة والخطأ فقط؛ فدوّن تلك التفاصيل قبل المتابعة.
2. لا تمسح بيانات الموقع، ولا تُعِد تعيين التطبيق، ولا تلغِ تثبيته: فالتغيير غير المحفوظ موجود على هذا الجهاز وحده، ومسح التخزين المحلي يحذفه نهائيًا.
3. تحقق من المساحة التخزينية المتاحة على الجهاز — فامتلاء القرص أو نفاد حصة التخزين في المتصفح هو السبب الأكثر شيوعًا لفشل الكتابة المحلية.
4. أعد تشغيل WCPOS مرة واحدة وأجرِ تعديلًا صغيرًا غير مؤذٍ للتأكد مما إذا كانت التغييرات الجديدة تُحفظ.
5. إذا استمر فشل عمليات الكتابة، فاستخدم نسخ معلومات التصحيح في شاشة السجلات وتواصل مع الدعم قبل محاولة أي إصلاح.

## أين تبحث {#where-to-look}

عندما يتمكن WCPOS من حفظ هذا الخطأ، يُسجَّل على الجهاز الذي أثاره. افتح **صحة المتجر → السجلات** (رمز نبض القلب أسفل درج التنقل)، وابحث عن الإدخال المعلَّم بهذا الرمز ووسّعه: يعرض الصف الموسّع السبب بلغة واضحة والسياق الذي التُقط لحظة حدوث العطل. وفي حالة طلب موجّه إلى المتجر، قد يشمل ذلك السياق رمز الخطأ الخاص بالخادم (`serverCode`)، أو حالة HTTP (`status`)، أو نقطة النهاية (`endpoint`)؛ وتعتمد الحقول المعروضة على موضع حدوث العطل. وعند الإبلاغ عن مشكلة، استخدم **نسخ معلومات التصحيح** أعلى شاشة السجلات (**مشاركة معلومات التصحيح** على الهواتف والأجهزة اللوحية) بدلًا من لقطات الشاشة: فهي تجمع إصدار التطبيق وحالة الاتصال وأحدث الأخطاء. وتُحفظ السجلات لمدة أقصاها 30 يومًا، لذا اجمعها بينما المشكلة لا تزال حديثة. وانسخ أيضًا أي خطأ في وحدة تحكم المتصفح ظهر قبل أن يتمكن POS من كتابة إدخال السجل الخاص به.

إلى جانب السجل داخل التطبيق، قد يترك هذا العطل أثرًا في:

- **وحدة تحكم المتصفح (Console)** (الويب وسطح المكتب): يسجّل تبويب **Console** في أدوات المطوّر نفسها أخطاء جانب العميل، بما فيها الأخطاء التي تقع قبل أن يتمكن POS من كتابة إدخال السجل الخاص به.

## التفاصيل {#details}

- **الرمز:** `SYNC101` (`LOCAL_DB_WRITE_FAILED`)
- **الخطورة:** خطأ
- **أُضيف في:** WCPOS 1.10.0
