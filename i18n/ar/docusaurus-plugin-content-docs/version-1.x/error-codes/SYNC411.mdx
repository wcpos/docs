---
title: "SYNC411: إغراق بطلبات عند الحاجة"
sidebar_label: SYNC411
description: "طلب هذا الجهاز البيانات من المتجر بمعدل أكبر بكثير من المعتاد."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## ماذا يعني هذا {#what-this-means}

طلب هذا الجهاز البيانات من المتجر بمعدل أكبر بكثير من المعتاد.

لم يُبطأ أي شيء ولم يُحجب، ولا تتأثر أي بيانات طلبات — فهذا إنذار للرصد فقط. وعادةً ما يدل الإغراق المستمر بالطلبات على مشكلة في التطبيق (شاشة تعيد طلب البيانات نفسها في حلقة) لا على أي شيء فعلته. وإذا استمر ظهور هذا الرمز، فصدّر التشخيصات وتواصل مع الدعم كي يمكن العثور على الحلقة وإصلاحها.

## ما الذي ينبغي فعله {#what-to-do}

يمكنك مواصلة العمل — فـ WCPOS يتولى هذا تلقائيًا. يعيد WCPOS محاولة هذا تلقائيًا.

## بياناتك {#your-data}

لا تتأثر أي بيانات طلبات أو منتجات. وإذا استمر هذا، فصدّر التشخيصات وتواصل مع دعم WCPOS.

## استكشاف الأخطاء {#troubleshoot}

1. تابع عملك — فهذا إنذار للرصد فقط. لا يبطئ WCPOS أي طلب ولا يضعه في قائمة انتظار ولا يُسقطه، ولا تتأثر أي بيانات طلبات أو منتجات.
2. إذا ظهر الرمز مرة واحدة ثم توقف، فلا حاجة إلى أي إجراء. أما إذا تكرر، فدوّن الشاشة التي كانت مفتوحة والإجراء الذي كنت تنفذه عند ظهوره.
3. استخدم نسخ معلومات التصحيح بينما لا يزال الحدث ضمن نافذة السجل البالغة 30 يومًا وتواصل مع دعم WCPOS؛ فالتصدير يتيح للدعم تتبع حلقة الطلبات.

## أين تبحث {#where-to-look}

عندما يتمكن WCPOS من حفظ هذا الخطأ، يُسجَّل على الجهاز الذي أثاره. افتح **صحة المتجر → السجلات** (رمز نبض القلب أسفل درج التنقل)، وابحث عن الإدخال المعلَّم بهذا الرمز ووسّعه: يعرض الصف الموسّع السبب بلغة واضحة والسياق الذي التُقط لحظة حدوث العطل. وفي حالة طلب موجّه إلى المتجر، قد يشمل ذلك السياق رمز الخطأ الخاص بالخادم (`serverCode`)، أو حالة HTTP (`status`)، أو نقطة النهاية (`endpoint`)؛ وتعتمد الحقول المعروضة على موضع حدوث العطل. وعند الإبلاغ عن مشكلة، استخدم **نسخ معلومات التصحيح** أعلى شاشة السجلات (**مشاركة معلومات التصحيح** على الهواتف والأجهزة اللوحية) بدلًا من لقطات الشاشة: فهي تجمع إصدار التطبيق وحالة الاتصال وأحدث الأخطاء. وتُحفظ السجلات لمدة أقصاها 30 يومًا، لذا اجمعها بينما المشكلة لا تزال حديثة. وانسخ أيضًا أي خطأ في وحدة تحكم المتصفح ظهر قبل أن يتمكن POS من كتابة إدخال السجل الخاص به.

## التفاصيل {#details}

- **الرمز:** `SYNC411` (`DEMAND_REQUEST_FLOOD`)
- **الخطورة:** تحذير
- **أُضيف في:** WCPOS 1.10.0
