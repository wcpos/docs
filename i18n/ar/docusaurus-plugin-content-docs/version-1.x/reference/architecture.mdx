---
title: البنية التقنية
sidebar_label: البنية التقنية
sidebar_position: 2
---

import LocalizedSVG from '@site/src/components/LocalizedSVG';

هذه الصفحة توضح البنية التقنية لـ WCPOS للمطورين والمستخدمين المتقدمين.

## نظام من جزأين

تم تصميم WCPOS كنظام من جزأين:

1. **برنامج PHP:** مستضاف على الخادم الخاص بك، وهو برنامج صغير نسبيًا يمتد [WooCommerce REST API](/reference/wc-rest-api).

2. **عميل JavaScript:** يعمل محليًا في المتصفح أو تطبيق سطح المكتب، وفيما بعد على تطبيقات iOS وأندرويد.

يمكنك التفكير فيه كعالمين منفصلين:

- _عالم PHP_ حيث تتم إدارة البيانات باستخدام WordPress وWooCommerce.
- _عالم JavaScript_ الذي يكرر متجر WooCommerce محليًا، مُحسّن للبحث السريع والاستخدام بدون اتصال.

<LocalizedSVG svgName="pos-client-woo-server" />

## مزامنة البيانات

يتم جلب البيانات من WooCommerce باستخدام [WooCommerce REST API](https://woocommerce.github.io/woocommerce-rest-api-docs/). هذا يعني أن نقطة البيع يجب أن تقوم بتنزيل كل منتج، وتنوع، وفئة منتج، وعميل، ومعدل ضريبة، والمزيد لتخزينها محليًا.

رغم أن هذا قد يستغرق وقتًا في البداية، إلا أنه بمجرد التنزيل تصبح متاحة فورًا دون الحاجة إلى جلبها باستمرار من الخادم.

### ما الذي يتم مزامنته؟

- المنتجات والمتغيرات
- فئات المنتجات والوسوم
- العملاء
- معدلات الضرائب وفئات الضرائب
- بوابات الدفع (للدفع)
- الطلبات (لسجل الطلبات في الإصدار المدفوع)

### استراتيجية المزامنة

يستخدم WCPOS استراتيجية مزامنة تزايدية:

1. **المزامنة الأولية:** تنزيل جميع البيانات عند التحميل الأول
2. **التحديثات التزايدية:** يجلب فقط التغييرات منذ آخر مزامنة
3. **المزامنة الخلفية:** يتحقق بشكل دوري من وجود تحديثات
4. **المزامنة اليدوية:** يمكن للمستخدم تشغيل المزامنة في أي وقت

## إيجابيات وسلبيات البنية

| إيجابيات 😊 | سلبيات 😟 |
| ------------------------------------------------------------------ | -------------------------------------------------------- |
| البحث في البيانات المحلية فوري | إبقاء البيانات متزامنة تحديًا |
| البيانات المخزنة مؤقتًا متاحة بدون اتصال | محدود بواسطة WooCommerce REST API |
| القدرة على إنشاء تطبيقات أصلية أفضل لسطح المكتب وiOS وأندرويد | لا يمكن لقوالب WordPress والخطافات تخصيص تطبيق نقطة البيع |

## قاعدة البيانات المحلية

يخزن العميل JavaScript البيانات في IndexedDB، وهي قاعدة بيانات مستندة إلى المتصفح. وهذا يوفر:

- **الديمومة:** تبقى البيانات بعد إعادة تشغيل المتصفح
- **الأداء:** استعلامات سريعة بدون تأخير شبكة
- **التصفح بدون اتصال:** تبقى البيانات المخزنة مؤقتًا متاحة بدون إنترنت

## بنية الدفع

يستخدم عملية الدفع إطار تضمين/عرض ويب يحمل صفحة دفع الطلب الخاصة بـ WooCommerce. هذا النهج:

- **يستفيد من بوابات الدفع الموجودة:** يمكن لأي بوابة دفع في WooCommerce العمل في نقطة البيع
- **يحافظ على الأمان:** تتم معالجة الدفع من خلال البنية الآمنة لـ WooCommerce
- **يقلل التعقيد:** لا حاجة لإعادة تنفيذ تكاملات بوابات الدفع

## امتدادات API

يمتد البرنامج PHP WooCommerce REST API مع نقاط نهاية إضافية للوظائف الخاصة بنقطة البيع. انظر [WooCommerce REST API](/reference/wc-rest-api) للتفاصيل.