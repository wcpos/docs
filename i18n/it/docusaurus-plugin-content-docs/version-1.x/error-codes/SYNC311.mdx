---
title: "SYNC311: Schema non corrispondente"
sidebar_label: SYNC311
description: "I dati locali provengono da una versione incompatibile e per ora non possono essere aperti."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Cosa significa {#what-this-means}

I dati locali provengono da una versione incompatibile e per ora non possono essere aperti.

Non reimpostare la collezione locale interessata quando questo dispositivo potrebbe contenere modifiche che non hanno mai raggiunto il tuo negozio — la reimpostazione elimina l'unica copia locale. Esporta le informazioni diagnostiche per aiutare l'assistenza nelle verifiche, poi contatta l'assistenza per ricevere indicazioni sul recupero.

## Cosa fare {#what-to-do}

Esporta le informazioni diagnostiche e contatta l'assistenza. Riprova dopo aver apportato la modifica indicata sopra; riprovare senza averla apportata fallirà allo stesso modo.

## I tuoi dati {#your-data}

I dati su questo dispositivo potrebbero essere a rischio — non cancellare i dati locali. Se il problema persiste, esporta le informazioni diagnostiche e contatta l'assistenza WCPOS.

## Risoluzione dei problemi {#troubleshoot}

1. Non reimpostare né eliminare la collezione interessata — se questo dispositivo contiene modifiche che non hanno mai raggiunto il negozio, la copia locale è l'unica copia esistente.
2. Verifica se di recente l'app è stata aggiornata o riportata a una versione precedente su questo dispositivo; una mancata corrispondenza tra la versione dell'app e i dati che ha archiviato è la causa più comune.
3. Aggiorna WCPOS all'ultima versione su questo dispositivo e riapri il negozio.
4. Se la collezione continua a non aprirsi, usa Copia informazioni di debug e contatta l'assistenza per ricevere indicazioni sul recupero prima di toccare i dati locali.

## Dove cercare {#where-to-look}

Quando WCPOS riesce a salvare questo errore, viene registrato sul dispositivo che lo ha generato. Apri **Stato del negozio → Log** (l'icona a forma di battito cardiaco in fondo al menu di navigazione), individua la voce contrassegnata con questo codice ed espandila: la riga espansa mostra il motivo in linguaggio semplice e il contesto acquisito nel momento dell'errore. Per una richiesta al negozio, quel contesto può includere il codice di errore del server stesso (`serverCode`), lo `status` HTTP o l'`endpoint`; i campi mostrati dipendono da dove si è verificato l'errore. Quando segnali un problema, usa **Copia informazioni di debug** in cima alla schermata dei Log (**Condividi informazioni di debug** su smartphone e tablet) anziché gli screenshot: raccoglie la versione dell'app, lo stato della connessione e gli errori più recenti. I log vengono conservati per un massimo di 30 giorni, quindi raccoglili finché il problema è recente. Copia anche qualsiasi errore della console del browser comparso prima che il POS riuscisse a scrivere la propria voce di log.

Oltre al log presente nell'app, questo errore può lasciare tracce in:

- **Console** (web e desktop): la scheda **Console** degli stessi strumenti per sviluppatori registra gli errori lato client, compresi quelli che si verificano prima che il POS riesca a scrivere la propria voce di log.

## Dettagli {#details}

- **Codice:** `SYNC311` (`SCHEMA_MISMATCH`)
- **Gravità:** error
- **Introdotto in:** WCPOS 1.10.0
