---
title: "PAYMENT301: Gateway non disponibile"
sidebar_label: PAYMENT301
description: "Il gateway di pagamento selezionato non è disponibile per questo negozio."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Cosa significa {#what-this-means}

Il gateway di pagamento selezionato non è disponibile per questo negozio.

Chiedi all'amministratore del sito di verificare che il gateway sia supportato, attivo e configurato correttamente.

## Cosa fare {#what-to-do}

È necessaria una modifica delle impostazioni prima che questo possa funzionare. Riprova dopo aver apportato la modifica indicata sopra; riprovare senza averla apportata fallirà allo stesso modo.

## I tuoi dati {#your-data}

Nessun dato relativo a ordini o prodotti viene compromesso. Se il problema persiste, rivolgiti alla persona che gestisce il tuo sito WordPress.

## Risoluzione dei problemi {#troubleshoot}

1. Chiedi all'amministratore del sito di controllare il gateway in WooCommerce → Impostazioni → Pagamenti: deve essere installato, attivo e abilitato all'uso nel POS nelle impostazioni POS.
2. Nel frattempo completa la vendita con un altro metodo, ad esempio in contanti.
3. Se il gateway funzionava ieri, verifica se è stato aggiornato lui o WooCommerce — WooCommerce → Stato → Log registra gli errori di inizializzazione dei gateway.

## Dove cercare {#where-to-look}

Quando WCPOS riesce a salvare questo errore, viene registrato sul dispositivo che lo ha generato. Apri **Stato del negozio → Log** (l'icona a forma di battito cardiaco in fondo al menu di navigazione), individua la voce contrassegnata con questo codice ed espandila: la riga espansa mostra il motivo in linguaggio semplice e il contesto acquisito nel momento dell'errore. Per una richiesta al negozio, quel contesto può includere il codice di errore del server stesso (`serverCode`), lo `status` HTTP o l'`endpoint`; i campi mostrati dipendono da dove si è verificato l'errore. Quando segnali un problema, usa **Copia informazioni di debug** in cima alla schermata dei Log (**Condividi informazioni di debug** su smartphone e tablet) anziché gli screenshot: raccoglie la versione dell'app, lo stato della connessione e gli errori più recenti. I log vengono conservati per un massimo di 30 giorni, quindi raccoglili finché il problema è recente. Copia anche qualsiasi errore della console del browser comparso prima che il POS riuscisse a scrivere la propria voce di log.

Oltre al log presente nell'app, questo errore può lasciare tracce in:

- **Log del POS lato server**: in WP Admin, apri **POS → Impostazioni → Strumenti → Log**. Questa pagina registra gli avvisi e gli errori relativi al POS generati sul server stesso, che potrebbero non comparire mai nell'app. Un badge rosso sul menu indica errori lato server non letti.
- **WooCommerce → Stato → Log**: sul sito WordPress, controlla il file `fatal-errors-*.log` più recente per individuare gli arresti anomali di PHP, oltre a qualsiasi origine di log che porti il nome di un plugin coinvolto nell'errore. Un errore di classe 500 proveniente dal negozio lascia quasi sempre qui la propria causa.

## Dettagli {#details}

- **Codice:** `PAYMENT301` (`GATEWAY_UNAVAILABLE`)
- **Gravità:** error
- **Introdotto in:** WCPOS 1.10.0
