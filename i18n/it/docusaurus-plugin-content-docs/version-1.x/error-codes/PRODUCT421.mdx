---
title: "PRODUCT421: Metadati di prezzo del prodotto variabile non validi"
sidebar_label: PRODUCT421
description: "Non è stato possibile leggere i dati di prezzo delle variazioni di questo prodotto, quindi i prezzi visualizzati potrebbero essere errati o assenti."
---

{/* GENERATED PAGE — do not edit. Source of truth: packages/utils/src/logger/error-registry.json in wcpos/monorepo. Regenerate with `pnpm generate:error-codes`. */}

## Cosa significa {#what-this-means}

Non è stato possibile leggere i dati di prezzo delle variazioni di questo prodotto, quindi i prezzi visualizzati potrebbero essere errati o assenti.

Risalva il prodotto in WooCommerce per ricostruire i suoi dati di prezzo, oppure contatta l'assistenza se molti prodotti presentano questo problema.

## Cosa fare {#what-to-do}

Controlla i dettagli qui sotto prima di riprovare. Riprova dopo aver apportato la modifica indicata sopra; riprovare senza averla apportata fallirà allo stesso modo.

## I tuoi dati {#your-data}

Nessun dato relativo a ordini o prodotti viene compromesso. Se il problema persiste, rivolgiti all'amministratore del tuo negozio.

## Risoluzione dei problemi {#troubleshoot}

1. Apri il prodotto in WP Admin e risalvalo — così vengono ricostruiti i dati di prezzo che il POS non è riuscito a leggere.
2. Verifica che ogni variazione del prodotto abbia un prezzo impostato; una variazione senza prezzo produce intervalli di prezzo illeggibili.
3. Se molti prodotti mostrano questo codice contemporaneamente, è probabile che un plugin di gestione dei prezzi scriva i dati di prezzo in un formato che il POS non riesce a leggere — annota quale plugin gestisce i prezzi e contatta l'assistenza.

## Dove cercare {#where-to-look}

Quando WCPOS riesce a salvare questo errore, viene registrato sul dispositivo che lo ha generato. Apri **Stato del negozio → Log** (l'icona a forma di battito cardiaco in fondo al menu di navigazione), individua la voce contrassegnata con questo codice ed espandila: la riga espansa mostra il motivo in linguaggio semplice e il contesto acquisito nel momento dell'errore. Per una richiesta al negozio, quel contesto può includere il codice di errore del server stesso (`serverCode`), lo `status` HTTP o l'`endpoint`; i campi mostrati dipendono da dove si è verificato l'errore. Quando segnali un problema, usa **Copia informazioni di debug** in cima alla schermata dei Log (**Condividi informazioni di debug** su smartphone e tablet) anziché gli screenshot: raccoglie la versione dell'app, lo stato della connessione e gli errori più recenti. I log vengono conservati per un massimo di 30 giorni, quindi raccoglili finché il problema è recente. Copia anche qualsiasi errore della console del browser comparso prima che il POS riuscisse a scrivere la propria voce di log.

## Dettagli {#details}

- **Codice:** `PRODUCT421` (`VARIABLE_PRICE_META_INVALID`)
- **Gravità:** warn
- **Introdotto in:** WCPOS 1.10.0
